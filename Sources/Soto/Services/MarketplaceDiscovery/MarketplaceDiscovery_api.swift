//===----------------------------------------------------------------------===//
//
// This source file is part of the Soto for AWS open source project
//
// Copyright (c) 2017-2024 the Soto project authors
// Licensed under Apache License v2.0
//
// See LICENSE.txt for license information
// See CONTRIBUTORS.txt for the list of Soto project authors
//
// SPDX-License-Identifier: Apache-2.0
//
//===----------------------------------------------------------------------===//

// THIS FILE IS AUTOMATICALLY GENERATED by https://github.com/soto-project/soto-codegenerator.
// DO NOT EDIT.

#if canImport(FoundationEssentials)
import FoundationEssentials
#else
import Foundation
#endif
@_exported import SotoCore

/// Service object for interacting with AWS MarketplaceDiscovery service.
///
/// The AWS Marketplace Discovery API provides programmatic access to the AWS Marketplace catalog, including searching and browsing listings, retrieving product details and fulfillment options, and accessing public and private offer pricing and terms.
public struct MarketplaceDiscovery: AWSService {
    // MARK: Member variables

    /// Client used for communication with AWS
    public let client: AWSClient
    /// Service configuration
    public let config: AWSServiceConfig

    // MARK: Initialization

    /// Initialize the MarketplaceDiscovery client
    /// - parameters:
    ///     - client: AWSClient used to process requests
    ///     - region: Region of server you want to communicate with. This will override the partition parameter.
    ///     - partition: AWS partition where service resides, standard (.aws), china (.awscn), government (.awsusgov).
    ///     - endpoint: Custom endpoint URL to use instead of standard AWS servers
    ///     - middleware: Middleware chain used to edit requests before they are sent and responses before they are decoded 
    ///     - timeout: Timeout value for HTTP requests
    ///     - byteBufferAllocator: Allocator for ByteBuffers
    ///     - options: Service options
    public init(
        client: AWSClient,
        region: SotoCore.Region? = nil,
        partition: AWSPartition = .aws,
        endpoint: String? = nil,
        middleware: AWSMiddlewareProtocol? = nil,
        timeout: TimeAmount? = nil,
        byteBufferAllocator: ByteBufferAllocator = ByteBufferAllocator(),
        options: AWSServiceConfig.Options = []
    ) {
        self.client = client
        self.config = AWSServiceConfig(
            region: region,
            partition: region?.partition ?? partition,
            serviceName: "MarketplaceDiscovery",
            serviceIdentifier: "discovery-marketplace",
            signingName: "aws-marketplace",
            serviceProtocol: .restjson,
            apiVersion: "2026-02-05",
            endpoint: endpoint,
            errorType: MarketplaceDiscoveryErrorType.self,
            middleware: middleware,
            timeout: timeout,
            byteBufferAllocator: byteBufferAllocator,
            options: options
        )
    }





    // MARK: API Calls

    /// Provides details about a listing, such as descriptions, badges, categories, pricing model summaries, reviews, and associated products and offers.
    @Sendable
    @inlinable
    public func getListing(_ input: GetListingInput, logger: Logger = AWSClient.loggingDisabled) async throws -> GetListingOutput {
        try await self.client.execute(
            operation: "GetListing", 
            path: "/2026-02-05/getListing", 
            httpMethod: .POST, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    /// Provides details about a listing, such as descriptions, badges, categories, pricing model summaries, reviews, and associated products and offers.
    ///
    /// Parameters:
    ///   - listingId: The unique identifier of the listing to retrieve.
    ///   - logger: Logger use during operation
    @inlinable
    public func getListing(
        listingId: String,
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws -> GetListingOutput {
        let input = GetListingInput(
            listingId: listingId
        )
        return try await self.getListing(input, logger: logger)
    }

    /// Provides details about an offer, such as the pricing model, seller of record, availability dates, badges, and associated products.
    @Sendable
    @inlinable
    public func getOffer(_ input: GetOfferInput, logger: Logger = AWSClient.loggingDisabled) async throws -> GetOfferOutput {
        try await self.client.execute(
            operation: "GetOffer", 
            path: "/2026-02-05/getOffer", 
            httpMethod: .POST, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    /// Provides details about an offer, such as the pricing model, seller of record, availability dates, badges, and associated products.
    ///
    /// Parameters:
    ///   - offerId: The unique identifier of the offer to retrieve.
    ///   - logger: Logger use during operation
    @inlinable
    public func getOffer(
        offerId: String,
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws -> GetOfferOutput {
        let input = GetOfferInput(
            offerId: offerId
        )
        return try await self.getOffer(input, logger: logger)
    }

    /// Provides details about an offer set, which is a bundle of offers across multiple products. Includes the seller, availability dates, buyer notes, and associated product-offer pairs.
    @Sendable
    @inlinable
    public func getOfferSet(_ input: GetOfferSetInput, logger: Logger = AWSClient.loggingDisabled) async throws -> GetOfferSetOutput {
        try await self.client.execute(
            operation: "GetOfferSet", 
            path: "/2026-02-05/getOfferSet", 
            httpMethod: .POST, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    /// Provides details about an offer set, which is a bundle of offers across multiple products. Includes the seller, availability dates, buyer notes, and associated product-offer pairs.
    ///
    /// Parameters:
    ///   - offerSetId: The unique identifier of the offer set to retrieve.
    ///   - logger: Logger use during operation
    @inlinable
    public func getOfferSet(
        offerSetId: String,
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws -> GetOfferSetOutput {
        let input = GetOfferSetInput(
            offerSetId: offerSetId
        )
        return try await self.getOfferSet(input, logger: logger)
    }

    /// Returns the terms attached to an offer, such as pricing terms (usage-based, contract, BYOL, free trial), legal terms, payment schedules, validity terms, support terms, and renewal terms.
    @Sendable
    @inlinable
    public func getOfferTerms(_ input: GetOfferTermsInput, logger: Logger = AWSClient.loggingDisabled) async throws -> GetOfferTermsOutput {
        try await self.client.execute(
            operation: "GetOfferTerms", 
            path: "/2026-02-05/getOfferTerms", 
            httpMethod: .POST, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    /// Returns the terms attached to an offer, such as pricing terms (usage-based, contract, BYOL, free trial), legal terms, payment schedules, validity terms, support terms, and renewal terms.
    ///
    /// Parameters:
    ///   - maxResults: The maximum number of results that are returned per call. You can use nextToken to get more results.
    ///   - nextToken: If nextToken is returned, there are more results available. Make the call again using the returned token to retrieve the next page.
    ///   - offerId: The unique identifier of the offer whose terms to retrieve.
    ///   - logger: Logger use during operation
    @inlinable
    public func getOfferTerms(
        maxResults: Int? = nil,
        nextToken: String? = nil,
        offerId: String,
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws -> GetOfferTermsOutput {
        let input = GetOfferTermsInput(
            maxResults: maxResults, 
            nextToken: nextToken, 
            offerId: offerId
        )
        return try await self.getOfferTerms(input, logger: logger)
    }

    /// Provides details about a product, such as descriptions, highlights, categories, fulfillment option summaries, promotional media, and seller engagement options.
    @Sendable
    @inlinable
    public func getProduct(_ input: GetProductInput, logger: Logger = AWSClient.loggingDisabled) async throws -> GetProductOutput {
        try await self.client.execute(
            operation: "GetProduct", 
            path: "/2026-02-05/getProduct", 
            httpMethod: .POST, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    /// Provides details about a product, such as descriptions, highlights, categories, fulfillment option summaries, promotional media, and seller engagement options.
    ///
    /// Parameters:
    ///   - productId: The unique identifier of the product to retrieve.
    ///   - logger: Logger use during operation
    @inlinable
    public func getProduct(
        productId: String,
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws -> GetProductOutput {
        let input = GetProductInput(
            productId: productId
        )
        return try await self.getProduct(input, logger: logger)
    }

    /// Returns the fulfillment options available for a product, including deployment details such as version information, operating systems, usage instructions, and release notes.
    @Sendable
    @inlinable
    public func listFulfillmentOptions(_ input: ListFulfillmentOptionsInput, logger: Logger = AWSClient.loggingDisabled) async throws -> ListFulfillmentOptionsOutput {
        try await self.client.execute(
            operation: "ListFulfillmentOptions", 
            path: "/2026-02-05/listFulfillmentOptions", 
            httpMethod: .POST, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    /// Returns the fulfillment options available for a product, including deployment details such as version information, operating systems, usage instructions, and release notes.
    ///
    /// Parameters:
    ///   - maxResults: The maximum number of results that are returned per call. You can use nextToken to get more results.
    ///   - nextToken: If nextToken is returned, there are more results available. Make the call again using the returned token to retrieve the next page.
    ///   - productId: The unique identifier of the product for which to list fulfillment options.
    ///   - logger: Logger use during operation
    @inlinable
    public func listFulfillmentOptions(
        maxResults: Int? = nil,
        nextToken: String? = nil,
        productId: String,
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws -> ListFulfillmentOptionsOutput {
        let input = ListFulfillmentOptionsInput(
            maxResults: maxResults, 
            nextToken: nextToken, 
            productId: productId
        )
        return try await self.listFulfillmentOptions(input, logger: logger)
    }

    /// Returns the purchase options (offers and offer sets) available to the buyer. You can filter results by product, seller, purchase option type, visibility scope, and availability status.  You must include at least one of the following filters in the request: a PRODUCT_ID filter to specify the product for which to retrieve purchase options, or a VISIBILITY_SCOPE filter to retrieve purchase options by visibility.
    @Sendable
    @inlinable
    public func listPurchaseOptions(_ input: ListPurchaseOptionsInput, logger: Logger = AWSClient.loggingDisabled) async throws -> ListPurchaseOptionsOutput {
        try await self.client.execute(
            operation: "ListPurchaseOptions", 
            path: "/2026-02-05/listPurchaseOptions", 
            httpMethod: .POST, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    /// Returns the purchase options (offers and offer sets) available to the buyer. You can filter results by product, seller, purchase option type, visibility scope, and availability status.  You must include at least one of the following filters in the request: a PRODUCT_ID filter to specify the product for which to retrieve purchase options, or a VISIBILITY_SCOPE filter to retrieve purchase options by visibility.
    ///
    /// Parameters:
    ///   - filters: Filters to narrow the results. Multiple filters are combined with AND logic. Multiple values within the same filter are combined with OR logic.
    ///   - maxResults: The maximum number of results that are returned per call. You can use nextToken to get more results.
    ///   - nextToken: If nextToken is returned, there are more results available. Make the call again using the returned token to retrieve the next page.
    ///   - logger: Logger use during operation
    @inlinable
    public func listPurchaseOptions(
        filters: [PurchaseOptionFilter]? = nil,
        maxResults: Int? = nil,
        nextToken: String? = nil,
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws -> ListPurchaseOptionsOutput {
        let input = ListPurchaseOptionsInput(
            filters: filters, 
            maxResults: maxResults, 
            nextToken: nextToken
        )
        return try await self.listPurchaseOptions(input, logger: logger)
    }

    /// Returns available facet values for filtering listings, such as categories, pricing models, fulfillment option types, publishers, and customer ratings. Each facet value includes a count of matching listings.
    @Sendable
    @inlinable
    public func searchFacets(_ input: SearchFacetsInput, logger: Logger = AWSClient.loggingDisabled) async throws -> SearchFacetsOutput {
        try await self.client.execute(
            operation: "SearchFacets", 
            path: "/2026-02-05/searchFacets", 
            httpMethod: .POST, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    /// Returns available facet values for filtering listings, such as categories, pricing models, fulfillment option types, publishers, and customer ratings. Each facet value includes a count of matching listings.
    ///
    /// Parameters:
    ///   - facetTypes: A list of specific facet types to retrieve. If empty or null, all available facets are returned.
    ///   - filters: Filters to apply before retrieving facets. Multiple filters are combined with AND logic. Multiple values within the same filter are combined with OR logic.
    ///   - nextToken: If nextToken is returned, there are more results available. Make the call again using the returned token to retrieve the next page.
    ///   - searchText: The search query text to filter listings before retrieving facets.
    ///   - logger: Logger use during operation
    @inlinable
    public func searchFacets(
        facetTypes: [SearchFacetType]? = nil,
        filters: [SearchFilter]? = nil,
        nextToken: String? = nil,
        searchText: String? = nil,
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws -> SearchFacetsOutput {
        let input = SearchFacetsInput(
            facetTypes: facetTypes, 
            filters: filters, 
            nextToken: nextToken, 
            searchText: searchText
        )
        return try await self.searchFacets(input, logger: logger)
    }

    /// Returns a list of product listings based on search criteria and filters. You can search by keyword, filter by category, pricing model, fulfillment type, and other attributes, and sort results by relevance or customer rating.
    @Sendable
    @inlinable
    public func searchListings(_ input: SearchListingsInput, logger: Logger = AWSClient.loggingDisabled) async throws -> SearchListingsOutput {
        try await self.client.execute(
            operation: "SearchListings", 
            path: "/2026-02-05/searchListings", 
            httpMethod: .POST, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    /// Returns a list of product listings based on search criteria and filters. You can search by keyword, filter by category, pricing model, fulfillment type, and other attributes, and sort results by relevance or customer rating.
    ///
    /// Parameters:
    ///   - filters: Filters to narrow search results. Multiple filters are combined with AND logic. Multiple values within the same filter are combined with OR logic.
    ///   - maxResults: The maximum number of results that are returned per call. You can use nextToken to get more results.
    ///   - nextToken: If nextToken is returned, there are more results available. Make the call again using the returned token to retrieve the next page.
    ///   - searchText: The search query text to find relevant listings.
    ///   - sortBy: The field to sort results by. Valid values are RELEVANCE and AVERAGE_CUSTOMER_RATING.
    ///   - sortOrder: The sort direction. Valid values are DESCENDING and ASCENDING.
    ///   - logger: Logger use during operation
    @inlinable
    public func searchListings(
        filters: [SearchFilter]? = nil,
        maxResults: Int? = nil,
        nextToken: String? = nil,
        searchText: String? = nil,
        sortBy: SearchListingsSortBy? = nil,
        sortOrder: SearchListingsSortOrder? = nil,
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws -> SearchListingsOutput {
        let input = SearchListingsInput(
            filters: filters, 
            maxResults: maxResults, 
            nextToken: nextToken, 
            searchText: searchText, 
            sortBy: sortBy, 
            sortOrder: sortOrder
        )
        return try await self.searchListings(input, logger: logger)
    }
}

extension MarketplaceDiscovery {
    /// Initializer required by `AWSService.with(middlewares:timeout:byteBufferAllocator:options)`. You are not able to use this initializer directly as there are not public
    /// initializers for `AWSServiceConfig.Patch`. Please use `AWSService.with(middlewares:timeout:byteBufferAllocator:options)` instead.
    public init(from: MarketplaceDiscovery, patch: AWSServiceConfig.Patch) {
        self.client = from.client
        self.config = from.config.with(patch: patch)
    }
}

// MARK: Paginators

@available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
extension MarketplaceDiscovery {
    /// Return PaginatorSequence for operation ``getOfferTerms(_:logger:)``.
    ///
    /// - Parameters:
    ///   - input: Input for operation
    ///   - logger: Logger used for logging
    @inlinable
    public func getOfferTermsPaginator(
        _ input: GetOfferTermsInput,
        logger: Logger = AWSClient.loggingDisabled
    ) -> AWSClient.PaginatorSequence<GetOfferTermsInput, GetOfferTermsOutput> {
        return .init(
            input: input,
            command: self.getOfferTerms,
            inputKey: \GetOfferTermsInput.nextToken,
            outputKey: \GetOfferTermsOutput.nextToken,
            logger: logger
        )
    }
    /// Return PaginatorSequence for operation ``getOfferTerms(_:logger:)``.
    ///
    /// - Parameters:
    ///   - maxResults: The maximum number of results that are returned per call. You can use nextToken to get more results.
    ///   - offerId: The unique identifier of the offer whose terms to retrieve.
    ///   - logger: Logger used for logging
    @inlinable
    public func getOfferTermsPaginator(
        maxResults: Int? = nil,
        offerId: String,
        logger: Logger = AWSClient.loggingDisabled        
    ) -> AWSClient.PaginatorSequence<GetOfferTermsInput, GetOfferTermsOutput> {
        let input = GetOfferTermsInput(
            maxResults: maxResults, 
            offerId: offerId
        )
        return self.getOfferTermsPaginator(input, logger: logger)
    }

    /// Return PaginatorSequence for operation ``listFulfillmentOptions(_:logger:)``.
    ///
    /// - Parameters:
    ///   - input: Input for operation
    ///   - logger: Logger used for logging
    @inlinable
    public func listFulfillmentOptionsPaginator(
        _ input: ListFulfillmentOptionsInput,
        logger: Logger = AWSClient.loggingDisabled
    ) -> AWSClient.PaginatorSequence<ListFulfillmentOptionsInput, ListFulfillmentOptionsOutput> {
        return .init(
            input: input,
            command: self.listFulfillmentOptions,
            inputKey: \ListFulfillmentOptionsInput.nextToken,
            outputKey: \ListFulfillmentOptionsOutput.nextToken,
            logger: logger
        )
    }
    /// Return PaginatorSequence for operation ``listFulfillmentOptions(_:logger:)``.
    ///
    /// - Parameters:
    ///   - maxResults: The maximum number of results that are returned per call. You can use nextToken to get more results.
    ///   - productId: The unique identifier of the product for which to list fulfillment options.
    ///   - logger: Logger used for logging
    @inlinable
    public func listFulfillmentOptionsPaginator(
        maxResults: Int? = nil,
        productId: String,
        logger: Logger = AWSClient.loggingDisabled        
    ) -> AWSClient.PaginatorSequence<ListFulfillmentOptionsInput, ListFulfillmentOptionsOutput> {
        let input = ListFulfillmentOptionsInput(
            maxResults: maxResults, 
            productId: productId
        )
        return self.listFulfillmentOptionsPaginator(input, logger: logger)
    }

    /// Return PaginatorSequence for operation ``listPurchaseOptions(_:logger:)``.
    ///
    /// - Parameters:
    ///   - input: Input for operation
    ///   - logger: Logger used for logging
    @inlinable
    public func listPurchaseOptionsPaginator(
        _ input: ListPurchaseOptionsInput,
        logger: Logger = AWSClient.loggingDisabled
    ) -> AWSClient.PaginatorSequence<ListPurchaseOptionsInput, ListPurchaseOptionsOutput> {
        return .init(
            input: input,
            command: self.listPurchaseOptions,
            inputKey: \ListPurchaseOptionsInput.nextToken,
            outputKey: \ListPurchaseOptionsOutput.nextToken,
            logger: logger
        )
    }
    /// Return PaginatorSequence for operation ``listPurchaseOptions(_:logger:)``.
    ///
    /// - Parameters:
    ///   - filters: Filters to narrow the results. Multiple filters are combined with AND logic. Multiple values within the same filter are combined with OR logic.
    ///   - maxResults: The maximum number of results that are returned per call. You can use nextToken to get more results.
    ///   - logger: Logger used for logging
    @inlinable
    public func listPurchaseOptionsPaginator(
        filters: [PurchaseOptionFilter]? = nil,
        maxResults: Int? = nil,
        logger: Logger = AWSClient.loggingDisabled        
    ) -> AWSClient.PaginatorSequence<ListPurchaseOptionsInput, ListPurchaseOptionsOutput> {
        let input = ListPurchaseOptionsInput(
            filters: filters, 
            maxResults: maxResults
        )
        return self.listPurchaseOptionsPaginator(input, logger: logger)
    }

    /// Return PaginatorSequence for operation ``searchFacets(_:logger:)``.
    ///
    /// - Parameters:
    ///   - input: Input for operation
    ///   - logger: Logger used for logging
    @inlinable
    public func searchFacetsPaginator(
        _ input: SearchFacetsInput,
        logger: Logger = AWSClient.loggingDisabled
    ) -> AWSClient.PaginatorSequence<SearchFacetsInput, SearchFacetsOutput> {
        return .init(
            input: input,
            command: self.searchFacets,
            inputKey: \SearchFacetsInput.nextToken,
            outputKey: \SearchFacetsOutput.nextToken,
            logger: logger
        )
    }
    /// Return PaginatorSequence for operation ``searchFacets(_:logger:)``.
    ///
    /// - Parameters:
    ///   - facetTypes: A list of specific facet types to retrieve. If empty or null, all available facets are returned.
    ///   - filters: Filters to apply before retrieving facets. Multiple filters are combined with AND logic. Multiple values within the same filter are combined with OR logic.
    ///   - searchText: The search query text to filter listings before retrieving facets.
    ///   - logger: Logger used for logging
    @inlinable
    public func searchFacetsPaginator(
        facetTypes: [SearchFacetType]? = nil,
        filters: [SearchFilter]? = nil,
        searchText: String? = nil,
        logger: Logger = AWSClient.loggingDisabled        
    ) -> AWSClient.PaginatorSequence<SearchFacetsInput, SearchFacetsOutput> {
        let input = SearchFacetsInput(
            facetTypes: facetTypes, 
            filters: filters, 
            searchText: searchText
        )
        return self.searchFacetsPaginator(input, logger: logger)
    }

    /// Return PaginatorSequence for operation ``searchListings(_:logger:)``.
    ///
    /// - Parameters:
    ///   - input: Input for operation
    ///   - logger: Logger used for logging
    @inlinable
    public func searchListingsPaginator(
        _ input: SearchListingsInput,
        logger: Logger = AWSClient.loggingDisabled
    ) -> AWSClient.PaginatorSequence<SearchListingsInput, SearchListingsOutput> {
        return .init(
            input: input,
            command: self.searchListings,
            inputKey: \SearchListingsInput.nextToken,
            outputKey: \SearchListingsOutput.nextToken,
            logger: logger
        )
    }
    /// Return PaginatorSequence for operation ``searchListings(_:logger:)``.
    ///
    /// - Parameters:
    ///   - filters: Filters to narrow search results. Multiple filters are combined with AND logic. Multiple values within the same filter are combined with OR logic.
    ///   - maxResults: The maximum number of results that are returned per call. You can use nextToken to get more results.
    ///   - searchText: The search query text to find relevant listings.
    ///   - sortBy: The field to sort results by. Valid values are RELEVANCE and AVERAGE_CUSTOMER_RATING.
    ///   - sortOrder: The sort direction. Valid values are DESCENDING and ASCENDING.
    ///   - logger: Logger used for logging
    @inlinable
    public func searchListingsPaginator(
        filters: [SearchFilter]? = nil,
        maxResults: Int? = nil,
        searchText: String? = nil,
        sortBy: SearchListingsSortBy? = nil,
        sortOrder: SearchListingsSortOrder? = nil,
        logger: Logger = AWSClient.loggingDisabled        
    ) -> AWSClient.PaginatorSequence<SearchListingsInput, SearchListingsOutput> {
        let input = SearchListingsInput(
            filters: filters, 
            maxResults: maxResults, 
            searchText: searchText, 
            sortBy: sortBy, 
            sortOrder: sortOrder
        )
        return self.searchListingsPaginator(input, logger: logger)
    }
}

extension MarketplaceDiscovery.GetOfferTermsInput: AWSPaginateToken {
    @inlinable
    public func usingPaginationToken(_ token: String) -> MarketplaceDiscovery.GetOfferTermsInput {
        return .init(
            maxResults: self.maxResults,
            nextToken: token,
            offerId: self.offerId
        )
    }
}

extension MarketplaceDiscovery.ListFulfillmentOptionsInput: AWSPaginateToken {
    @inlinable
    public func usingPaginationToken(_ token: String) -> MarketplaceDiscovery.ListFulfillmentOptionsInput {
        return .init(
            maxResults: self.maxResults,
            nextToken: token,
            productId: self.productId
        )
    }
}

extension MarketplaceDiscovery.ListPurchaseOptionsInput: AWSPaginateToken {
    @inlinable
    public func usingPaginationToken(_ token: String) -> MarketplaceDiscovery.ListPurchaseOptionsInput {
        return .init(
            filters: self.filters,
            maxResults: self.maxResults,
            nextToken: token
        )
    }
}

extension MarketplaceDiscovery.SearchFacetsInput: AWSPaginateToken {
    @inlinable
    public func usingPaginationToken(_ token: String) -> MarketplaceDiscovery.SearchFacetsInput {
        return .init(
            facetTypes: self.facetTypes,
            filters: self.filters,
            nextToken: token,
            searchText: self.searchText
        )
    }
}

extension MarketplaceDiscovery.SearchListingsInput: AWSPaginateToken {
    @inlinable
    public func usingPaginationToken(_ token: String) -> MarketplaceDiscovery.SearchListingsInput {
        return .init(
            filters: self.filters,
            maxResults: self.maxResults,
            nextToken: token,
            searchText: self.searchText,
            sortBy: self.sortBy,
            sortOrder: self.sortOrder
        )
    }
}
