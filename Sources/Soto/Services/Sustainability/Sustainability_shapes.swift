//===----------------------------------------------------------------------===//
//
// This source file is part of the Soto for AWS open source project
//
// Copyright (c) 2017-2024 the Soto project authors
// Licensed under Apache License v2.0
//
// See LICENSE.txt for license information
// See CONTRIBUTORS.txt for the list of Soto project authors
//
// SPDX-License-Identifier: Apache-2.0
//
//===----------------------------------------------------------------------===//

// THIS FILE IS AUTOMATICALLY GENERATED by https://github.com/soto-project/soto-codegenerator.
// DO NOT EDIT.

#if canImport(FoundationEssentials)
import FoundationEssentials
#else
import Foundation
#endif
@_spi(SotoInternal) import SotoCore

extension Sustainability {
    // MARK: Enums

    public enum Dimension: String, CustomStringConvertible, Codable, Sendable, CodingKeyRepresentable {
        /// The geographical area containing data center clusters where Amazon Web Services services are hosted.
        case region = "REGION"
        /// The cloud computing product and solution offered by Amazon Web Services.
        case service = "SERVICE"
        /// The account ID whose Amazon Web Services usage is associated with the estimated environmental impact data.
        case usageAccountId = "USAGE_ACCOUNT_ID"
        public var description: String { return self.rawValue }
    }

    public enum EmissionsType: String, CustomStringConvertible, Codable, Sendable, CodingKeyRepresentable {
        /// Total direct, indirect, and value chain emissions, calculated using GHG Protocol's Location-based method (LBM).
        case totalLbmCarbonEmissions = "TOTAL_LBM_CARBON_EMISSIONS"
        /// Total direct, indirect, and value chain emissions, calculated using GHG Protocol's Market-based method (MBM).
        case totalMbmCarbonEmissions = "TOTAL_MBM_CARBON_EMISSIONS"
        /// Total direct emissions from Amazon Web Services owned or controlled sources.
        case totalScope1CarbonEmissions = "TOTAL_SCOPE_1_CARBON_EMISSIONS"
        /// Total indirect emissions from the production of purchased energy, calculated using GHG Protocol's Location-based method (LBM).
        case totalScope2LbmCarbonEmissions = "TOTAL_SCOPE_2_LBM_CARBON_EMISSIONS"
        /// Total indirect emissions from the production of purchased energy, calculated using GHG Protocol's Market-based method (MBM).
        case totalScope2MbmCarbonEmissions = "TOTAL_SCOPE_2_MBM_CARBON_EMISSIONS"
        /// Total value chain emissions, calculated using GHG Protocol's Location-based method (LBM).
        case totalScope3LbmCarbonEmissions = "TOTAL_SCOPE_3_LBM_CARBON_EMISSIONS"
        /// Total value chain emissions, calculated using GHG Protocol's Market-based method (MBM).
        case totalScope3MbmCarbonEmissions = "TOTAL_SCOPE_3_MBM_CARBON_EMISSIONS"
        public var description: String { return self.rawValue }
    }

    public enum EmissionsUnit: String, CustomStringConvertible, Codable, Sendable, CodingKeyRepresentable {
        /// Metric tons of carbon dioxide-equivalent (MTCO2e).
        case mtCo2E = "MTCO2e"
        public var description: String { return self.rawValue }
    }

    public enum TimeGranularity: String, CustomStringConvertible, Codable, Sendable, CodingKeyRepresentable {
        /// Environmental impact data aggregated by calendar month.
        case monthly = "MONTHLY"
        /// Environmental impact data aggregated over calendar quarter periods (Q1: Jan-Mar, Q2: Apr-Jun, Q3: Jul-Sep, Q4: Oct-Dec).
        case quarterlyCalendar = "QUARTERLY_CALENDAR"
        /// Environmental impact data aggregated over fiscal quarter periods based on the fiscal year start month specified in GranularityConfiguration.FiscalYearStartMonth.
        case quarterlyFiscal = "QUARTERLY_FISCAL"
        /// Environmental impact data aggregated over calendar year periods (January-December).
        case yearlyCalendar = "YEARLY_CALENDAR"
        /// Environmental impact data aggregated over fiscal year periods starting from the month specified in GranularityConfiguration.FiscalYearStartMonth.
        case yearlyFiscal = "YEARLY_FISCAL"
        public var description: String { return self.rawValue }
    }

    public enum WaterAllocationType: String, CustomStringConvertible, Codable, Sendable, CodingKeyRepresentable {
        /// Total water drawn from surface water, groundwater, seawater, or a third party associated with Amazon Web Services account usage.
        case totalWaterWithdrawals = "TOTAL_WATER_WITHDRAWALS"
        public var description: String { return self.rawValue }
    }

    public enum WaterAllocationUnit: String, CustomStringConvertible, Codable, Sendable, CodingKeyRepresentable {
        /// Cubic meters of water.
        case cubicMeters = "m3"
        public var description: String { return self.rawValue }
    }

    // MARK: Shapes

    public struct DimensionEntry: AWSDecodableShape {
        /// The dimension type that categorizes this entry.
        public let dimension: Dimension
        ///  The value for the specified dimension. Valid values vary based on the dimension type (e.g., us-east-1 for the REGION dimension, AmazonEC2 for the SERVICE dimension).
        public let value: String

        @inlinable
        public init(dimension: Dimension, value: String) {
            self.dimension = dimension
            self.value = value
        }

        private enum CodingKeys: String, CodingKey {
            case dimension = "Dimension"
            case value = "Value"
        }
    }

    public struct Emissions: AWSDecodableShape {
        /// The unit of measurement for the emissions value.
        public let unit: EmissionsUnit
        /// The numeric value of the emissions quantity.
        public let value: Double

        @inlinable
        public init(unit: EmissionsUnit, value: Double) {
            self.unit = unit
            self.value = value
        }

        private enum CodingKeys: String, CodingKey {
            case unit = "Unit"
            case value = "Value"
        }
    }

    public struct EstimatedCarbonEmissions: AWSDecodableShape {
        /// The dimensions used to group emissions values.
        public let dimensionsValues: [Dimension: String]
        /// The emissions values for the requested emissions types.
        public let emissionsValues: [EmissionsType: Emissions]
        /// The semantic version-formatted string that indicates the methodology version used to calculate the emission values.    The AWS Sustainability service reflects the most recent model version for every month. You will not see two entries for the same month with different ModelVersion values. To track the evolution of the methodology and compare emission values from previous versions, we recommend creating a Data Export.
        public let modelVersion: String
        /// The reporting period for emission values.
        public let timePeriod: TimePeriod

        @inlinable
        public init(dimensionsValues: [Dimension: String], emissionsValues: [EmissionsType: Emissions], modelVersion: String, timePeriod: TimePeriod) {
            self.dimensionsValues = dimensionsValues
            self.emissionsValues = emissionsValues
            self.modelVersion = modelVersion
            self.timePeriod = timePeriod
        }

        private enum CodingKeys: String, CodingKey {
            case dimensionsValues = "DimensionsValues"
            case emissionsValues = "EmissionsValues"
            case modelVersion = "ModelVersion"
            case timePeriod = "TimePeriod"
        }
    }

    public struct EstimatedWaterAllocation: AWSDecodableShape {
        /// The allocation values for the requested water allocation types.
        public let allocationValues: [WaterAllocationType: WaterAllocation]
        /// The dimensions used to group water allocation values.
        public let dimensionsValues: [Dimension: String]
        /// The semantic version-formatted string that indicates the methodology version used to calculate the water allocation values.    The AWS Sustainability service reflects the most recent model version for every month. You will not see two entries for the same month with different ModelVersion values.
        public let modelVersion: String
        /// The reporting period for water allocation values.
        public let timePeriod: TimePeriod

        @inlinable
        public init(allocationValues: [WaterAllocationType: WaterAllocation], dimensionsValues: [Dimension: String], modelVersion: String, timePeriod: TimePeriod) {
            self.allocationValues = allocationValues
            self.dimensionsValues = dimensionsValues
            self.modelVersion = modelVersion
            self.timePeriod = timePeriod
        }

        private enum CodingKeys: String, CodingKey {
            case allocationValues = "AllocationValues"
            case dimensionsValues = "DimensionsValues"
            case modelVersion = "ModelVersion"
            case timePeriod = "TimePeriod"
        }
    }

    public struct FilterExpression: AWSEncodableShape {
        /// Filters environmental impact values by specific dimension values.
        public let dimensions: [Dimension: [String]]?

        @inlinable
        public init(dimensions: [Dimension: [String]]? = nil) {
            self.dimensions = dimensions
        }

        private enum CodingKeys: String, CodingKey {
            case dimensions = "Dimensions"
        }
    }

    public struct GetEstimatedCarbonEmissionsDimensionValuesRequest: AWSEncodableShape {
        /// The dimensions available for grouping estimated carbon emissions.
        public let dimensions: [Dimension]
        /// The maximum number of results to return in a single call. Default is 1000.
        public let maxResults: Int?
        /// The pagination token specifying which page of results to return in the response. If no token is provided, the default page is the first page.
        public let nextToken: String?
        ///  The date range for fetching the dimension values. The range must include the start date of a month for that month's dimensions to be included in the response.
        public let timePeriod: TimePeriod

        @inlinable
        public init(dimensions: [Dimension], maxResults: Int? = nil, nextToken: String? = nil, timePeriod: TimePeriod) {
            self.dimensions = dimensions
            self.maxResults = maxResults
            self.nextToken = nextToken
            self.timePeriod = timePeriod
        }

        public func validate(name: String) throws {
            try self.validate(self.maxResults, name: "maxResults", parent: name, max: 5000)
            try self.validate(self.maxResults, name: "maxResults", parent: name, min: 1)
            try self.validate(self.nextToken, name: "nextToken", parent: name, max: 2000)
            try self.validate(self.nextToken, name: "nextToken", parent: name, min: 1)
        }

        private enum CodingKeys: String, CodingKey {
            case dimensions = "Dimensions"
            case maxResults = "MaxResults"
            case nextToken = "NextToken"
            case timePeriod = "TimePeriod"
        }
    }

    public struct GetEstimatedCarbonEmissionsDimensionValuesResponse: AWSDecodableShape {
        /// The pagination token indicating there are additional pages available. You can use the token in a following request to fetch the next set of results.
        public let nextToken: String?
        /// The list of possible dimensions over which the emissions data is aggregated.
        public let results: [DimensionEntry]?

        @inlinable
        public init(nextToken: String? = nil, results: [DimensionEntry]? = nil) {
            self.nextToken = nextToken
            self.results = results
        }

        private enum CodingKeys: String, CodingKey {
            case nextToken = "NextToken"
            case results = "Results"
        }
    }

    public struct GetEstimatedCarbonEmissionsRequest: AWSEncodableShape {
        /// The emission types to include in the results. If absent, returns TOTAL_LBM_CARBON_EMISSIONS and TOTAL_MBM_CARBON_EMISSIONS emissions types.
        public let emissionsTypes: [EmissionsType]?
        ///  The criteria for filtering estimated carbon emissions. To determine which dimensions are available to be filtered by, you can first call GetEstimatedCarbonEmissionsDimensionValues
        public let filterBy: FilterExpression?
        ///  The time granularity for the results. If absent, uses MONTHLY time granularity. The smallest supported granularity for carbon emissions is MONTHLY.   If requesting partial time periods, data will be returned based on the smallest supported granularity. For example, requesting 2025-04-01T00:00:00Z to 2026-04-01T00:00:00Z with YEARLY_CALENDAR granularity will return the last 9 months for 2025 and the first 3 months of 2026.
        public let granularity: TimeGranularity?
        /// Configuration for fiscal year calculations when using YEARLY_FISCAL or QUARTERLY_FISCAL granularity.
        public let granularityConfiguration: GranularityConfiguration?
        /// The dimensions available for grouping estimated carbon emissions.
        public let groupBy: [Dimension]?
        /// The maximum number of results to return in a single call. Default is 1000.
        public let maxResults: Int?
        /// The pagination token specifying which page of results to return in the response. If no token is provided, the default page is the first page.
        public let nextToken: String?
        ///  The date range for fetching estimated carbon emissions. The range must include the start date of a month for that month's data to be included in the response.
        public let timePeriod: TimePeriod

        @inlinable
        public init(emissionsTypes: [EmissionsType]? = nil, filterBy: FilterExpression? = nil, granularity: TimeGranularity? = nil, granularityConfiguration: GranularityConfiguration? = nil, groupBy: [Dimension]? = nil, maxResults: Int? = nil, nextToken: String? = nil, timePeriod: TimePeriod) {
            self.emissionsTypes = emissionsTypes
            self.filterBy = filterBy
            self.granularity = granularity
            self.granularityConfiguration = granularityConfiguration
            self.groupBy = groupBy
            self.maxResults = maxResults
            self.nextToken = nextToken
            self.timePeriod = timePeriod
        }

        public func validate(name: String) throws {
            try self.granularityConfiguration?.validate(name: "\(name).granularityConfiguration")
            try self.validate(self.maxResults, name: "maxResults", parent: name, max: 5000)
            try self.validate(self.maxResults, name: "maxResults", parent: name, min: 1)
            try self.validate(self.nextToken, name: "nextToken", parent: name, max: 2000)
            try self.validate(self.nextToken, name: "nextToken", parent: name, min: 1)
        }

        private enum CodingKeys: String, CodingKey {
            case emissionsTypes = "EmissionsTypes"
            case filterBy = "FilterBy"
            case granularity = "Granularity"
            case granularityConfiguration = "GranularityConfiguration"
            case groupBy = "GroupBy"
            case maxResults = "MaxResults"
            case nextToken = "NextToken"
            case timePeriod = "TimePeriod"
        }
    }

    public struct GetEstimatedCarbonEmissionsResponse: AWSDecodableShape {
        /// The pagination token indicating there are additional pages available. You can use the token in a following request to fetch the next set of results.
        public let nextToken: String?
        /// The result of the requested inputs.
        public let results: [EstimatedCarbonEmissions]

        @inlinable
        public init(nextToken: String? = nil, results: [EstimatedCarbonEmissions]) {
            self.nextToken = nextToken
            self.results = results
        }

        private enum CodingKeys: String, CodingKey {
            case nextToken = "NextToken"
            case results = "Results"
        }
    }

    public struct GetEstimatedWaterAllocationDimensionValuesRequest: AWSEncodableShape {
        /// The dimensions available for grouping estimated water allocation.
        public let dimensions: [Dimension]
        /// The maximum number of results to return in a single call. Default is 1000.
        public let maxResults: Int?
        /// The pagination token specifying which page of results to return in the response. If no token is provided, the default page is the first page.
        public let nextToken: String?
        ///  The date range for fetching the dimension values. The range must include the start date of a year for that year's data to be included in the response.
        public let timePeriod: TimePeriod

        @inlinable
        public init(dimensions: [Dimension], maxResults: Int? = nil, nextToken: String? = nil, timePeriod: TimePeriod) {
            self.dimensions = dimensions
            self.maxResults = maxResults
            self.nextToken = nextToken
            self.timePeriod = timePeriod
        }

        public func validate(name: String) throws {
            try self.validate(self.maxResults, name: "maxResults", parent: name, max: 5000)
            try self.validate(self.maxResults, name: "maxResults", parent: name, min: 1)
            try self.validate(self.nextToken, name: "nextToken", parent: name, max: 2000)
            try self.validate(self.nextToken, name: "nextToken", parent: name, min: 1)
        }

        private enum CodingKeys: String, CodingKey {
            case dimensions = "Dimensions"
            case maxResults = "MaxResults"
            case nextToken = "NextToken"
            case timePeriod = "TimePeriod"
        }
    }

    public struct GetEstimatedWaterAllocationDimensionValuesResponse: AWSDecodableShape {
        /// The pagination token indicating there are additional pages available. You can use the token in a following request to fetch the next set of results.
        public let nextToken: String?
        /// The list of possible dimensions over which the allocation data is aggregated.
        public let results: [DimensionEntry]

        @inlinable
        public init(nextToken: String? = nil, results: [DimensionEntry]) {
            self.nextToken = nextToken
            self.results = results
        }

        private enum CodingKeys: String, CodingKey {
            case nextToken = "NextToken"
            case results = "Results"
        }
    }

    public struct GetEstimatedWaterAllocationRequest: AWSEncodableShape {
        /// The allocation types to include in the results. If absent, returns TOTAL_WATER_WITHDRAWALS allocation types.
        public let allocationTypes: [WaterAllocationType]?
        ///  The criteria for filtering estimated water allocation. To determine which dimensions are available to be filtered by, you can first call GetEstimatedWaterAllocationDimensionValues
        public let filterBy: FilterExpression?
        /// The time granularity for the results. Only YEARLY_CALENDAR time granularity is currently supported for water allocation. Defaults to YEARLY_CALENDAR if absent.  If requesting partial time periods, data will be returned based on the smallest supported granularity. For example, requesting 2025-04-01T00:00:00Z to 2026-04-01T00:00:00Z with YEARLY_CALENDAR will return all the data for 2026 only.
        public let granularity: TimeGranularity?
        /// The dimensions available for grouping estimated water allocation.
        public let groupBy: [Dimension]?
        /// The maximum number of results to return in a single call. Default is 1000.
        public let maxResults: Int?
        /// The pagination token specifying which page of results to return in the response. If no token is provided, the default page is the first page.
        public let nextToken: String?
        ///  The date range for fetching estimated water allocation. The range must include the start date of a year for that year's data to be included in the response.
        public let timePeriod: TimePeriod

        @inlinable
        public init(allocationTypes: [WaterAllocationType]? = nil, filterBy: FilterExpression? = nil, granularity: TimeGranularity? = nil, groupBy: [Dimension]? = nil, maxResults: Int? = nil, nextToken: String? = nil, timePeriod: TimePeriod) {
            self.allocationTypes = allocationTypes
            self.filterBy = filterBy
            self.granularity = granularity
            self.groupBy = groupBy
            self.maxResults = maxResults
            self.nextToken = nextToken
            self.timePeriod = timePeriod
        }

        public func validate(name: String) throws {
            try self.validate(self.maxResults, name: "maxResults", parent: name, max: 5000)
            try self.validate(self.maxResults, name: "maxResults", parent: name, min: 1)
            try self.validate(self.nextToken, name: "nextToken", parent: name, max: 2000)
            try self.validate(self.nextToken, name: "nextToken", parent: name, min: 1)
        }

        private enum CodingKeys: String, CodingKey {
            case allocationTypes = "AllocationTypes"
            case filterBy = "FilterBy"
            case granularity = "Granularity"
            case groupBy = "GroupBy"
            case maxResults = "MaxResults"
            case nextToken = "NextToken"
            case timePeriod = "TimePeriod"
        }
    }

    public struct GetEstimatedWaterAllocationResponse: AWSDecodableShape {
        /// The pagination token indicating there are additional pages available. You can use the token in a following request to fetch the next set of results.
        public let nextToken: String?
        /// The result of the requested inputs.
        public let results: [EstimatedWaterAllocation]

        @inlinable
        public init(nextToken: String? = nil, results: [EstimatedWaterAllocation]) {
            self.nextToken = nextToken
            self.results = results
        }

        private enum CodingKeys: String, CodingKey {
            case nextToken = "NextToken"
            case results = "Results"
        }
    }

    public struct GranularityConfiguration: AWSEncodableShape {
        ///  The month (1-12) when the fiscal year begins. Used for YEARLY_FISCAL and QUARTERLY_FISCAL granularity. Defaults to 1 (January).
        public let fiscalYearStartMonth: Int?

        @inlinable
        public init(fiscalYearStartMonth: Int? = nil) {
            self.fiscalYearStartMonth = fiscalYearStartMonth
        }

        public func validate(name: String) throws {
            try self.validate(self.fiscalYearStartMonth, name: "fiscalYearStartMonth", parent: name, max: 12)
            try self.validate(self.fiscalYearStartMonth, name: "fiscalYearStartMonth", parent: name, min: 1)
        }

        private enum CodingKeys: String, CodingKey {
            case fiscalYearStartMonth = "FiscalYearStartMonth"
        }
    }

    public struct TimePeriod: AWSEncodableShape & AWSDecodableShape {
        /// The end (exclusive) of the time period. ISO-8601 formatted timestamp, for example: YYYY-MM-DDThh:mm:ss.sssZ
        @CustomCoding<ISO8601DateCoder>
        public var end: Date
        /// The start (inclusive) of the time period. ISO-8601 formatted timestamp, for example: YYYY-MM-DDThh:mm:ss.sssZ
        @CustomCoding<ISO8601DateCoder>
        public var start: Date

        @inlinable
        public init(end: Date, start: Date) {
            self.end = end
            self.start = start
        }

        private enum CodingKeys: String, CodingKey {
            case end = "End"
            case start = "Start"
        }
    }

    public struct WaterAllocation: AWSDecodableShape {
        /// The unit of measurement for the allocation value.
        public let unit: WaterAllocationUnit
        /// The numeric value of the allocation quantity.
        public let value: Double

        @inlinable
        public init(unit: WaterAllocationUnit, value: Double) {
            self.unit = unit
            self.value = value
        }

        private enum CodingKeys: String, CodingKey {
            case unit = "Unit"
            case value = "Value"
        }
    }
}

// MARK: - Errors

/// Error enum for Sustainability
public struct SustainabilityErrorType: AWSErrorType {
    enum Code: String {
        case accessDeniedException = "AccessDeniedException"
        case internalServerException = "InternalServerException"
        case throttlingException = "ThrottlingException"
        case validationException = "ValidationException"
    }

    private let error: Code
    public let context: AWSErrorContext?

    /// initialize Sustainability
    public init?(errorCode: String, context: AWSErrorContext) {
        guard let error = Code(rawValue: errorCode) else { return nil }
        self.error = error
        self.context = context
    }

    internal init(_ error: Code) {
        self.error = error
        self.context = nil
    }

    /// return error code string
    public var errorCode: String { self.error.rawValue }

    /// You do not have sufficient access to perform this action.
    public static var accessDeniedException: Self { .init(.accessDeniedException) }
    /// The request processing has failed because of an unknown error, exception, or failure.
    public static var internalServerException: Self { .init(.internalServerException) }
    /// The request was denied due to request throttling.
    public static var throttlingException: Self { .init(.throttlingException) }
    /// The input fails to satisfy the constraints specified by an Amazon Web Services service.
    public static var validationException: Self { .init(.validationException) }
}

extension SustainabilityErrorType: Equatable {
    public static func == (lhs: SustainabilityErrorType, rhs: SustainabilityErrorType) -> Bool {
        lhs.error == rhs.error
    }
}

extension SustainabilityErrorType: CustomStringConvertible {
    public var description: String {
        return "\(self.error.rawValue): \(self.message ?? "")"
    }
}
