//===----------------------------------------------------------------------===//
//
// This source file is part of the Soto for AWS open source project
//
// Copyright (c) 2017-2024 the Soto project authors
// Licensed under Apache License v2.0
//
// See LICENSE.txt for license information
// See CONTRIBUTORS.txt for the list of Soto project authors
//
// SPDX-License-Identifier: Apache-2.0
//
//===----------------------------------------------------------------------===//

// THIS FILE IS AUTOMATICALLY GENERATED by https://github.com/soto-project/soto-codegenerator.
// DO NOT EDIT.

#if canImport(FoundationEssentials)
import FoundationEssentials
#else
import Foundation
#endif
@_exported import SotoCore

/// Service object for interacting with AWS MediaStore service.
///
/// An AWS Elemental MediaStore container is a namespace that holds folders and objects. You use a container endpoint to create, read, and delete objects.
public struct MediaStore: AWSService {
    // MARK: Member variables

    /// Client used for communication with AWS
    public let client: AWSClient
    /// Service configuration
    public let config: AWSServiceConfig

    // MARK: Initialization

    /// Initialize the MediaStore client
    /// - parameters:
    ///     - client: AWSClient used to process requests
    ///     - region: Region of server you want to communicate with. This will override the partition parameter.
    ///     - partition: AWS partition where service resides, standard (.aws), china (.awscn), government (.awsusgov).
    ///     - endpoint: Custom endpoint URL to use instead of standard AWS servers
    ///     - middleware: Middleware chain used to edit requests before they are sent and responses before they are decoded 
    ///     - timeout: Timeout value for HTTP requests
    ///     - byteBufferAllocator: Allocator for ByteBuffers
    ///     - options: Service options
    public init(
        client: AWSClient,
        region: SotoCore.Region? = nil,
        partition: AWSPartition = .aws,
        endpoint: String? = nil,
        middleware: AWSMiddlewareProtocol? = nil,
        timeout: TimeAmount? = nil,
        byteBufferAllocator: ByteBufferAllocator = ByteBufferAllocator(),
        options: AWSServiceConfig.Options = []
    ) {
        self.client = client
        self.config = AWSServiceConfig(
            region: region,
            partition: region?.partition ?? partition,
            amzTarget: "MediaStore_20170901",
            serviceName: "MediaStore",
            serviceIdentifier: "mediastore",
            serviceProtocol: .json(version: "1.1"),
            apiVersion: "2017-09-01",
            endpoint: endpoint,
            errorType: MediaStoreErrorType.self,
            xmlNamespace: "https://mediastore.amazonaws.com/doc/2017-09-01",
            middleware: middleware,
            timeout: timeout,
            byteBufferAllocator: byteBufferAllocator,
            options: options
        )
    }





    // MARK: API Calls

    /// Creates a storage container to hold objects. A container is similar to a bucket in the Amazon S3 service.
    @Sendable
    @inlinable
    public func createContainer(_ input: CreateContainerInput, logger: Logger = AWSClient.loggingDisabled) async throws -> CreateContainerOutput {
        try await self.client.execute(
            operation: "CreateContainer", 
            path: "/", 
            httpMethod: .POST, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    /// Creates a storage container to hold objects. A container is similar to a bucket in the Amazon S3 service.
    ///
    /// Parameters:
    ///   - containerName: The name for the container. The name must be from 1 to 255 characters. Container names must be unique to your AWS account within a specific region. As an example, you could create a container named movies in every region, as long as you don’t have an existing container with that name.
    ///   - tags: An array of key:value pairs that you define. These values can be anything that you want. Typically, the tag key represents a category (such as "environment") and the tag value represents a specific value within that category (such as "test," "development," or "production"). You can add up to 50  tags to each container. For more information about tagging, including naming and usage conventions, see Tagging Resources in MediaStore.
    ///   - logger: Logger use during operation
    @inlinable
    public func createContainer(
        containerName: String,
        tags: [Tag]? = nil,
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws -> CreateContainerOutput {
        let input = CreateContainerInput(
            containerName: containerName, 
            tags: tags
        )
        return try await self.createContainer(input, logger: logger)
    }

    /// Deletes the specified container. Before you make a DeleteContainer request, delete any objects in the container or in any folders in the container. You can delete only empty containers.
    @Sendable
    @inlinable
    public func deleteContainer(_ input: DeleteContainerInput, logger: Logger = AWSClient.loggingDisabled) async throws -> DeleteContainerOutput {
        try await self.client.execute(
            operation: "DeleteContainer", 
            path: "/", 
            httpMethod: .POST, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    /// Deletes the specified container. Before you make a DeleteContainer request, delete any objects in the container or in any folders in the container. You can delete only empty containers.
    ///
    /// Parameters:
    ///   - containerName: The name of the container to delete.
    ///   - logger: Logger use during operation
    @inlinable
    public func deleteContainer(
        containerName: String,
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws -> DeleteContainerOutput {
        let input = DeleteContainerInput(
            containerName: containerName
        )
        return try await self.deleteContainer(input, logger: logger)
    }

    /// Deletes the access policy that is associated with the specified container.
    @Sendable
    @inlinable
    public func deleteContainerPolicy(_ input: DeleteContainerPolicyInput, logger: Logger = AWSClient.loggingDisabled) async throws -> DeleteContainerPolicyOutput {
        try await self.client.execute(
            operation: "DeleteContainerPolicy", 
            path: "/", 
            httpMethod: .POST, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    /// Deletes the access policy that is associated with the specified container.
    ///
    /// Parameters:
    ///   - containerName: The name of the container that holds the policy.
    ///   - logger: Logger use during operation
    @inlinable
    public func deleteContainerPolicy(
        containerName: String,
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws -> DeleteContainerPolicyOutput {
        let input = DeleteContainerPolicyInput(
            containerName: containerName
        )
        return try await self.deleteContainerPolicy(input, logger: logger)
    }

    /// Deletes the cross-origin resource sharing (CORS) configuration information that is set for the container. To use this operation, you must have permission to perform the MediaStore:DeleteCorsPolicy action. The container owner has this permission by default and can grant this permission to others.
    @Sendable
    @inlinable
    public func deleteCorsPolicy(_ input: DeleteCorsPolicyInput, logger: Logger = AWSClient.loggingDisabled) async throws -> DeleteCorsPolicyOutput {
        try await self.client.execute(
            operation: "DeleteCorsPolicy", 
            path: "/", 
            httpMethod: .POST, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    /// Deletes the cross-origin resource sharing (CORS) configuration information that is set for the container. To use this operation, you must have permission to perform the MediaStore:DeleteCorsPolicy action. The container owner has this permission by default and can grant this permission to others.
    ///
    /// Parameters:
    ///   - containerName: The name of the container to remove the policy from.
    ///   - logger: Logger use during operation
    @inlinable
    public func deleteCorsPolicy(
        containerName: String,
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws -> DeleteCorsPolicyOutput {
        let input = DeleteCorsPolicyInput(
            containerName: containerName
        )
        return try await self.deleteCorsPolicy(input, logger: logger)
    }

    /// Removes an object lifecycle policy from a container. It takes up to 20 minutes for the change to take effect.
    @Sendable
    @inlinable
    public func deleteLifecyclePolicy(_ input: DeleteLifecyclePolicyInput, logger: Logger = AWSClient.loggingDisabled) async throws -> DeleteLifecyclePolicyOutput {
        try await self.client.execute(
            operation: "DeleteLifecyclePolicy", 
            path: "/", 
            httpMethod: .POST, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    /// Removes an object lifecycle policy from a container. It takes up to 20 minutes for the change to take effect.
    ///
    /// Parameters:
    ///   - containerName: The name of the container that holds the object lifecycle policy.
    ///   - logger: Logger use during operation
    @inlinable
    public func deleteLifecyclePolicy(
        containerName: String,
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws -> DeleteLifecyclePolicyOutput {
        let input = DeleteLifecyclePolicyInput(
            containerName: containerName
        )
        return try await self.deleteLifecyclePolicy(input, logger: logger)
    }

    /// Deletes the metric policy that is associated with the specified container. If there is no metric policy associated with the container, MediaStore doesn't send metrics to CloudWatch.
    @Sendable
    @inlinable
    public func deleteMetricPolicy(_ input: DeleteMetricPolicyInput, logger: Logger = AWSClient.loggingDisabled) async throws -> DeleteMetricPolicyOutput {
        try await self.client.execute(
            operation: "DeleteMetricPolicy", 
            path: "/", 
            httpMethod: .POST, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    /// Deletes the metric policy that is associated with the specified container. If there is no metric policy associated with the container, MediaStore doesn't send metrics to CloudWatch.
    ///
    /// Parameters:
    ///   - containerName: The name of the container that is associated with the metric policy that you want to delete.
    ///   - logger: Logger use during operation
    @inlinable
    public func deleteMetricPolicy(
        containerName: String,
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws -> DeleteMetricPolicyOutput {
        let input = DeleteMetricPolicyInput(
            containerName: containerName
        )
        return try await self.deleteMetricPolicy(input, logger: logger)
    }

    /// Retrieves the properties of the requested container. This request is commonly used to retrieve the endpoint of a container. An endpoint is a value assigned by the service when a new container is created. A container's endpoint does not change after it has been assigned. The DescribeContainer request returns a single Container object based on ContainerName. To return all Container objects that are associated with a specified AWS account, use ListContainers.
    @Sendable
    @inlinable
    public func describeContainer(_ input: DescribeContainerInput, logger: Logger = AWSClient.loggingDisabled) async throws -> DescribeContainerOutput {
        try await self.client.execute(
            operation: "DescribeContainer", 
            path: "/", 
            httpMethod: .POST, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    /// Retrieves the properties of the requested container. This request is commonly used to retrieve the endpoint of a container. An endpoint is a value assigned by the service when a new container is created. A container's endpoint does not change after it has been assigned. The DescribeContainer request returns a single Container object based on ContainerName. To return all Container objects that are associated with a specified AWS account, use ListContainers.
    ///
    /// Parameters:
    ///   - containerName: The name of the container to query.
    ///   - logger: Logger use during operation
    @inlinable
    public func describeContainer(
        containerName: String? = nil,
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws -> DescribeContainerOutput {
        let input = DescribeContainerInput(
            containerName: containerName
        )
        return try await self.describeContainer(input, logger: logger)
    }

    /// Retrieves the access policy for the specified container. For information about the data that is included in an access policy, see the AWS Identity and Access Management User Guide.
    @Sendable
    @inlinable
    public func getContainerPolicy(_ input: GetContainerPolicyInput, logger: Logger = AWSClient.loggingDisabled) async throws -> GetContainerPolicyOutput {
        try await self.client.execute(
            operation: "GetContainerPolicy", 
            path: "/", 
            httpMethod: .POST, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    /// Retrieves the access policy for the specified container. For information about the data that is included in an access policy, see the AWS Identity and Access Management User Guide.
    ///
    /// Parameters:
    ///   - containerName: The name of the container.
    ///   - logger: Logger use during operation
    @inlinable
    public func getContainerPolicy(
        containerName: String,
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws -> GetContainerPolicyOutput {
        let input = GetContainerPolicyInput(
            containerName: containerName
        )
        return try await self.getContainerPolicy(input, logger: logger)
    }

    /// Returns the cross-origin resource sharing (CORS) configuration information that is set for the container. To use this operation, you must have permission to perform the MediaStore:GetCorsPolicy action. By default, the container owner has this permission and can grant it to others.
    @Sendable
    @inlinable
    public func getCorsPolicy(_ input: GetCorsPolicyInput, logger: Logger = AWSClient.loggingDisabled) async throws -> GetCorsPolicyOutput {
        try await self.client.execute(
            operation: "GetCorsPolicy", 
            path: "/", 
            httpMethod: .POST, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    /// Returns the cross-origin resource sharing (CORS) configuration information that is set for the container. To use this operation, you must have permission to perform the MediaStore:GetCorsPolicy action. By default, the container owner has this permission and can grant it to others.
    ///
    /// Parameters:
    ///   - containerName: The name of the container that the policy is assigned to.
    ///   - logger: Logger use during operation
    @inlinable
    public func getCorsPolicy(
        containerName: String,
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws -> GetCorsPolicyOutput {
        let input = GetCorsPolicyInput(
            containerName: containerName
        )
        return try await self.getCorsPolicy(input, logger: logger)
    }

    /// Retrieves the object lifecycle policy that is assigned to a container.
    @Sendable
    @inlinable
    public func getLifecyclePolicy(_ input: GetLifecyclePolicyInput, logger: Logger = AWSClient.loggingDisabled) async throws -> GetLifecyclePolicyOutput {
        try await self.client.execute(
            operation: "GetLifecyclePolicy", 
            path: "/", 
            httpMethod: .POST, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    /// Retrieves the object lifecycle policy that is assigned to a container.
    ///
    /// Parameters:
    ///   - containerName: The name of the container that the object lifecycle policy is assigned to.
    ///   - logger: Logger use during operation
    @inlinable
    public func getLifecyclePolicy(
        containerName: String,
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws -> GetLifecyclePolicyOutput {
        let input = GetLifecyclePolicyInput(
            containerName: containerName
        )
        return try await self.getLifecyclePolicy(input, logger: logger)
    }

    /// Returns the metric policy for the specified container.
    @Sendable
    @inlinable
    public func getMetricPolicy(_ input: GetMetricPolicyInput, logger: Logger = AWSClient.loggingDisabled) async throws -> GetMetricPolicyOutput {
        try await self.client.execute(
            operation: "GetMetricPolicy", 
            path: "/", 
            httpMethod: .POST, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    /// Returns the metric policy for the specified container.
    ///
    /// Parameters:
    ///   - containerName: The name of the container that is associated with the metric policy.
    ///   - logger: Logger use during operation
    @inlinable
    public func getMetricPolicy(
        containerName: String,
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws -> GetMetricPolicyOutput {
        let input = GetMetricPolicyInput(
            containerName: containerName
        )
        return try await self.getMetricPolicy(input, logger: logger)
    }

    /// Lists the properties of all containers in AWS Elemental MediaStore.  You can query to receive all the containers in one response. Or you can include the MaxResults parameter to receive a limited number of containers in each response. In this case, the response includes a token. To get the next set of containers, send the command again, this time with the NextToken parameter (with the returned token as its value). The next set of responses appears, with a token if there are still more containers to receive.  See also DescribeContainer, which gets the properties of one container.
    @Sendable
    @inlinable
    public func listContainers(_ input: ListContainersInput, logger: Logger = AWSClient.loggingDisabled) async throws -> ListContainersOutput {
        try await self.client.execute(
            operation: "ListContainers", 
            path: "/", 
            httpMethod: .POST, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    /// Lists the properties of all containers in AWS Elemental MediaStore.  You can query to receive all the containers in one response. Or you can include the MaxResults parameter to receive a limited number of containers in each response. In this case, the response includes a token. To get the next set of containers, send the command again, this time with the NextToken parameter (with the returned token as its value). The next set of responses appears, with a token if there are still more containers to receive.  See also DescribeContainer, which gets the properties of one container.
    ///
    /// Parameters:
    ///   - maxResults: Enter the maximum number of containers in the response. Use from 1 to 255 characters.
    ///   - nextToken: Only if you used MaxResults in the first command, enter the token (which was included in the previous response) to obtain the next set of containers. This token is included in a response only if there actually are more containers to list.
    ///   - logger: Logger use during operation
    @inlinable
    public func listContainers(
        maxResults: Int? = nil,
        nextToken: String? = nil,
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws -> ListContainersOutput {
        let input = ListContainersInput(
            maxResults: maxResults, 
            nextToken: nextToken
        )
        return try await self.listContainers(input, logger: logger)
    }

    /// Returns a list of the tags assigned to the specified container.
    @Sendable
    @inlinable
    public func listTagsForResource(_ input: ListTagsForResourceInput, logger: Logger = AWSClient.loggingDisabled) async throws -> ListTagsForResourceOutput {
        try await self.client.execute(
            operation: "ListTagsForResource", 
            path: "/", 
            httpMethod: .POST, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    /// Returns a list of the tags assigned to the specified container.
    ///
    /// Parameters:
    ///   - resource: The Amazon Resource Name (ARN) for the container.
    ///   - logger: Logger use during operation
    @inlinable
    public func listTagsForResource(
        resource: String,
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws -> ListTagsForResourceOutput {
        let input = ListTagsForResourceInput(
            resource: resource
        )
        return try await self.listTagsForResource(input, logger: logger)
    }

    /// Creates an access policy for the specified container to restrict the users and clients that can access it. For information about the data that is included in an access policy, see the AWS Identity and Access Management User Guide. For this release of the REST API, you can create only one policy for a container. If you enter PutContainerPolicy twice, the second command modifies the existing policy.
    @Sendable
    @inlinable
    public func putContainerPolicy(_ input: PutContainerPolicyInput, logger: Logger = AWSClient.loggingDisabled) async throws -> PutContainerPolicyOutput {
        try await self.client.execute(
            operation: "PutContainerPolicy", 
            path: "/", 
            httpMethod: .POST, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    /// Creates an access policy for the specified container to restrict the users and clients that can access it. For information about the data that is included in an access policy, see the AWS Identity and Access Management User Guide. For this release of the REST API, you can create only one policy for a container. If you enter PutContainerPolicy twice, the second command modifies the existing policy.
    ///
    /// Parameters:
    ///   - containerName: The name of the container.
    ///   - policy: The contents of the policy, which includes the following:    One Version tag   One Statement tag that contains the standard tags for the policy.
    ///   - logger: Logger use during operation
    @inlinable
    public func putContainerPolicy(
        containerName: String,
        policy: String,
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws -> PutContainerPolicyOutput {
        let input = PutContainerPolicyInput(
            containerName: containerName, 
            policy: policy
        )
        return try await self.putContainerPolicy(input, logger: logger)
    }

    /// Sets the cross-origin resource sharing (CORS) configuration on a container so that the container can service cross-origin requests. For example, you might want to enable a request whose origin is http://www.example.com to access your AWS Elemental MediaStore container at my.example.container.com by using the browser's XMLHttpRequest capability. To enable CORS on a container, you attach a CORS policy to the container. In the CORS policy, you configure rules that identify origins and the HTTP methods that can be executed on your container. The policy can contain up to 398,000 characters. You can add up to 100 rules to a CORS policy. If more than one rule applies, the service uses the first applicable rule listed. To learn more about CORS, see Cross-Origin Resource Sharing (CORS) in AWS Elemental MediaStore.
    @Sendable
    @inlinable
    public func putCorsPolicy(_ input: PutCorsPolicyInput, logger: Logger = AWSClient.loggingDisabled) async throws -> PutCorsPolicyOutput {
        try await self.client.execute(
            operation: "PutCorsPolicy", 
            path: "/", 
            httpMethod: .POST, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    /// Sets the cross-origin resource sharing (CORS) configuration on a container so that the container can service cross-origin requests. For example, you might want to enable a request whose origin is http://www.example.com to access your AWS Elemental MediaStore container at my.example.container.com by using the browser's XMLHttpRequest capability. To enable CORS on a container, you attach a CORS policy to the container. In the CORS policy, you configure rules that identify origins and the HTTP methods that can be executed on your container. The policy can contain up to 398,000 characters. You can add up to 100 rules to a CORS policy. If more than one rule applies, the service uses the first applicable rule listed. To learn more about CORS, see Cross-Origin Resource Sharing (CORS) in AWS Elemental MediaStore.
    ///
    /// Parameters:
    ///   - containerName: The name of the container that you want to assign the CORS policy to.
    ///   - corsPolicy: The CORS policy to apply to the container.
    ///   - logger: Logger use during operation
    @inlinable
    public func putCorsPolicy(
        containerName: String,
        corsPolicy: [CorsRule],
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws -> PutCorsPolicyOutput {
        let input = PutCorsPolicyInput(
            containerName: containerName, 
            corsPolicy: corsPolicy
        )
        return try await self.putCorsPolicy(input, logger: logger)
    }

    /// Writes an object lifecycle policy to a container. If the container already has an object lifecycle policy, the service replaces the existing policy with the new policy. It takes up to 20 minutes for the change to take effect. For information about how to construct an object lifecycle policy, see Components of an Object Lifecycle Policy.
    @Sendable
    @inlinable
    public func putLifecyclePolicy(_ input: PutLifecyclePolicyInput, logger: Logger = AWSClient.loggingDisabled) async throws -> PutLifecyclePolicyOutput {
        try await self.client.execute(
            operation: "PutLifecyclePolicy", 
            path: "/", 
            httpMethod: .POST, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    /// Writes an object lifecycle policy to a container. If the container already has an object lifecycle policy, the service replaces the existing policy with the new policy. It takes up to 20 minutes for the change to take effect. For information about how to construct an object lifecycle policy, see Components of an Object Lifecycle Policy.
    ///
    /// Parameters:
    ///   - containerName: The name of the container that you want to assign the object lifecycle policy to.
    ///   - lifecyclePolicy: The object lifecycle policy to apply to the container.
    ///   - logger: Logger use during operation
    @inlinable
    public func putLifecyclePolicy(
        containerName: String,
        lifecyclePolicy: String,
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws -> PutLifecyclePolicyOutput {
        let input = PutLifecyclePolicyInput(
            containerName: containerName, 
            lifecyclePolicy: lifecyclePolicy
        )
        return try await self.putLifecyclePolicy(input, logger: logger)
    }

    /// The metric policy that you want to add to the container. A metric policy allows AWS Elemental MediaStore to send metrics to Amazon CloudWatch. It takes up to 20 minutes for the new policy to take effect.
    @Sendable
    @inlinable
    public func putMetricPolicy(_ input: PutMetricPolicyInput, logger: Logger = AWSClient.loggingDisabled) async throws -> PutMetricPolicyOutput {
        try await self.client.execute(
            operation: "PutMetricPolicy", 
            path: "/", 
            httpMethod: .POST, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    /// The metric policy that you want to add to the container. A metric policy allows AWS Elemental MediaStore to send metrics to Amazon CloudWatch. It takes up to 20 minutes for the new policy to take effect.
    ///
    /// Parameters:
    ///   - containerName: The name of the container that you want to add the metric policy to.
    ///   - metricPolicy: The metric policy that you want to associate with the container. In the policy, you must indicate whether you want MediaStore to send container-level metrics. You can also include up to five rules to define groups of objects that you want MediaStore to send object-level metrics for.  If you include rules in the policy, construct each rule with both of the following:   An object group that defines which objects to include in the group. The definition can be a path or a file name, but it can't have more than 900 characters. Valid characters are: a-z, A-Z, 0-9, _ (underscore), = (equal), : (colon), . (period), - (hyphen), ~ (tilde), / (forward slash), and * (asterisk). Wildcards (*) are acceptable.   An object group name that allows you to refer to the object group. The name can't have more than 30 characters. Valid characters are: a-z, A-Z, 0-9, and _ (underscore).
    ///   - logger: Logger use during operation
    @inlinable
    public func putMetricPolicy(
        containerName: String,
        metricPolicy: MetricPolicy,
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws -> PutMetricPolicyOutput {
        let input = PutMetricPolicyInput(
            containerName: containerName, 
            metricPolicy: metricPolicy
        )
        return try await self.putMetricPolicy(input, logger: logger)
    }

    /// Starts access logging on the specified container. When you enable access logging on a container, MediaStore delivers access logs for objects stored in that container to Amazon CloudWatch Logs.
    @Sendable
    @inlinable
    public func startAccessLogging(_ input: StartAccessLoggingInput, logger: Logger = AWSClient.loggingDisabled) async throws -> StartAccessLoggingOutput {
        try await self.client.execute(
            operation: "StartAccessLogging", 
            path: "/", 
            httpMethod: .POST, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    /// Starts access logging on the specified container. When you enable access logging on a container, MediaStore delivers access logs for objects stored in that container to Amazon CloudWatch Logs.
    ///
    /// Parameters:
    ///   - containerName: The name of the container that you want to start access logging on.
    ///   - logger: Logger use during operation
    @inlinable
    public func startAccessLogging(
        containerName: String,
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws -> StartAccessLoggingOutput {
        let input = StartAccessLoggingInput(
            containerName: containerName
        )
        return try await self.startAccessLogging(input, logger: logger)
    }

    /// Stops access logging on the specified container. When you stop access logging on a container, MediaStore stops sending access logs to Amazon CloudWatch Logs. These access logs are not saved and are not retrievable.
    @Sendable
    @inlinable
    public func stopAccessLogging(_ input: StopAccessLoggingInput, logger: Logger = AWSClient.loggingDisabled) async throws -> StopAccessLoggingOutput {
        try await self.client.execute(
            operation: "StopAccessLogging", 
            path: "/", 
            httpMethod: .POST, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    /// Stops access logging on the specified container. When you stop access logging on a container, MediaStore stops sending access logs to Amazon CloudWatch Logs. These access logs are not saved and are not retrievable.
    ///
    /// Parameters:
    ///   - containerName: The name of the container that you want to stop access logging on.
    ///   - logger: Logger use during operation
    @inlinable
    public func stopAccessLogging(
        containerName: String,
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws -> StopAccessLoggingOutput {
        let input = StopAccessLoggingInput(
            containerName: containerName
        )
        return try await self.stopAccessLogging(input, logger: logger)
    }

    /// Adds tags to the specified AWS Elemental MediaStore container. Tags are key:value pairs that you can associate with AWS resources. For example, the tag key might be "customer" and the tag value might be "companyA." You can specify one or more tags to add to each container. You can add up to 50 tags to each container. For more information about tagging, including naming and usage conventions, see Tagging Resources in MediaStore.
    @Sendable
    @inlinable
    public func tagResource(_ input: TagResourceInput, logger: Logger = AWSClient.loggingDisabled) async throws -> TagResourceOutput {
        try await self.client.execute(
            operation: "TagResource", 
            path: "/", 
            httpMethod: .POST, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    /// Adds tags to the specified AWS Elemental MediaStore container. Tags are key:value pairs that you can associate with AWS resources. For example, the tag key might be "customer" and the tag value might be "companyA." You can specify one or more tags to add to each container. You can add up to 50 tags to each container. For more information about tagging, including naming and usage conventions, see Tagging Resources in MediaStore.
    ///
    /// Parameters:
    ///   - resource: The Amazon Resource Name (ARN) for the container.
    ///   - tags: An array of key:value pairs that you want to add to the container. You need to specify only the tags that you want to add or update. For example, suppose a container already has two tags (customer:CompanyA and priority:High). You want to change the priority tag and also add a third tag (type:Contract). For TagResource, you specify the following tags: priority:Medium, type:Contract. The result is that your container has three tags: customer:CompanyA, priority:Medium, and type:Contract.
    ///   - logger: Logger use during operation
    @inlinable
    public func tagResource(
        resource: String,
        tags: [Tag],
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws -> TagResourceOutput {
        let input = TagResourceInput(
            resource: resource, 
            tags: tags
        )
        return try await self.tagResource(input, logger: logger)
    }

    /// Removes tags from the specified container. You can specify one or more tags to remove.
    @Sendable
    @inlinable
    public func untagResource(_ input: UntagResourceInput, logger: Logger = AWSClient.loggingDisabled) async throws -> UntagResourceOutput {
        try await self.client.execute(
            operation: "UntagResource", 
            path: "/", 
            httpMethod: .POST, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    /// Removes tags from the specified container. You can specify one or more tags to remove.
    ///
    /// Parameters:
    ///   - resource: The Amazon Resource Name (ARN) for the container.
    ///   - tagKeys: A comma-separated list of keys for tags that you want to remove from the container. For example, if your container has two tags (customer:CompanyA and priority:High) and you want to remove one of the tags (priority:High), you specify the key for the tag that you want to remove (priority).
    ///   - logger: Logger use during operation
    @inlinable
    public func untagResource(
        resource: String,
        tagKeys: [String],
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws -> UntagResourceOutput {
        let input = UntagResourceInput(
            resource: resource, 
            tagKeys: tagKeys
        )
        return try await self.untagResource(input, logger: logger)
    }
}

extension MediaStore {
    /// Initializer required by `AWSService.with(middlewares:timeout:byteBufferAllocator:options)`. You are not able to use this initializer directly as there are not public
    /// initializers for `AWSServiceConfig.Patch`. Please use `AWSService.with(middlewares:timeout:byteBufferAllocator:options)` instead.
    public init(from: MediaStore, patch: AWSServiceConfig.Patch) {
        self.client = from.client
        self.config = from.config.with(patch: patch)
    }
}

// MARK: Paginators

@available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
extension MediaStore {
    /// Return PaginatorSequence for operation ``listContainers(_:logger:)``.
    ///
    /// - Parameters:
    ///   - input: Input for operation
    ///   - logger: Logger used for logging
    @inlinable
    public func listContainersPaginator(
        _ input: ListContainersInput,
        logger: Logger = AWSClient.loggingDisabled
    ) -> AWSClient.PaginatorSequence<ListContainersInput, ListContainersOutput> {
        return .init(
            input: input,
            command: self.listContainers,
            inputKey: \ListContainersInput.nextToken,
            outputKey: \ListContainersOutput.nextToken,
            logger: logger
        )
    }
    /// Return PaginatorSequence for operation ``listContainers(_:logger:)``.
    ///
    /// - Parameters:
    ///   - maxResults: Enter the maximum number of containers in the response. Use from 1 to 255 characters.
    ///   - logger: Logger used for logging
    @inlinable
    public func listContainersPaginator(
        maxResults: Int? = nil,
        logger: Logger = AWSClient.loggingDisabled        
    ) -> AWSClient.PaginatorSequence<ListContainersInput, ListContainersOutput> {
        let input = ListContainersInput(
            maxResults: maxResults
        )
        return self.listContainersPaginator(input, logger: logger)
    }
}

extension MediaStore.ListContainersInput: AWSPaginateToken {
    @inlinable
    public func usingPaginationToken(_ token: String) -> MediaStore.ListContainersInput {
        return .init(
            maxResults: self.maxResults,
            nextToken: token
        )
    }
}
