//===----------------------------------------------------------------------===//
//
// This source file is part of the Soto for AWS open source project
//
// Copyright (c) 2017-2024 the Soto project authors
// Licensed under Apache License v2.0
//
// See LICENSE.txt for license information
// See CONTRIBUTORS.txt for the list of Soto project authors
//
// SPDX-License-Identifier: Apache-2.0
//
//===----------------------------------------------------------------------===//

// THIS FILE IS AUTOMATICALLY GENERATED by https://github.com/soto-project/soto-codegenerator.
// DO NOT EDIT.

#if canImport(FoundationEssentials)
import FoundationEssentials
#else
import Foundation
#endif
@_exported import SotoCore

/// Service object for interacting with AWS AccountAccess service.
///
/// Account access manager enables you to manage applications and entitlements that grant IAM Identity Center principals access to IAM roles across accounts.
public struct AccountAccess: AWSService {
    // MARK: Member variables

    /// Client used for communication with AWS
    public let client: AWSClient
    /// Service configuration
    public let config: AWSServiceConfig

    // MARK: Initialization

    /// Initialize the AccountAccess client
    /// - parameters:
    ///     - client: AWSClient used to process requests
    ///     - region: Region of server you want to communicate with. This will override the partition parameter.
    ///     - partition: AWS partition where service resides, standard (.aws), china (.awscn), government (.awsusgov).
    ///     - endpoint: Custom endpoint URL to use instead of standard AWS servers
    ///     - middleware: Middleware chain used to edit requests before they are sent and responses before they are decoded 
    ///     - timeout: Timeout value for HTTP requests
    ///     - byteBufferAllocator: Allocator for ByteBuffers
    ///     - options: Service options
    public init(
        client: AWSClient,
        region: SotoCore.Region? = nil,
        partition: AWSPartition = .aws,
        endpoint: String? = nil,
        middleware: AWSMiddlewareProtocol? = nil,
        timeout: TimeAmount? = nil,
        byteBufferAllocator: ByteBufferAllocator = ByteBufferAllocator(),
        options: AWSServiceConfig.Options = []
    ) {
        self.client = client
        self.config = AWSServiceConfig(
            region: region,
            partition: region?.partition ?? partition,
            serviceName: "AccountAccess",
            serviceIdentifier: "account-access",
            serviceProtocol: .restjson,
            apiVersion: "2018-05-10",
            endpoint: endpoint,
            errorType: AccountAccessErrorType.self,
            middleware: middleware,
            timeout: timeout,
            byteBufferAllocator: byteBufferAllocator,
            options: options
        )
    }





    // MARK: API Calls

    /// Creates an account access manager instance and its Amazon Web Services account access application in the associated IAM Identity Center instance. This operation is idempotent; calling it multiple times with the same parameters returns the existing application.
    @Sendable
    @inlinable
    public func createApplication(_ input: CreateApplicationRequest, logger: Logger = AWSClient.loggingDisabled) async throws -> CreateApplicationResponse {
        try await self.client.execute(
            operation: "CreateApplication", 
            path: "/applications", 
            httpMethod: .POST, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    /// Creates an account access manager instance and its Amazon Web Services account access application in the associated IAM Identity Center instance. This operation is idempotent; calling it multiple times with the same parameters returns the existing application.
    ///
    /// Parameters:
    ///   - identitySource: Specifies the identity source for the application. The identity source defines the IAM Identity Center instance that provides principals for entitlements.
    ///   - tags: Specifies the tags to assign to the application.
    ///   - logger: Logger use during operation
    @inlinable
    public func createApplication(
        identitySource: IdentitySource,
        tags: [String: String]? = nil,
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws -> CreateApplicationResponse {
        let input = CreateApplicationRequest(
            identitySource: identitySource, 
            tags: tags
        )
        return try await self.createApplication(input, logger: logger)
    }

    /// Creates an entitlement (assignment) in account access manager. An entitlement (assignment) grants a principal (IAM Identity Center user or group) permission to assume a specified IAM role in an Amazon Web Services account. This operation is idempotent.
    @Sendable
    @inlinable
    public func createEntitlement(_ input: CreateEntitlementRequest, logger: Logger = AWSClient.loggingDisabled) async throws -> CreateEntitlementResponse {
        try await self.client.execute(
            operation: "CreateEntitlement", 
            path: "/entitlements", 
            httpMethod: .POST, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    /// Creates an entitlement (assignment) in account access manager. An entitlement (assignment) grants a principal (IAM Identity Center user or group) permission to assume a specified IAM role in an Amazon Web Services account. This operation is idempotent.
    ///
    /// Parameters:
    ///   - applicationArn: Specifies the ARN of the application to create the entitlement for.
    ///   - entitlement: Specifies the entitlement configuration, including the principal and the IAM role to grant access to.
    ///   - logger: Logger use during operation
    @inlinable
    public func createEntitlement(
        applicationArn: String,
        entitlement: Entitlement,
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws -> CreateEntitlementResponse {
        let input = CreateEntitlementRequest(
            applicationArn: applicationArn, 
            entitlement: entitlement
        )
        return try await self.createEntitlement(input, logger: logger)
    }

    /// Deletes an account access manager application. This operation is idempotent; deleting an application that has already been deleted does not return an error.
    @Sendable
    @inlinable
    public func deleteApplication(_ input: DeleteApplicationRequest, logger: Logger = AWSClient.loggingDisabled) async throws -> DeleteApplicationResponse {
        try await self.client.execute(
            operation: "DeleteApplication", 
            path: "/applications/{applicationArn}", 
            httpMethod: .DELETE, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    /// Deletes an account access manager application. This operation is idempotent; deleting an application that has already been deleted does not return an error.
    ///
    /// Parameters:
    ///   - applicationArn: Specifies the ARN of the application to delete.
    ///   - logger: Logger use during operation
    @inlinable
    public func deleteApplication(
        applicationArn: String,
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws -> DeleteApplicationResponse {
        let input = DeleteApplicationRequest(
            applicationArn: applicationArn
        )
        return try await self.deleteApplication(input, logger: logger)
    }

    /// Deletes an entitlement from an account access manager application. This operation is idempotent; deleting an entitlement that has already been deleted does not return an error.
    @Sendable
    @inlinable
    public func deleteEntitlement(_ input: DeleteEntitlementRequest, logger: Logger = AWSClient.loggingDisabled) async throws -> DeleteEntitlementResponse {
        try await self.client.execute(
            operation: "DeleteEntitlement", 
            path: "/entitlements/{entitlementId}", 
            httpMethod: .DELETE, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    /// Deletes an entitlement from an account access manager application. This operation is idempotent; deleting an entitlement that has already been deleted does not return an error.
    ///
    /// Parameters:
    ///   - applicationArn: Specifies the ARN of the application that the entitlement belongs to.
    ///   - entitlementId: Specifies the unique identifier of the entitlement to delete.
    ///   - logger: Logger use during operation
    @inlinable
    public func deleteEntitlement(
        applicationArn: String,
        entitlementId: String,
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws -> DeleteEntitlementResponse {
        let input = DeleteEntitlementRequest(
            applicationArn: applicationArn, 
            entitlementId: entitlementId
        )
        return try await self.deleteEntitlement(input, logger: logger)
    }

    /// Retrieves details about an account access manager application, including its status, identity source, and tags.
    @Sendable
    @inlinable
    public func getApplication(_ input: GetApplicationRequest, logger: Logger = AWSClient.loggingDisabled) async throws -> GetApplicationResponse {
        try await self.client.execute(
            operation: "GetApplication", 
            path: "/applications/{applicationArn}", 
            httpMethod: .GET, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    /// Retrieves details about an account access manager application, including its status, identity source, and tags.
    ///
    /// Parameters:
    ///   - applicationArn: Specifies the ARN of the application to retrieve.
    ///   - logger: Logger use during operation
    @inlinable
    public func getApplication(
        applicationArn: String,
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws -> GetApplicationResponse {
        let input = GetApplicationRequest(
            applicationArn: applicationArn
        )
        return try await self.getApplication(input, logger: logger)
    }

    /// Retrieves details about a specific entitlement for an account access manager application, including the principal, IAM role, and target account.
    @Sendable
    @inlinable
    public func getEntitlement(_ input: GetEntitlementRequest, logger: Logger = AWSClient.loggingDisabled) async throws -> GetEntitlementResponse {
        try await self.client.execute(
            operation: "GetEntitlement", 
            path: "/entitlements/{entitlementId}", 
            httpMethod: .GET, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    /// Retrieves details about a specific entitlement for an account access manager application, including the principal, IAM role, and target account.
    ///
    /// Parameters:
    ///   - applicationArn: Specifies the ARN of the application that the entitlement belongs to.
    ///   - entitlementId: Specifies the unique identifier of the entitlement to retrieve.
    ///   - logger: Logger use during operation
    @inlinable
    public func getEntitlement(
        applicationArn: String,
        entitlementId: String,
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws -> GetEntitlementResponse {
        let input = GetEntitlementRequest(
            applicationArn: applicationArn, 
            entitlementId: entitlementId
        )
        return try await self.getEntitlement(input, logger: logger)
    }

    /// Lists the account access manager applications in your account. Use pagination to ensure that the operation returns quickly and successfully.
    @Sendable
    @inlinable
    public func listApplications(_ input: ListApplicationsRequest, logger: Logger = AWSClient.loggingDisabled) async throws -> ListApplicationsResponse {
        try await self.client.execute(
            operation: "ListApplications", 
            path: "/applications-list", 
            httpMethod: .POST, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    /// Lists the account access manager applications in your account. Use pagination to ensure that the operation returns quickly and successfully.
    ///
    /// Parameters:
    ///   - maxResults: Specifies the maximum number of results to return in a single call.
    ///   - nextToken: Specifies the pagination token from a previous call to retrieve the next set of results.
    ///   - logger: Logger use during operation
    @inlinable
    public func listApplications(
        maxResults: Int? = nil,
        nextToken: String? = nil,
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws -> ListApplicationsResponse {
        let input = ListApplicationsRequest(
            maxResults: maxResults, 
            nextToken: nextToken
        )
        return try await self.listApplications(input, logger: logger)
    }

    /// Lists the entitlements for a specified account access manager application. You can filter results by principal, IAM role, or account. Use pagination to ensure that the operation returns quickly and successfully.
    @Sendable
    @inlinable
    public func listEntitlements(_ input: ListEntitlementsRequest, logger: Logger = AWSClient.loggingDisabled) async throws -> ListEntitlementsResponse {
        try await self.client.execute(
            operation: "ListEntitlements", 
            path: "/entitlements-list", 
            httpMethod: .POST, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    /// Lists the entitlements for a specified account access manager application. You can filter results by principal, IAM role, or account. Use pagination to ensure that the operation returns quickly and successfully.
    ///
    /// Parameters:
    ///   - applicationArn: Specifies the ARN of the application to list entitlements for.
    ///   - filter: Specifies filter criteria to narrow the entitlements returned. You can filter by principal, IAM role, or account.
    ///   - maxResults: Specifies the maximum number of results to return in a single call.
    ///   - nextToken: Specifies the pagination token from a previous call to retrieve the next set of results.
    ///   - logger: Logger use during operation
    @inlinable
    public func listEntitlements(
        applicationArn: String,
        filter: EntitlementFilter,
        maxResults: Int? = nil,
        nextToken: String? = nil,
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws -> ListEntitlementsResponse {
        let input = ListEntitlementsRequest(
            applicationArn: applicationArn, 
            filter: filter, 
            maxResults: maxResults, 
            nextToken: nextToken
        )
        return try await self.listEntitlements(input, logger: logger)
    }

    /// Lists the tags associated with an account access manager resource.
    @Sendable
    @inlinable
    public func listTagsForResource(_ input: ListTagsForResourceRequest, logger: Logger = AWSClient.loggingDisabled) async throws -> ListTagsForResourceResponse {
        try await self.client.execute(
            operation: "ListTagsForResource", 
            path: "/tags/{resourceArn}", 
            httpMethod: .GET, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    /// Lists the tags associated with an account access manager resource.
    ///
    /// Parameters:
    ///   - resourceArn: Specifies the ARN of the resource to list tags for.
    ///   - logger: Logger use during operation
    @inlinable
    public func listTagsForResource(
        resourceArn: String,
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws -> ListTagsForResourceResponse {
        let input = ListTagsForResourceRequest(
            resourceArn: resourceArn
        )
        return try await self.listTagsForResource(input, logger: logger)
    }

    /// Adds tags to an account access manager resource.
    @Sendable
    @inlinable
    public func tagResource(_ input: TagResourceRequest, logger: Logger = AWSClient.loggingDisabled) async throws -> TagResourceResponse {
        try await self.client.execute(
            operation: "TagResource", 
            path: "/tags/{resourceArn}", 
            httpMethod: .POST, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    /// Adds tags to an account access manager resource.
    ///
    /// Parameters:
    ///   - resourceArn: Specifies the ARN of the resource to add tags to.
    ///   - tags: Specifies the tags to add to the resource.
    ///   - logger: Logger use during operation
    @inlinable
    public func tagResource(
        resourceArn: String,
        tags: [String: String],
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws -> TagResourceResponse {
        let input = TagResourceRequest(
            resourceArn: resourceArn, 
            tags: tags
        )
        return try await self.tagResource(input, logger: logger)
    }

    /// Removes tags from an account access manager resource.
    @Sendable
    @inlinable
    public func untagResource(_ input: UntagResourceRequest, logger: Logger = AWSClient.loggingDisabled) async throws -> UntagResourceResponse {
        try await self.client.execute(
            operation: "UntagResource", 
            path: "/tags/{resourceArn}", 
            httpMethod: .DELETE, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    /// Removes tags from an account access manager resource.
    ///
    /// Parameters:
    ///   - resourceArn: Specifies the ARN of the resource to remove tags from.
    ///   - tagKeys: Specifies the tag keys to remove from the resource.
    ///   - logger: Logger use during operation
    @inlinable
    public func untagResource(
        resourceArn: String,
        tagKeys: [String],
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws -> UntagResourceResponse {
        let input = UntagResourceRequest(
            resourceArn: resourceArn, 
            tagKeys: tagKeys
        )
        return try await self.untagResource(input, logger: logger)
    }
}

extension AccountAccess {
    /// Initializer required by `AWSService.with(middlewares:timeout:byteBufferAllocator:options)`. You are not able to use this initializer directly as there are not public
    /// initializers for `AWSServiceConfig.Patch`. Please use `AWSService.with(middlewares:timeout:byteBufferAllocator:options)` instead.
    public init(from: AccountAccess, patch: AWSServiceConfig.Patch) {
        self.client = from.client
        self.config = from.config.with(patch: patch)
    }
}

// MARK: Paginators

@available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
extension AccountAccess {
    /// Return PaginatorSequence for operation ``listApplications(_:logger:)``.
    ///
    /// - Parameters:
    ///   - input: Input for operation
    ///   - logger: Logger used for logging
    @inlinable
    public func listApplicationsPaginator(
        _ input: ListApplicationsRequest,
        logger: Logger = AWSClient.loggingDisabled
    ) -> AWSClient.PaginatorSequence<ListApplicationsRequest, ListApplicationsResponse> {
        return .init(
            input: input,
            command: self.listApplications,
            inputKey: \ListApplicationsRequest.nextToken,
            outputKey: \ListApplicationsResponse.nextToken,
            logger: logger
        )
    }
    /// Return PaginatorSequence for operation ``listApplications(_:logger:)``.
    ///
    /// - Parameters:
    ///   - maxResults: Specifies the maximum number of results to return in a single call.
    ///   - logger: Logger used for logging
    @inlinable
    public func listApplicationsPaginator(
        maxResults: Int? = nil,
        logger: Logger = AWSClient.loggingDisabled        
    ) -> AWSClient.PaginatorSequence<ListApplicationsRequest, ListApplicationsResponse> {
        let input = ListApplicationsRequest(
            maxResults: maxResults
        )
        return self.listApplicationsPaginator(input, logger: logger)
    }

    /// Return PaginatorSequence for operation ``listEntitlements(_:logger:)``.
    ///
    /// - Parameters:
    ///   - input: Input for operation
    ///   - logger: Logger used for logging
    @inlinable
    public func listEntitlementsPaginator(
        _ input: ListEntitlementsRequest,
        logger: Logger = AWSClient.loggingDisabled
    ) -> AWSClient.PaginatorSequence<ListEntitlementsRequest, ListEntitlementsResponse> {
        return .init(
            input: input,
            command: self.listEntitlements,
            inputKey: \ListEntitlementsRequest.nextToken,
            outputKey: \ListEntitlementsResponse.nextToken,
            logger: logger
        )
    }
    /// Return PaginatorSequence for operation ``listEntitlements(_:logger:)``.
    ///
    /// - Parameters:
    ///   - applicationArn: Specifies the ARN of the application to list entitlements for.
    ///   - filter: Specifies filter criteria to narrow the entitlements returned. You can filter by principal, IAM role, or account.
    ///   - maxResults: Specifies the maximum number of results to return in a single call.
    ///   - logger: Logger used for logging
    @inlinable
    public func listEntitlementsPaginator(
        applicationArn: String,
        filter: EntitlementFilter,
        maxResults: Int? = nil,
        logger: Logger = AWSClient.loggingDisabled        
    ) -> AWSClient.PaginatorSequence<ListEntitlementsRequest, ListEntitlementsResponse> {
        let input = ListEntitlementsRequest(
            applicationArn: applicationArn, 
            filter: filter, 
            maxResults: maxResults
        )
        return self.listEntitlementsPaginator(input, logger: logger)
    }
}

extension AccountAccess.ListApplicationsRequest: AWSPaginateToken {
    @inlinable
    public func usingPaginationToken(_ token: String) -> AccountAccess.ListApplicationsRequest {
        return .init(
            maxResults: self.maxResults,
            nextToken: token
        )
    }
}

extension AccountAccess.ListEntitlementsRequest: AWSPaginateToken {
    @inlinable
    public func usingPaginationToken(_ token: String) -> AccountAccess.ListEntitlementsRequest {
        return .init(
            applicationArn: self.applicationArn,
            filter: self.filter,
            maxResults: self.maxResults,
            nextToken: token
        )
    }
}

// MARK: Waiters

@available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
extension AccountAccess {
    /// Waiter for operation ``getApplication(_:logger:)``.
    ///
    /// - Parameters:
    ///   - input: Input for operation
    ///   - logger: Logger used for logging
    @inlinable
    public func waitUntilApplicationActive(
        _ input: GetApplicationRequest,
        maxWaitTime: TimeAmount? = nil,
        logger: Logger = AWSClient.loggingDisabled
    ) async throws {
        let waiter = AWSClient.Waiter<GetApplicationRequest, _>(
            acceptors: [
                .init(state: .success, matcher: try! JMESPathMatcher("status", expected: "ACTIVE")),
                .init(state: .failure, matcher: try! JMESPathMatcher("status", expected: "CREATE_FAILED")),
                .init(state: .failure, matcher: try! JMESPathMatcher("status", expected: "DELETE_FAILED")),
            ],
            minDelayTime: .seconds(5),
            command: self.getApplication
        )
        return try await self.client.waitUntil(input, waiter: waiter, maxWaitTime: maxWaitTime, logger: logger)
    }
    /// Waiter for operation ``getApplication(_:logger:)``.
    ///
    /// - Parameters:
    ///   - applicationArn: Specifies the ARN of the application to retrieve.
    ///   - logger: Logger used for logging
    @inlinable
    public func waitUntilApplicationActive(
        applicationArn: String,
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws {
        let input = GetApplicationRequest(
            applicationArn: applicationArn
        )
        try await self.waitUntilApplicationActive(input, logger: logger)
    }
}
