//===----------------------------------------------------------------------===//
//
// This source file is part of the Soto for AWS open source project
//
// Copyright (c) 2017-2024 the Soto project authors
// Licensed under Apache License v2.0
//
// See LICENSE.txt for license information
// See CONTRIBUTORS.txt for the list of Soto project authors
//
// SPDX-License-Identifier: Apache-2.0
//
//===----------------------------------------------------------------------===//

// THIS FILE IS AUTOMATICALLY GENERATED by https://github.com/soto-project/soto-codegenerator.
// DO NOT EDIT.

#if canImport(FoundationEssentials)
import FoundationEssentials
#else
import Foundation
#endif
@_spi(SotoInternal) import SotoCore

extension AccountAccess {
    // MARK: Enums

    public enum ErrorCode: String, CustomStringConvertible, Codable, Sendable, CodingKeyRepresentable {
        case authorizationError = "AUTHORIZATION_ERROR"
        case internalServiceError = "INTERNAL_SERVICE_ERROR"
        case resourceNotFoundError = "RESOURCE_NOT_FOUND_ERROR"
        case serviceQuotaExceededError = "SERVICE_QUOTA_EXCEEDED_ERROR"
        public var description: String { return self.rawValue }
    }

    public enum Status: String, CustomStringConvertible, Codable, Sendable, CodingKeyRepresentable {
        case active = "ACTIVE"
        case createFailed = "CREATE_FAILED"
        case createInProgress = "CREATE_IN_PROGRESS"
        case deleteFailed = "DELETE_FAILED"
        case deleteInProgress = "DELETE_IN_PROGRESS"
        public var description: String { return self.rawValue }
    }

    public enum IdentityCenterPrincipal: AWSEncodableShape & AWSDecodableShape, Sendable {
        /// The unique identifier of a group in IAM Identity Center.
        case groupId(String)
        /// The unique identifier of a user in IAM Identity Center.
        case userId(String)

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            guard container.allKeys.count == 1, let key = container.allKeys.first else {
                let context = DecodingError.Context(
                    codingPath: container.codingPath,
                    debugDescription: "Expected exactly one key, but got \(container.allKeys.count)"
                )
                throw DecodingError.dataCorrupted(context)
            }
            switch key {
            case .groupId:
                let value = try container.decode(String.self, forKey: .groupId)
                self = .groupId(value)
            case .userId:
                let value = try container.decode(String.self, forKey: .userId)
                self = .userId(value)
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            switch self {
            case .groupId(let value):
                try container.encode(value, forKey: .groupId)
            case .userId(let value):
                try container.encode(value, forKey: .userId)
            }
        }

        public func validate(name: String) throws {
            switch self {
            case .groupId(let value):
                try self.validate(value, name: "groupId", parent: name, max: 47)
                try self.validate(value, name: "groupId", parent: name, min: 1)
                try self.validate(value, name: "groupId", parent: name, pattern: "^([0-9a-f]{10}-|)[A-Fa-f0-9]{8}-[A-Fa-f0-9]{4}-[A-Fa-f0-9]{4}-[A-Fa-f0-9]{4}-[A-Fa-f0-9]{12}$")
            case .userId(let value):
                try self.validate(value, name: "userId", parent: name, max: 47)
                try self.validate(value, name: "userId", parent: name, min: 1)
                try self.validate(value, name: "userId", parent: name, pattern: "^([0-9a-f]{10}-|)[A-Fa-f0-9]{8}-[A-Fa-f0-9]{4}-[A-Fa-f0-9]{4}-[A-Fa-f0-9]{4}-[A-Fa-f0-9]{12}$")
            }
        }

        private enum CodingKeys: String, CodingKey {
            case groupId = "groupId"
            case userId = "userId"
        }
    }

    public enum IdentityCenterPrincipalFilter: AWSEncodableShape, Sendable {
        /// The unique identifier of a group in IAM Identity Center to filter by.
        case groupId(String)
        /// The unique identifier of a user in IAM Identity Center to filter by.
        case userId(String)

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            switch self {
            case .groupId(let value):
                try container.encode(value, forKey: .groupId)
            case .userId(let value):
                try container.encode(value, forKey: .userId)
            }
        }

        public func validate(name: String) throws {
            switch self {
            case .groupId(let value):
                try self.validate(value, name: "groupId", parent: name, max: 47)
                try self.validate(value, name: "groupId", parent: name, min: 1)
                try self.validate(value, name: "groupId", parent: name, pattern: "^([0-9a-f]{10}-|)[A-Fa-f0-9]{8}-[A-Fa-f0-9]{4}-[A-Fa-f0-9]{4}-[A-Fa-f0-9]{4}-[A-Fa-f0-9]{12}$")
            case .userId(let value):
                try self.validate(value, name: "userId", parent: name, max: 47)
                try self.validate(value, name: "userId", parent: name, min: 1)
                try self.validate(value, name: "userId", parent: name, pattern: "^([0-9a-f]{10}-|)[A-Fa-f0-9]{8}-[A-Fa-f0-9]{4}-[A-Fa-f0-9]{4}-[A-Fa-f0-9]{4}-[A-Fa-f0-9]{12}$")
            }
        }

        private enum CodingKeys: String, CodingKey {
            case groupId = "groupId"
            case userId = "userId"
        }
    }

    // MARK: Shapes

    public struct ApplicationSummary: AWSDecodableShape {
        /// The ARN of the application.
        public let applicationArn: String
        /// The date and time when the application was created.
        @CustomCoding<ISO8601DateCoder>
        public var createdAt: Date
        /// The tenant identifier associated with the application.
        public let tenantId: String?
        /// The date and time when the application was last updated.
        @CustomCoding<ISO8601DateCoder>
        public var updatedAt: Date

        @inlinable
        public init(applicationArn: String, createdAt: Date, tenantId: String? = nil, updatedAt: Date) {
            self.applicationArn = applicationArn
            self.createdAt = createdAt
            self.tenantId = tenantId
            self.updatedAt = updatedAt
        }

        private enum CodingKeys: String, CodingKey {
            case applicationArn = "applicationArn"
            case createdAt = "createdAt"
            case tenantId = "tenantId"
            case updatedAt = "updatedAt"
        }
    }

    public struct CreateApplicationRequest: AWSEncodableShape {
        /// Specifies the identity source for the application. The identity source defines the IAM Identity Center instance that provides principals for entitlements.
        public let identitySource: IdentitySource
        /// Specifies the tags to assign to the application.
        public let tags: [String: String]?

        @inlinable
        public init(identitySource: IdentitySource, tags: [String: String]? = nil) {
            self.identitySource = identitySource
            self.tags = tags
        }

        public func validate(name: String) throws {
            try self.identitySource.validate(name: "\(name).identitySource")
        }

        private enum CodingKeys: String, CodingKey {
            case identitySource = "identitySource"
            case tags = "tags"
        }
    }

    public struct CreateApplicationResponse: AWSDecodableShape {
        /// The Amazon Resource Name (ARN) of the created application.
        public let applicationArn: String

        @inlinable
        public init(applicationArn: String) {
            self.applicationArn = applicationArn
        }

        private enum CodingKeys: String, CodingKey {
            case applicationArn = "applicationArn"
        }
    }

    public struct CreateEntitlementRequest: AWSEncodableShape {
        /// Specifies the ARN of the application to create the entitlement for.
        public let applicationArn: String
        /// Specifies the entitlement configuration, including the principal and the IAM role to grant access to.
        public let entitlement: Entitlement

        @inlinable
        public init(applicationArn: String, entitlement: Entitlement) {
            self.applicationArn = applicationArn
            self.entitlement = entitlement
        }

        public func validate(name: String) throws {
            try self.validate(self.applicationArn, name: "applicationArn", parent: name, max: 2048)
            try self.validate(self.applicationArn, name: "applicationArn", parent: name, min: 49)
            try self.validate(self.applicationArn, name: "applicationArn", parent: name, pattern: "^arn:[a-z0-9-]+:account-access:[a-z0-9]+(-[a-z0-9]+)*:[0-9]{12}:application/[a-zA-Z0-9-]+$")
            try self.entitlement.validate(name: "\(name).entitlement")
        }

        private enum CodingKeys: String, CodingKey {
            case applicationArn = "applicationArn"
            case entitlement = "entitlement"
        }
    }

    public struct CreateEntitlementResponse: AWSDecodableShape {
        /// The unique identifier of the created entitlement.
        public let entitlementId: String

        @inlinable
        public init(entitlementId: String) {
            self.entitlementId = entitlementId
        }

        private enum CodingKeys: String, CodingKey {
            case entitlementId = "entitlementId"
        }
    }

    public struct DeleteApplicationRequest: AWSEncodableShape {
        /// Specifies the ARN of the application to delete.
        public let applicationArn: String

        @inlinable
        public init(applicationArn: String) {
            self.applicationArn = applicationArn
        }

        public func encode(to encoder: Encoder) throws {
            let request = encoder.userInfo[.awsRequest]! as! RequestEncodingContainer
            _ = encoder.container(keyedBy: CodingKeys.self)
            request.encodePath(self.applicationArn, key: "applicationArn")
        }

        public func validate(name: String) throws {
            try self.validate(self.applicationArn, name: "applicationArn", parent: name, max: 2048)
            try self.validate(self.applicationArn, name: "applicationArn", parent: name, min: 49)
            try self.validate(self.applicationArn, name: "applicationArn", parent: name, pattern: "^arn:[a-z0-9-]+:account-access:[a-z0-9]+(-[a-z0-9]+)*:[0-9]{12}:application/[a-zA-Z0-9-]+$")
        }

        private enum CodingKeys: CodingKey {}
    }

    public struct DeleteApplicationResponse: AWSDecodableShape {
        public init() {}
    }

    public struct DeleteEntitlementRequest: AWSEncodableShape {
        /// Specifies the ARN of the application that the entitlement belongs to.
        public let applicationArn: String
        /// Specifies the unique identifier of the entitlement to delete.
        public let entitlementId: String

        @inlinable
        public init(applicationArn: String, entitlementId: String) {
            self.applicationArn = applicationArn
            self.entitlementId = entitlementId
        }

        public func encode(to encoder: Encoder) throws {
            let request = encoder.userInfo[.awsRequest]! as! RequestEncodingContainer
            _ = encoder.container(keyedBy: CodingKeys.self)
            request.encodeQuery(self.applicationArn, key: "applicationArn")
            request.encodePath(self.entitlementId, key: "entitlementId")
        }

        public func validate(name: String) throws {
            try self.validate(self.applicationArn, name: "applicationArn", parent: name, max: 2048)
            try self.validate(self.applicationArn, name: "applicationArn", parent: name, min: 49)
            try self.validate(self.applicationArn, name: "applicationArn", parent: name, pattern: "^arn:[a-z0-9-]+:account-access:[a-z0-9]+(-[a-z0-9]+)*:[0-9]{12}:application/[a-zA-Z0-9-]+$")
        }

        private enum CodingKeys: CodingKey {}
    }

    public struct DeleteEntitlementResponse: AWSDecodableShape {
        public init() {}
    }

    public struct EntitlementFilter: AWSEncodableShape {
        /// The principal-to-role filter criteria for narrowing entitlement results.
        public let principalRole: PrincipalRoleEntitlementFilter?

        @inlinable
        public init(principalRole: PrincipalRoleEntitlementFilter? = nil) {
            self.principalRole = principalRole
        }

        public func validate(name: String) throws {
            try self.principalRole?.validate(name: "\(name).principalRole")
        }

        private enum CodingKeys: String, CodingKey {
            case principalRole = "principalRole"
        }
    }

    public struct EntitlementsListMember: AWSDecodableShape {
        /// The date and time when the entitlement was created.
        @CustomCoding<ISO8601DateCoder>
        public var createdAt: Date
        /// The summary information for the entitlement.
        public let entitlement: EntitlementSummary
        /// The unique identifier of the entitlement.
        public let entitlementId: String

        @inlinable
        public init(createdAt: Date, entitlement: EntitlementSummary, entitlementId: String) {
            self.createdAt = createdAt
            self.entitlement = entitlement
            self.entitlementId = entitlementId
        }

        private enum CodingKeys: String, CodingKey {
            case createdAt = "createdAt"
            case entitlement = "entitlement"
            case entitlementId = "entitlementId"
        }
    }

    public struct ErrorDetails: AWSDecodableShape {
        /// The error code that identifies the type of error.
        public let code: ErrorCode
        /// A human-readable message that describes the error.
        public let message: String

        @inlinable
        public init(code: ErrorCode, message: String) {
            self.code = code
            self.message = message
        }

        private enum CodingKeys: String, CodingKey {
            case code = "code"
            case message = "message"
        }
    }

    public struct GetApplicationRequest: AWSEncodableShape {
        /// Specifies the ARN of the application to retrieve.
        public let applicationArn: String

        @inlinable
        public init(applicationArn: String) {
            self.applicationArn = applicationArn
        }

        public func encode(to encoder: Encoder) throws {
            let request = encoder.userInfo[.awsRequest]! as! RequestEncodingContainer
            _ = encoder.container(keyedBy: CodingKeys.self)
            request.encodePath(self.applicationArn, key: "applicationArn")
        }

        public func validate(name: String) throws {
            try self.validate(self.applicationArn, name: "applicationArn", parent: name, max: 2048)
            try self.validate(self.applicationArn, name: "applicationArn", parent: name, min: 49)
            try self.validate(self.applicationArn, name: "applicationArn", parent: name, pattern: "^arn:[a-z0-9-]+:account-access:[a-z0-9]+(-[a-z0-9]+)*:[0-9]{12}:application/[a-zA-Z0-9-]+$")
        }

        private enum CodingKeys: CodingKey {}
    }

    public struct GetApplicationResponse: AWSDecodableShape {
        /// The date and time when the application was created.
        @CustomCoding<ISO8601DateCoder>
        public var createdAt: Date
        /// The error details if the application is in a failed state.
        public let error: ErrorDetails?
        /// The identity source details for the application, including the IAM Identity Center instance configuration.
        public let identitySource: IdentitySourceDetails
        /// The current status of the application.
        public let status: Status
        /// The tags associated with the application.
        public let tags: [String: String]?
        /// The tenant identifier associated with the application.
        public let tenantId: String?
        /// The date and time when the application was last updated.
        @CustomCoding<ISO8601DateCoder>
        public var updatedAt: Date

        @inlinable
        public init(createdAt: Date, error: ErrorDetails? = nil, identitySource: IdentitySourceDetails, status: Status, tags: [String: String]? = nil, tenantId: String? = nil, updatedAt: Date) {
            self.createdAt = createdAt
            self.error = error
            self.identitySource = identitySource
            self.status = status
            self.tags = tags
            self.tenantId = tenantId
            self.updatedAt = updatedAt
        }

        private enum CodingKeys: String, CodingKey {
            case createdAt = "createdAt"
            case error = "error"
            case identitySource = "identitySource"
            case status = "status"
            case tags = "tags"
            case tenantId = "tenantId"
            case updatedAt = "updatedAt"
        }
    }

    public struct GetEntitlementRequest: AWSEncodableShape {
        /// Specifies the ARN of the application that the entitlement belongs to.
        public let applicationArn: String
        /// Specifies the unique identifier of the entitlement to retrieve.
        public let entitlementId: String

        @inlinable
        public init(applicationArn: String, entitlementId: String) {
            self.applicationArn = applicationArn
            self.entitlementId = entitlementId
        }

        public func encode(to encoder: Encoder) throws {
            let request = encoder.userInfo[.awsRequest]! as! RequestEncodingContainer
            _ = encoder.container(keyedBy: CodingKeys.self)
            request.encodeQuery(self.applicationArn, key: "applicationArn")
            request.encodePath(self.entitlementId, key: "entitlementId")
        }

        public func validate(name: String) throws {
            try self.validate(self.applicationArn, name: "applicationArn", parent: name, max: 2048)
            try self.validate(self.applicationArn, name: "applicationArn", parent: name, min: 49)
            try self.validate(self.applicationArn, name: "applicationArn", parent: name, pattern: "^arn:[a-z0-9-]+:account-access:[a-z0-9]+(-[a-z0-9]+)*:[0-9]{12}:application/[a-zA-Z0-9-]+$")
        }

        private enum CodingKeys: CodingKey {}
    }

    public struct GetEntitlementResponse: AWSDecodableShape {
        /// The ARN of the application that the entitlement belongs to.
        public let applicationArn: String
        /// The date and time when the entitlement was created.
        @CustomCoding<ISO8601DateCoder>
        public var createdAt: Date
        /// The entitlement details, including the principal, IAM role, and target account.
        public let entitlement: EntitlementDetails
        /// The unique identifier of the entitlement.
        public let entitlementId: String

        @inlinable
        public init(applicationArn: String, createdAt: Date, entitlement: EntitlementDetails, entitlementId: String) {
            self.applicationArn = applicationArn
            self.createdAt = createdAt
            self.entitlement = entitlement
            self.entitlementId = entitlementId
        }

        private enum CodingKeys: String, CodingKey {
            case applicationArn = "applicationArn"
            case createdAt = "createdAt"
            case entitlement = "entitlement"
            case entitlementId = "entitlementId"
        }
    }

    public struct IdentityCenter: AWSEncodableShape {
        /// The ARN of the IAM Identity Center instance.
        public let instanceArn: String

        @inlinable
        public init(instanceArn: String) {
            self.instanceArn = instanceArn
        }

        public func validate(name: String) throws {
            try self.validate(self.instanceArn, name: "instanceArn", parent: name, max: 1224)
            try self.validate(self.instanceArn, name: "instanceArn", parent: name, min: 10)
            try self.validate(self.instanceArn, name: "instanceArn", parent: name, pattern: "^arn:[a-z0-9-]+:sso:::instance/(sso)?ins-[a-zA-Z0-9-.]{16}$")
        }

        private enum CodingKeys: String, CodingKey {
            case instanceArn = "instanceArn"
        }
    }

    public struct IdentityCenterDetails: AWSDecodableShape {
        /// The ARN of the IAM Identity Center application created for this account access manager application.
        public let applicationArn: String?
        /// The ARN of the IAM Identity Center instance.
        public let instanceArn: String

        @inlinable
        public init(applicationArn: String? = nil, instanceArn: String) {
            self.applicationArn = applicationArn
            self.instanceArn = instanceArn
        }

        private enum CodingKeys: String, CodingKey {
            case applicationArn = "applicationArn"
            case instanceArn = "instanceArn"
        }
    }

    public struct ListApplicationsRequest: AWSEncodableShape {
        /// Specifies the maximum number of results to return in a single call.
        public let maxResults: Int?
        /// Specifies the pagination token from a previous call to retrieve the next set of results.
        public let nextToken: String?

        @inlinable
        public init(maxResults: Int? = nil, nextToken: String? = nil) {
            self.maxResults = maxResults
            self.nextToken = nextToken
        }

        private enum CodingKeys: String, CodingKey {
            case maxResults = "maxResults"
            case nextToken = "nextToken"
        }
    }

    public struct ListApplicationsResponse: AWSDecodableShape {
        /// The list of applications.
        public let applications: [ApplicationSummary]
        /// The pagination token to use in a subsequent request to retrieve the next set of results. This value is null when there are no more results to return.
        public let nextToken: String?

        @inlinable
        public init(applications: [ApplicationSummary], nextToken: String? = nil) {
            self.applications = applications
            self.nextToken = nextToken
        }

        private enum CodingKeys: String, CodingKey {
            case applications = "applications"
            case nextToken = "nextToken"
        }
    }

    public struct ListEntitlementsRequest: AWSEncodableShape {
        /// Specifies the ARN of the application to list entitlements for.
        public let applicationArn: String
        /// Specifies filter criteria to narrow the entitlements returned. You can filter by principal, IAM role, or account.
        public let filter: EntitlementFilter
        /// Specifies the maximum number of results to return in a single call.
        public let maxResults: Int?
        /// Specifies the pagination token from a previous call to retrieve the next set of results.
        public let nextToken: String?

        @inlinable
        public init(applicationArn: String, filter: EntitlementFilter, maxResults: Int? = nil, nextToken: String? = nil) {
            self.applicationArn = applicationArn
            self.filter = filter
            self.maxResults = maxResults
            self.nextToken = nextToken
        }

        public func validate(name: String) throws {
            try self.validate(self.applicationArn, name: "applicationArn", parent: name, max: 2048)
            try self.validate(self.applicationArn, name: "applicationArn", parent: name, min: 49)
            try self.validate(self.applicationArn, name: "applicationArn", parent: name, pattern: "^arn:[a-z0-9-]+:account-access:[a-z0-9]+(-[a-z0-9]+)*:[0-9]{12}:application/[a-zA-Z0-9-]+$")
            try self.filter.validate(name: "\(name).filter")
        }

        private enum CodingKeys: String, CodingKey {
            case applicationArn = "applicationArn"
            case filter = "filter"
            case maxResults = "maxResults"
            case nextToken = "nextToken"
        }
    }

    public struct ListEntitlementsResponse: AWSDecodableShape {
        /// The list of entitlements for the specified application.
        public let entitlements: [EntitlementsListMember]
        /// The pagination token to use in a subsequent request to retrieve the next set of results. This value is null when there are no more results to return.
        public let nextToken: String?

        @inlinable
        public init(entitlements: [EntitlementsListMember], nextToken: String? = nil) {
            self.entitlements = entitlements
            self.nextToken = nextToken
        }

        private enum CodingKeys: String, CodingKey {
            case entitlements = "entitlements"
            case nextToken = "nextToken"
        }
    }

    public struct ListTagsForResourceRequest: AWSEncodableShape {
        /// Specifies the ARN of the resource to list tags for.
        public let resourceArn: String

        @inlinable
        public init(resourceArn: String) {
            self.resourceArn = resourceArn
        }

        public func encode(to encoder: Encoder) throws {
            let request = encoder.userInfo[.awsRequest]! as! RequestEncodingContainer
            _ = encoder.container(keyedBy: CodingKeys.self)
            request.encodePath(self.resourceArn, key: "resourceArn")
        }

        public func validate(name: String) throws {
            try self.validate(self.resourceArn, name: "resourceArn", parent: name, max: 2048)
            try self.validate(self.resourceArn, name: "resourceArn", parent: name, min: 49)
            try self.validate(self.resourceArn, name: "resourceArn", parent: name, pattern: "^arn:[a-z0-9-]+:account-access:[a-z0-9]+(-[a-z0-9]+)*:[0-9]{12}:application/[a-zA-Z0-9-]+$")
        }

        private enum CodingKeys: CodingKey {}
    }

    public struct ListTagsForResourceResponse: AWSDecodableShape {
        /// The tags associated with the resource.
        public let tags: [String: String]?

        @inlinable
        public init(tags: [String: String]? = nil) {
            self.tags = tags
        }

        private enum CodingKeys: String, CodingKey {
            case tags = "tags"
        }
    }

    public struct PrincipalRoleEntitlement: AWSEncodableShape {
        /// The principal (user or group) that is granted access to assume the IAM role.
        public let principal: Principal
        /// The ARN of the IAM role that the principal can assume.
        public let roleArn: String

        @inlinable
        public init(principal: Principal, roleArn: String) {
            self.principal = principal
            self.roleArn = roleArn
        }

        public func validate(name: String) throws {
            try self.principal.validate(name: "\(name).principal")
            try self.validate(self.roleArn, name: "roleArn", parent: name, pattern: "^arn:[a-z0-9-]+:iam::[0-9]{12}:role/([a-zA-Z0-9+=,.@_-]+/)*[a-zA-Z0-9+=,.@_-]+$")
        }

        private enum CodingKeys: String, CodingKey {
            case principal = "principal"
            case roleArn = "roleArn"
        }
    }

    public struct PrincipalRoleEntitlementDetails: AWSDecodableShape {
        /// The 12-digit Amazon Web Services account ID where the IAM role resides.
        public let account: String
        /// The friendly name of the Amazon Web Services account where the IAM role resides.
        public let accountName: String?
        /// The principal (user or group) that is granted access to assume the IAM role.
        public let principal: Principal
        /// The ARN of the IAM role that the principal can assume.
        public let roleArn: String

        @inlinable
        public init(account: String, accountName: String? = nil, principal: Principal, roleArn: String) {
            self.account = account
            self.accountName = accountName
            self.principal = principal
            self.roleArn = roleArn
        }

        private enum CodingKeys: String, CodingKey {
            case account = "account"
            case accountName = "accountName"
            case principal = "principal"
            case roleArn = "roleArn"
        }
    }

    public struct PrincipalRoleEntitlementFilter: AWSEncodableShape {
        /// The 12-digit Amazon Web Services account ID to filter entitlements by.
        public let account: String?
        /// The principal to filter entitlements by.
        public let principal: PrincipalFilter?
        /// The IAM role ARN to filter entitlements by.
        public let roleArn: String?

        @inlinable
        public init(account: String? = nil, principal: PrincipalFilter? = nil, roleArn: String? = nil) {
            self.account = account
            self.principal = principal
            self.roleArn = roleArn
        }

        public func validate(name: String) throws {
            try self.validate(self.account, name: "account", parent: name, max: 12)
            try self.validate(self.account, name: "account", parent: name, min: 12)
            try self.validate(self.account, name: "account", parent: name, pattern: "^[0-9]{12}$")
            try self.principal?.validate(name: "\(name).principal")
            try self.validate(self.roleArn, name: "roleArn", parent: name, pattern: "^arn:[a-z0-9-]+:iam::[0-9]{12}:role/([a-zA-Z0-9+=,.@_-]+/)*[a-zA-Z0-9+=,.@_-]+$")
        }

        private enum CodingKeys: String, CodingKey {
            case account = "account"
            case principal = "principal"
            case roleArn = "roleArn"
        }
    }

    public struct PrincipalRoleEntitlementSummary: AWSDecodableShape {
        /// The 12-digit Amazon Web Services account ID where the IAM role resides.
        public let account: String
        /// The friendly name of the Amazon Web Services account where the IAM role resides.
        public let accountName: String?
        /// The principal (user or group) that is granted access to assume the IAM role.
        public let principal: Principal
        /// The ARN of the IAM role that the principal can assume.
        public let roleArn: String

        @inlinable
        public init(account: String, accountName: String? = nil, principal: Principal, roleArn: String) {
            self.account = account
            self.accountName = accountName
            self.principal = principal
            self.roleArn = roleArn
        }

        private enum CodingKeys: String, CodingKey {
            case account = "account"
            case accountName = "accountName"
            case principal = "principal"
            case roleArn = "roleArn"
        }
    }

    public struct TagResourceRequest: AWSEncodableShape {
        /// Specifies the ARN of the resource to add tags to.
        public let resourceArn: String
        /// Specifies the tags to add to the resource.
        public let tags: [String: String]

        @inlinable
        public init(resourceArn: String, tags: [String: String]) {
            self.resourceArn = resourceArn
            self.tags = tags
        }

        public func encode(to encoder: Encoder) throws {
            let request = encoder.userInfo[.awsRequest]! as! RequestEncodingContainer
            var container = encoder.container(keyedBy: CodingKeys.self)
            request.encodePath(self.resourceArn, key: "resourceArn")
            try container.encode(self.tags, forKey: .tags)
        }

        public func validate(name: String) throws {
            try self.validate(self.resourceArn, name: "resourceArn", parent: name, max: 2048)
            try self.validate(self.resourceArn, name: "resourceArn", parent: name, min: 49)
            try self.validate(self.resourceArn, name: "resourceArn", parent: name, pattern: "^arn:[a-z0-9-]+:account-access:[a-z0-9]+(-[a-z0-9]+)*:[0-9]{12}:application/[a-zA-Z0-9-]+$")
        }

        private enum CodingKeys: String, CodingKey {
            case tags = "tags"
        }
    }

    public struct TagResourceResponse: AWSDecodableShape {
        public init() {}
    }

    public struct UntagResourceRequest: AWSEncodableShape {
        /// Specifies the ARN of the resource to remove tags from.
        public let resourceArn: String
        /// Specifies the tag keys to remove from the resource.
        public let tagKeys: [String]

        @inlinable
        public init(resourceArn: String, tagKeys: [String]) {
            self.resourceArn = resourceArn
            self.tagKeys = tagKeys
        }

        public func encode(to encoder: Encoder) throws {
            let request = encoder.userInfo[.awsRequest]! as! RequestEncodingContainer
            _ = encoder.container(keyedBy: CodingKeys.self)
            request.encodePath(self.resourceArn, key: "resourceArn")
            request.encodeQuery(self.tagKeys, key: "tagKeys")
        }

        public func validate(name: String) throws {
            try self.validate(self.resourceArn, name: "resourceArn", parent: name, max: 2048)
            try self.validate(self.resourceArn, name: "resourceArn", parent: name, min: 49)
            try self.validate(self.resourceArn, name: "resourceArn", parent: name, pattern: "^arn:[a-z0-9-]+:account-access:[a-z0-9]+(-[a-z0-9]+)*:[0-9]{12}:application/[a-zA-Z0-9-]+$")
        }

        private enum CodingKeys: CodingKey {}
    }

    public struct UntagResourceResponse: AWSDecodableShape {
        public init() {}
    }

    public struct Entitlement: AWSEncodableShape {
        /// The principal-to-role mapping for the entitlement.
        public let principalRole: PrincipalRoleEntitlement?

        @inlinable
        public init(principalRole: PrincipalRoleEntitlement? = nil) {
            self.principalRole = principalRole
        }

        public func validate(name: String) throws {
            try self.principalRole?.validate(name: "\(name).principalRole")
        }

        private enum CodingKeys: String, CodingKey {
            case principalRole = "principalRole"
        }
    }

    public struct EntitlementDetails: AWSDecodableShape {
        /// The principal-to-role mapping details for the entitlement, including the target account.
        public let principalRole: PrincipalRoleEntitlementDetails?

        @inlinable
        public init(principalRole: PrincipalRoleEntitlementDetails? = nil) {
            self.principalRole = principalRole
        }

        private enum CodingKeys: String, CodingKey {
            case principalRole = "principalRole"
        }
    }

    public struct EntitlementSummary: AWSDecodableShape {
        /// The principal-to-role mapping summary for the entitlement.
        public let principalRole: PrincipalRoleEntitlementSummary?

        @inlinable
        public init(principalRole: PrincipalRoleEntitlementSummary? = nil) {
            self.principalRole = principalRole
        }

        private enum CodingKeys: String, CodingKey {
            case principalRole = "principalRole"
        }
    }

    public struct IdentitySource: AWSEncodableShape {
        /// The IAM Identity Center instance to use as the identity source.
        public let identityCenter: IdentityCenter?

        @inlinable
        public init(identityCenter: IdentityCenter? = nil) {
            self.identityCenter = identityCenter
        }

        public func validate(name: String) throws {
            try self.identityCenter?.validate(name: "\(name).identityCenter")
        }

        private enum CodingKeys: String, CodingKey {
            case identityCenter = "identityCenter"
        }
    }

    public struct IdentitySourceDetails: AWSDecodableShape {
        /// The IAM Identity Center configuration details for the identity source.
        public let identityCenter: IdentityCenterDetails?

        @inlinable
        public init(identityCenter: IdentityCenterDetails? = nil) {
            self.identityCenter = identityCenter
        }

        private enum CodingKeys: String, CodingKey {
            case identityCenter = "identityCenter"
        }
    }

    public struct Principal: AWSEncodableShape & AWSDecodableShape {
        /// The IAM Identity Center principal (user or group).
        public let identityCenter: IdentityCenterPrincipal?

        @inlinable
        public init(identityCenter: IdentityCenterPrincipal? = nil) {
            self.identityCenter = identityCenter
        }

        public func validate(name: String) throws {
            try self.identityCenter?.validate(name: "\(name).identityCenter")
        }

        private enum CodingKeys: String, CodingKey {
            case identityCenter = "identityCenter"
        }
    }

    public struct PrincipalFilter: AWSEncodableShape {
        /// The IAM Identity Center principal filter criteria.
        public let identityCenter: IdentityCenterPrincipalFilter?

        @inlinable
        public init(identityCenter: IdentityCenterPrincipalFilter? = nil) {
            self.identityCenter = identityCenter
        }

        public func validate(name: String) throws {
            try self.identityCenter?.validate(name: "\(name).identityCenter")
        }

        private enum CodingKeys: String, CodingKey {
            case identityCenter = "identityCenter"
        }
    }
}

// MARK: - Errors

/// Error enum for AccountAccess
public struct AccountAccessErrorType: AWSErrorType {
    enum Code: String {
        case accessDeniedException = "AccessDeniedException"
        case alreadyCreatedException = "AlreadyCreatedException"
        case conflictException = "ConflictException"
        case internalServerException = "InternalServerException"
        case resourceNotFoundException = "ResourceNotFoundException"
        case serviceQuotaExceededException = "ServiceQuotaExceededException"
        case throttlingException = "ThrottlingException"
        case validationException = "ValidationException"
    }

    private let error: Code
    public let context: AWSErrorContext?

    /// initialize AccountAccess
    public init?(errorCode: String, context: AWSErrorContext) {
        guard let error = Code(rawValue: errorCode) else { return nil }
        self.error = error
        self.context = context
    }

    internal init(_ error: Code) {
        self.error = error
        self.context = nil
    }

    /// return error code string
    public var errorCode: String { self.error.rawValue }

    /// You do not have sufficient access to perform this operation.
    public static var accessDeniedException: Self { .init(.accessDeniedException) }
    /// The resource you are trying to create already exists. To retrieve the existing resource, use the corresponding Get operation.
    public static var alreadyCreatedException: Self { .init(.alreadyCreatedException) }
    /// The request conflicts with the current state of the resource.
    public static var conflictException: Self { .init(.conflictException) }
    /// An internal service error occurred. Try your request again later.
    public static var internalServerException: Self { .init(.internalServerException) }
    /// The specified resource does not exist. Verify that the resource identifier is correct and that the resource exists in the current Region.
    public static var resourceNotFoundException: Self { .init(.resourceNotFoundException) }
    /// The request exceeds a service quota for your account.
    public static var serviceQuotaExceededException: Self { .init(.serviceQuotaExceededException) }
    /// The request was denied due to request throttling. Try your request again later.
    public static var throttlingException: Self { .init(.throttlingException) }
    /// The input does not satisfy the constraints specified by the service. Check your request parameters and retry the request.
    public static var validationException: Self { .init(.validationException) }
}

extension AccountAccessErrorType: Equatable {
    public static func == (lhs: AccountAccessErrorType, rhs: AccountAccessErrorType) -> Bool {
        lhs.error == rhs.error
    }
}

extension AccountAccessErrorType: CustomStringConvertible {
    public var description: String {
        return "\(self.error.rawValue): \(self.message ?? "")"
    }
}
