//===----------------------------------------------------------------------===//
//
// This source file is part of the Soto for AWS open source project
//
// Copyright (c) 2017-2024 the Soto project authors
// Licensed under Apache License v2.0
//
// See LICENSE.txt for license information
// See CONTRIBUTORS.txt for the list of Soto project authors
//
// SPDX-License-Identifier: Apache-2.0
//
//===----------------------------------------------------------------------===//

// THIS FILE IS AUTOMATICALLY GENERATED by https://github.com/soto-project/soto-codegenerator.
// DO NOT EDIT.

#if canImport(FoundationEssentials)
import FoundationEssentials
#else
import Foundation
#endif
@_exported import SotoCore

/// Service object for interacting with AWS ElementalInference service.
///
/// This is the AWS Elemental Inference REST API Reference. It provides information on the URL, request contents, and response contents of each AWS Elemental Inference REST operation.  We assume that you have the IAM permissions that you need to use AWS Elemental Inference via the REST API. We also assume that you are familiar with the features and operations of AWS Elemental Inference as described in AWS Elemental Inference User Guide.
public struct ElementalInference: AWSService {
    // MARK: Member variables

    /// Client used for communication with AWS
    public let client: AWSClient
    /// Service configuration
    public let config: AWSServiceConfig

    // MARK: Initialization

    /// Initialize the ElementalInference client
    /// - parameters:
    ///     - client: AWSClient used to process requests
    ///     - region: Region of server you want to communicate with. This will override the partition parameter.
    ///     - partition: AWS partition where service resides, standard (.aws), china (.awscn), government (.awsusgov).
    ///     - endpoint: Custom endpoint URL to use instead of standard AWS servers
    ///     - middleware: Middleware chain used to edit requests before they are sent and responses before they are decoded 
    ///     - timeout: Timeout value for HTTP requests
    ///     - byteBufferAllocator: Allocator for ByteBuffers
    ///     - options: Service options
    public init(
        client: AWSClient,
        region: SotoCore.Region? = nil,
        partition: AWSPartition = .aws,
        endpoint: String? = nil,
        middleware: AWSMiddlewareProtocol? = nil,
        timeout: TimeAmount? = nil,
        byteBufferAllocator: ByteBufferAllocator = ByteBufferAllocator(),
        options: AWSServiceConfig.Options = []
    ) {
        self.client = client
        self.config = AWSServiceConfig(
            region: region,
            partition: region?.partition ?? partition,
            serviceName: "ElementalInference",
            serviceIdentifier: "elemental-inference",
            serviceProtocol: .restjson,
            apiVersion: "2018-11-14",
            endpoint: endpoint,
            errorType: ElementalInferenceErrorType.self,
            middleware: middleware,
            timeout: timeout,
            byteBufferAllocator: byteBufferAllocator,
            options: options
        )
    }





    // MARK: API Calls

    /// Associates a resource with the feed. The resource provides the input that Elemental Inference needs needs in order to perform an Elemental Inference feature, such as cropping video. You always provide the resource by associating it with a feed. You can associate only one resource with each feed.
    @Sendable
    @inlinable
    public func associateFeed(_ input: AssociateFeedRequest, logger: Logger = AWSClient.loggingDisabled) async throws -> AssociateFeedResponse {
        try await self.client.execute(
            operation: "AssociateFeed", 
            path: "/v1/feed/{id}/associate", 
            httpMethod: .POST, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    /// Associates a resource with the feed. The resource provides the input that Elemental Inference needs needs in order to perform an Elemental Inference feature, such as cropping video. You always provide the resource by associating it with a feed. You can associate only one resource with each feed.
    ///
    /// Parameters:
    ///   - associatedResourceName: An identifier for the resource. If the resource is from an AWS service, this identifier must be the full ARN of that resource. Otherwise, the identifier is a name that you assign and that is appropriate for the application that owns the resource. This name must not resemble an ARN.
    ///   - dryRun: Set to true if you want to do a dry run of the associate action.
    ///   - id: The ID of the feed.
    ///   - outputs: The outputs to add to this feed. You must specify at least one output. You can later use the UpdateFeed action to change the list of outputs.
    ///   - logger: Logger use during operation
    @inlinable
    public func associateFeed(
        associatedResourceName: String = AssociateFeedRequest.idempotencyToken(),
        dryRun: Bool? = nil,
        id: String,
        outputs: [CreateOutput],
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws -> AssociateFeedResponse {
        let input = AssociateFeedRequest(
            associatedResourceName: associatedResourceName, 
            dryRun: dryRun, 
            id: id, 
            outputs: outputs
        )
        return try await self.associateFeed(input, logger: logger)
    }

    /// Creates a feed. The feed is the target for live streams being sent by the calling application. An example of a calling application is AWS Elemental MediaLive. After you create the feed, you can associate a resource with the feed.
    @Sendable
    @inlinable
    public func createFeed(_ input: CreateFeedRequest, logger: Logger = AWSClient.loggingDisabled) async throws -> CreateFeedResponse {
        try await self.client.execute(
            operation: "CreateFeed", 
            path: "/v1/feed", 
            httpMethod: .POST, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    /// Creates a feed. The feed is the target for live streams being sent by the calling application. An example of a calling application is AWS Elemental MediaLive. After you create the feed, you can associate a resource with the feed.
    ///
    /// Parameters:
    ///   - name: A name for this feed.
    ///   - outputs: An array of outputs for this feed. Each output represents a specific Elemental Inference feature. For example, an output might represent the crop feature.
    ///   - tags: If you want to include tags, add them now. You won't be able to add them later.
    ///   - logger: Logger use during operation
    @inlinable
    public func createFeed(
        name: String,
        outputs: [CreateOutput],
        tags: [String: String]? = nil,
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws -> CreateFeedResponse {
        let input = CreateFeedRequest(
            name: name, 
            outputs: outputs, 
            tags: tags
        )
        return try await self.createFeed(input, logger: logger)
    }

    /// Deletes the specified feed. The feed can be deleted at any time.
    @Sendable
    @inlinable
    public func deleteFeed(_ input: DeleteFeedRequest, logger: Logger = AWSClient.loggingDisabled) async throws -> DeleteFeedResponse {
        try await self.client.execute(
            operation: "DeleteFeed", 
            path: "/v1/feed/{id}", 
            httpMethod: .DELETE, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    /// Deletes the specified feed. The feed can be deleted at any time.
    ///
    /// Parameters:
    ///   - id: The ID of the feed.
    ///   - logger: Logger use during operation
    @inlinable
    public func deleteFeed(
        id: String,
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws -> DeleteFeedResponse {
        let input = DeleteFeedRequest(
            id: id
        )
        return try await self.deleteFeed(input, logger: logger)
    }

    /// Releases the resource (for example, an MediaLive channel) that is associated with this feed. The outputs in the feed become disabled.
    @Sendable
    @inlinable
    public func disassociateFeed(_ input: DisassociateFeedRequest, logger: Logger = AWSClient.loggingDisabled) async throws -> DisassociateFeedResponse {
        try await self.client.execute(
            operation: "DisassociateFeed", 
            path: "/v1/feed/{id}/disassociate", 
            httpMethod: .POST, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    /// Releases the resource (for example, an MediaLive channel) that is associated with this feed. The outputs in the feed become disabled.
    ///
    /// Parameters:
    ///   - associatedResourceName: The name of the resource currently associated with the feed'.
    ///   - dryRun: Set to true if you want to do a dry run of the disassociate action.
    ///   - id: The ID of the feed where you want to release the resource.
    ///   - logger: Logger use during operation
    @inlinable
    public func disassociateFeed(
        associatedResourceName: String = DisassociateFeedRequest.idempotencyToken(),
        dryRun: Bool? = nil,
        id: String,
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws -> DisassociateFeedResponse {
        let input = DisassociateFeedRequest(
            associatedResourceName: associatedResourceName, 
            dryRun: dryRun, 
            id: id
        )
        return try await self.disassociateFeed(input, logger: logger)
    }

    /// Retrieves information about the specified feed.
    @Sendable
    @inlinable
    public func getFeed(_ input: GetFeedRequest, logger: Logger = AWSClient.loggingDisabled) async throws -> GetFeedResponse {
        try await self.client.execute(
            operation: "GetFeed", 
            path: "/v1/feed/{id}", 
            httpMethod: .GET, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    /// Retrieves information about the specified feed.
    ///
    /// Parameters:
    ///   - id: The ID of the feed to query.
    ///   - logger: Logger use during operation
    @inlinable
    public func getFeed(
        id: String,
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws -> GetFeedResponse {
        let input = GetFeedRequest(
            id: id
        )
        return try await self.getFeed(input, logger: logger)
    }

    /// Displays a list of feeds that belong to this AWS account.
    @Sendable
    @inlinable
    public func listFeeds(_ input: ListFeedsRequest, logger: Logger = AWSClient.loggingDisabled) async throws -> ListFeedsResponse {
        try await self.client.execute(
            operation: "ListFeeds", 
            path: "/v1/feeds", 
            httpMethod: .GET, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    /// Displays a list of feeds that belong to this AWS account.
    ///
    /// Parameters:
    ///   - maxResults: The maximum number of results to return per API request. For example, you submit a list request with MaxResults set at 5. Although 20 items match your request, the service returns no more than the first 5 items. (The service also returns a NextToken value that you can use to fetch the next batch of results.) The service might return fewer results than the MaxResults value. If MaxResults is not included in the request, the service defaults to pagination with a maximum of 10 results per page. Valid Range: Minimum value of 1. Maximum value of 1000.
    ///   - nextToken: The token that identifies the batch of results that you want to see. For example, you submit a ListBridges request with MaxResults set at 5. The service returns the first batch of results (up to 5) and a NextToken value. To see the next batch of results, you can submit the ListBridges request a second time and specify the NextToken value.
    ///   - logger: Logger use during operation
    @inlinable
    public func listFeeds(
        maxResults: Int? = nil,
        nextToken: String? = nil,
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws -> ListFeedsResponse {
        let input = ListFeedsRequest(
            maxResults: maxResults, 
            nextToken: nextToken
        )
        return try await self.listFeeds(input, logger: logger)
    }

    /// List all tags that are on an Elemental Inference resource in the current region.
    @Sendable
    @inlinable
    public func listTagsForResource(_ input: ListTagsForResourceRequest, logger: Logger = AWSClient.loggingDisabled) async throws -> ListTagsForResourceResponse {
        try await self.client.execute(
            operation: "ListTagsForResource", 
            path: "/v1/tags/{resourceArn}", 
            httpMethod: .GET, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    /// List all tags that are on an Elemental Inference resource in the current region.
    ///
    /// Parameters:
    ///   - resourceArn: The ARN of the resource whose tags you want to query.
    ///   - logger: Logger use during operation
    @inlinable
    public func listTagsForResource(
        resourceArn: String,
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws -> ListTagsForResourceResponse {
        let input = ListTagsForResourceRequest(
            resourceArn: resourceArn
        )
        return try await self.listTagsForResource(input, logger: logger)
    }

    /// Associates the specified tags to the resource identified by the specified resourceArn in the current region. If existing tags on a resource are not specified in the request parameters, they are not changed. When a resource is deleted, the tags associated with that resource are also deleted.
    @Sendable
    @inlinable
    public func tagResource(_ input: TagResourceRequest, logger: Logger = AWSClient.loggingDisabled) async throws {
        try await self.client.execute(
            operation: "TagResource", 
            path: "/v1/tags/{resourceArn}", 
            httpMethod: .POST, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    /// Associates the specified tags to the resource identified by the specified resourceArn in the current region. If existing tags on a resource are not specified in the request parameters, they are not changed. When a resource is deleted, the tags associated with that resource are also deleted.
    ///
    /// Parameters:
    ///   - resourceArn: The ARN of the resource where you want to add tags.
    ///   - tags: A list of tags to add to the resource.
    ///   - logger: Logger use during operation
    @inlinable
    public func tagResource(
        resourceArn: String,
        tags: [String: String],
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws {
        let input = TagResourceRequest(
            resourceArn: resourceArn, 
            tags: tags
        )
        return try await self.tagResource(input, logger: logger)
    }

    /// Deletes specified tags from the specified resource in the current region.
    @Sendable
    @inlinable
    public func untagResource(_ input: UntagResourceRequest, logger: Logger = AWSClient.loggingDisabled) async throws {
        try await self.client.execute(
            operation: "UntagResource", 
            path: "/v1/tags/{resourceArn}", 
            httpMethod: .DELETE, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    /// Deletes specified tags from the specified resource in the current region.
    ///
    /// Parameters:
    ///   - resourceArn: The ARN of the resource where you want to delete one or more tags.
    ///   - tagKeys: The keys of the tags to delete.
    ///   - logger: Logger use during operation
    @inlinable
    public func untagResource(
        resourceArn: String,
        tagKeys: [String],
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws {
        let input = UntagResourceRequest(
            resourceArn: resourceArn, 
            tagKeys: tagKeys
        )
        return try await self.untagResource(input, logger: logger)
    }

    /// Updates the name and/or outputs in a feed.
    @Sendable
    @inlinable
    public func updateFeed(_ input: UpdateFeedRequest, logger: Logger = AWSClient.loggingDisabled) async throws -> UpdateFeedResponse {
        try await self.client.execute(
            operation: "UpdateFeed", 
            path: "/v1/feed/{id}", 
            httpMethod: .PUT, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    /// Updates the name and/or outputs in a feed.
    ///
    /// Parameters:
    ///   - id: The ID of the feed to update.
    ///   - name: Required. You can specify the existing name (to leave it unchanged) or a new name.
    ///   - outputs: Required. You can specify the existing array of outputs (to leave outputs unchanged) or you can specify a new array.
    ///   - logger: Logger use during operation
    @inlinable
    public func updateFeed(
        id: String,
        name: String,
        outputs: [UpdateOutput],
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws -> UpdateFeedResponse {
        let input = UpdateFeedRequest(
            id: id, 
            name: name, 
            outputs: outputs
        )
        return try await self.updateFeed(input, logger: logger)
    }
}

extension ElementalInference {
    /// Initializer required by `AWSService.with(middlewares:timeout:byteBufferAllocator:options)`. You are not able to use this initializer directly as there are not public
    /// initializers for `AWSServiceConfig.Patch`. Please use `AWSService.with(middlewares:timeout:byteBufferAllocator:options)` instead.
    public init(from: ElementalInference, patch: AWSServiceConfig.Patch) {
        self.client = from.client
        self.config = from.config.with(patch: patch)
    }
}

// MARK: Paginators

@available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
extension ElementalInference {
    /// Return PaginatorSequence for operation ``listFeeds(_:logger:)``.
    ///
    /// - Parameters:
    ///   - input: Input for operation
    ///   - logger: Logger used for logging
    @inlinable
    public func listFeedsPaginator(
        _ input: ListFeedsRequest,
        logger: Logger = AWSClient.loggingDisabled
    ) -> AWSClient.PaginatorSequence<ListFeedsRequest, ListFeedsResponse> {
        return .init(
            input: input,
            command: self.listFeeds,
            inputKey: \ListFeedsRequest.nextToken,
            outputKey: \ListFeedsResponse.nextToken,
            logger: logger
        )
    }
    /// Return PaginatorSequence for operation ``listFeeds(_:logger:)``.
    ///
    /// - Parameters:
    ///   - maxResults: The maximum number of results to return per API request. For example, you submit a list request with MaxResults set at 5. Although 20 items match your request, the service returns no more than the first 5 items. (The service also returns a NextToken value that you can use to fetch the next batch of results.) The service might return fewer results than the MaxResults value. If MaxResults is not included in the request, the service defaults to pagination with a maximum of 10 results per page. Valid Range: Minimum value of 1. Maximum value of 1000.
    ///   - logger: Logger used for logging
    @inlinable
    public func listFeedsPaginator(
        maxResults: Int? = nil,
        logger: Logger = AWSClient.loggingDisabled        
    ) -> AWSClient.PaginatorSequence<ListFeedsRequest, ListFeedsResponse> {
        let input = ListFeedsRequest(
            maxResults: maxResults
        )
        return self.listFeedsPaginator(input, logger: logger)
    }
}

extension ElementalInference.ListFeedsRequest: AWSPaginateToken {
    @inlinable
    public func usingPaginationToken(_ token: String) -> ElementalInference.ListFeedsRequest {
        return .init(
            maxResults: self.maxResults,
            nextToken: token
        )
    }
}

// MARK: Waiters

@available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
extension ElementalInference {
    /// Waiter for operation ``getFeed(_:logger:)``.
    ///
    /// - Parameters:
    ///   - input: Input for operation
    ///   - logger: Logger used for logging
    @inlinable
    public func waitUntilFeedDeleted(
        _ input: GetFeedRequest,
        maxWaitTime: TimeAmount? = nil,
        logger: Logger = AWSClient.loggingDisabled
    ) async throws {
        let waiter = AWSClient.Waiter<GetFeedRequest, _>(
            acceptors: [
                .init(state: .success, matcher: AWSErrorCodeMatcher("ResourceNotFoundException")),
                .init(state: .success, matcher: try! JMESPathMatcher("status", expected: "DELETED")),
                .init(state: .retry, matcher: try! JMESPathMatcher("status", expected: "DELETING")),
                .init(state: .retry, matcher: AWSErrorCodeMatcher("InternalServerErrorException")),
                .init(state: .retry, matcher: AWSErrorCodeMatcher("TooManyRequestException")),
            ],
            minDelayTime: .seconds(3),
            command: self.getFeed
        )
        return try await self.client.waitUntil(input, waiter: waiter, maxWaitTime: maxWaitTime, logger: logger)
    }
    /// Waiter for operation ``getFeed(_:logger:)``.
    ///
    /// - Parameters:
    ///   - id: The ID of the feed to query.
    ///   - logger: Logger used for logging
    @inlinable
    public func waitUntilFeedDeleted(
        id: String,
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws {
        let input = GetFeedRequest(
            id: id
        )
        try await self.waitUntilFeedDeleted(input, logger: logger)
    }
}
