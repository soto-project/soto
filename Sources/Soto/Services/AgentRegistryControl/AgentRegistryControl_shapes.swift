//===----------------------------------------------------------------------===//
//
// This source file is part of the Soto for AWS open source project
//
// Copyright (c) 2017-2024 the Soto project authors
// Licensed under Apache License v2.0
//
// See LICENSE.txt for license information
// See CONTRIBUTORS.txt for the list of Soto project authors
//
// SPDX-License-Identifier: Apache-2.0
//
//===----------------------------------------------------------------------===//

// THIS FILE IS AUTOMATICALLY GENERATED by https://github.com/soto-project/soto-codegenerator.
// DO NOT EDIT.

#if canImport(FoundationEssentials)
import FoundationEssentials
#else
import Foundation
#endif
@_spi(SotoInternal) import SotoCore

extension AgentRegistryControl {
    // MARK: Enums

    public enum AgentCoreGatewayProtocolType: String, CustomStringConvertible, Codable, Sendable, CodingKeyRepresentable {
        case mcp = "MCP"
        public var description: String { return self.rawValue }
    }

    public enum AgentCoreRuntimeServerProtocol: String, CustomStringConvertible, Codable, Sendable, CodingKeyRepresentable {
        case a2a = "A2A"
        case agui = "AGUI"
        case http = "HTTP"
        case mcp = "MCP"
        public var description: String { return self.rawValue }
    }

    public enum AutoApprovalRule: String, CustomStringConvertible, Codable, Sendable, CodingKeyRepresentable {
        case approveAll = "APPROVE_ALL"
        public var description: String { return self.rawValue }
    }

    public enum AutoDetectionScope: String, CustomStringConvertible, Codable, Sendable, CodingKeyRepresentable {
        case organization = "ORGANIZATION"
        public var description: String { return self.rawValue }
    }

    public enum AutoDetectionStatus: String, CustomStringConvertible, Codable, Sendable, CodingKeyRepresentable {
        case active = "ACTIVE"
        case inactive = "INACTIVE"
        public var description: String { return self.rawValue }
    }

    public enum ClaimMatchOperatorType: String, CustomStringConvertible, Codable, Sendable, CodingKeyRepresentable {
        case contains = "CONTAINS"
        case containsAny = "CONTAINS_ANY"
        case equals = "EQUALS"
        public var description: String { return self.rawValue }
    }

    public enum EndpointIpAddressType: String, CustomStringConvertible, Codable, Sendable, CodingKeyRepresentable {
        case ipv4 = "IPV4"
        case ipv6 = "IPV6"
        public var description: String { return self.rawValue }
    }

    public enum InboundTokenClaimValueType: String, CustomStringConvertible, Codable, Sendable, CodingKeyRepresentable {
        case string = "STRING"
        case stringArray = "STRING_ARRAY"
        public var description: String { return self.rawValue }
    }

    public enum ProvenanceRelation: String, CustomStringConvertible, Codable, Sendable, CodingKeyRepresentable {
        case detectedFrom = "DETECTED_FROM"
        public var description: String { return self.rawValue }
    }

    public enum RecordType: String, CustomStringConvertible, Codable, Sendable, CodingKeyRepresentable {
        case agent = "AGENT"
        case custom = "CUSTOM"
        case gateway = "GATEWAY"
        case mcp = "MCP"
        case skill = "SKILL"
        public var description: String { return self.rawValue }
    }

    public enum RegistryAuthorizerType: String, CustomStringConvertible, Codable, Sendable, CodingKeyRepresentable {
        case awsIam = "AWS_IAM"
        case customJwt = "CUSTOM_JWT"
        public var description: String { return self.rawValue }
    }

    public enum RegistryFilterName: String, CustomStringConvertible, Codable, Sendable, CodingKeyRepresentable {
        case discoveryConfigurationAuthorizerType = "discoveryConfiguration.authorizerType"
        case status = "status"
        public var description: String { return self.rawValue }
    }

    public enum RegistryRecordCredentialProviderType: String, CustomStringConvertible, Codable, Sendable, CodingKeyRepresentable {
        case iam = "IAM"
        case oauth = "OAUTH"
        public var description: String { return self.rawValue }
    }

    public enum RegistryRecordFilterName: String, CustomStringConvertible, Codable, Sendable, CodingKeyRepresentable {
        case name = "name"
        case recordType = "recordType"
        case status = "status"
        public var description: String { return self.rawValue }
    }

    public enum RegistryRecordOAuthGrantType: String, CustomStringConvertible, Codable, Sendable, CodingKeyRepresentable {
        case clientCredentials = "CLIENT_CREDENTIALS"
        public var description: String { return self.rawValue }
    }

    public enum RegistryRecordStatus: String, CustomStringConvertible, Codable, Sendable, CodingKeyRepresentable {
        case approved = "APPROVED"
        case createFailed = "CREATE_FAILED"
        case creating = "CREATING"
        case deprecated = "DEPRECATED"
        case draft = "DRAFT"
        case pendingApproval = "PENDING_APPROVAL"
        case rejected = "REJECTED"
        case updateFailed = "UPDATE_FAILED"
        case updating = "UPDATING"
        public var description: String { return self.rawValue }
    }

    public enum RegistryStatus: String, CustomStringConvertible, Codable, Sendable, CodingKeyRepresentable {
        case createFailed = "CREATE_FAILED"
        case creating = "CREATING"
        case deleteFailed = "DELETE_FAILED"
        case deleting = "DELETING"
        case ready = "READY"
        case updateFailed = "UPDATE_FAILED"
        case updating = "UPDATING"
        public var description: String { return self.rawValue }
    }

    public enum SourceType: String, CustomStringConvertible, Codable, Sendable, CodingKeyRepresentable {
        case awsBedrockAgentcoreGateway = "AWS::BedrockAgentCore::Gateway"
        case awsBedrockAgentcoreRuntime = "AWS::BedrockAgentCore::Runtime"
        public var description: String { return self.rawValue }
    }

    public enum ValidationExceptionReason: String, CustomStringConvertible, Codable, Sendable, CodingKeyRepresentable {
        case cannotParse = "CannotParse"
        case fieldValidationFailed = "FieldValidationFailed"
        case idempotentParameterMismatchException = "IdempotentParameterMismatchException"
        case resourceConflict = "ResourceConflict"
        case rootEventInOtherSession = "EventInOtherSession"
        public var description: String { return self.rawValue }
    }

    public enum ClaimMatchValueType: AWSEncodableShape & AWSDecodableShape, Sendable {
        /// A single string value to match the claim against.
        case matchValueString(String)
        /// A list of string values to match the claim against.
        case matchValueStringList([String])

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            guard container.allKeys.count == 1, let key = container.allKeys.first else {
                let context = DecodingError.Context(
                    codingPath: container.codingPath,
                    debugDescription: "Expected exactly one key, but got \(container.allKeys.count)"
                )
                throw DecodingError.dataCorrupted(context)
            }
            switch key {
            case .matchValueString:
                let value = try container.decode(String.self, forKey: .matchValueString)
                self = .matchValueString(value)
            case .matchValueStringList:
                let value = try container.decode([String].self, forKey: .matchValueStringList)
                self = .matchValueStringList(value)
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            switch self {
            case .matchValueString(let value):
                try container.encode(value, forKey: .matchValueString)
            case .matchValueStringList(let value):
                try container.encode(value, forKey: .matchValueStringList)
            }
        }

        public func validate(name: String) throws {
            switch self {
            case .matchValueString(let value):
                try self.validate(value, name: "matchValueString", parent: name, max: 255)
                try self.validate(value, name: "matchValueString", parent: name, min: 1)
                try self.validate(value, name: "matchValueString", parent: name, pattern: "^[A-Za-z0-9_.:/-]+$")
            case .matchValueStringList(let value):
                try value.forEach {
                    try validate($0, name: "matchValueStringList[]", parent: name, max: 255)
                    try validate($0, name: "matchValueStringList[]", parent: name, min: 1)
                    try validate($0, name: "matchValueStringList[]", parent: name, pattern: "^[A-Za-z0-9_.:/-]+$")
                }
                try self.validate(value, name: "matchValueStringList", parent: name, min: 1)
            }
        }

        private enum CodingKeys: String, CodingKey {
            case matchValueString = "matchValueString"
            case matchValueStringList = "matchValueStringList"
        }
    }

    public enum PrivateEndpoint: AWSEncodableShape & AWSDecodableShape, Sendable {
        /// A private endpoint backed by a service-managed VPC resource.
        case managedVpcResource(ManagedVpcResource)
        /// A private endpoint backed by a self-managed VPC Lattice resource configuration.
        case selfManagedLatticeResource(SelfManagedLatticeResource)

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            guard container.allKeys.count == 1, let key = container.allKeys.first else {
                let context = DecodingError.Context(
                    codingPath: container.codingPath,
                    debugDescription: "Expected exactly one key, but got \(container.allKeys.count)"
                )
                throw DecodingError.dataCorrupted(context)
            }
            switch key {
            case .managedVpcResource:
                let value = try container.decode(ManagedVpcResource.self, forKey: .managedVpcResource)
                self = .managedVpcResource(value)
            case .selfManagedLatticeResource:
                let value = try container.decode(SelfManagedLatticeResource.self, forKey: .selfManagedLatticeResource)
                self = .selfManagedLatticeResource(value)
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            switch self {
            case .managedVpcResource(let value):
                try container.encode(value, forKey: .managedVpcResource)
            case .selfManagedLatticeResource(let value):
                try container.encode(value, forKey: .selfManagedLatticeResource)
            }
        }

        public func validate(name: String) throws {
            switch self {
            case .managedVpcResource(let value):
                try value.validate(name: "\(name).managedVpcResource")
            case .selfManagedLatticeResource(let value):
                try value.validate(name: "\(name).selfManagedLatticeResource")
            }
        }

        private enum CodingKeys: String, CodingKey {
            case managedVpcResource = "managedVpcResource"
            case selfManagedLatticeResource = "selfManagedLatticeResource"
        }
    }

    public enum RegistryRecordCredentialProviderUnion: AWSEncodableShape & AWSDecodableShape, Sendable {
        /// The IAM role credential provider details.
        case iamCredentialProvider(RegistryRecordIamCredentialProvider)
        /// The OAuth 2.0 credential provider details.
        case oauthCredentialProvider(RegistryRecordOAuthCredentialProvider)

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            guard container.allKeys.count == 1, let key = container.allKeys.first else {
                let context = DecodingError.Context(
                    codingPath: container.codingPath,
                    debugDescription: "Expected exactly one key, but got \(container.allKeys.count)"
                )
                throw DecodingError.dataCorrupted(context)
            }
            switch key {
            case .iamCredentialProvider:
                let value = try container.decode(RegistryRecordIamCredentialProvider.self, forKey: .iamCredentialProvider)
                self = .iamCredentialProvider(value)
            case .oauthCredentialProvider:
                let value = try container.decode(RegistryRecordOAuthCredentialProvider.self, forKey: .oauthCredentialProvider)
                self = .oauthCredentialProvider(value)
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            switch self {
            case .iamCredentialProvider(let value):
                try container.encode(value, forKey: .iamCredentialProvider)
            case .oauthCredentialProvider(let value):
                try container.encode(value, forKey: .oauthCredentialProvider)
            }
        }

        public func validate(name: String) throws {
            switch self {
            case .iamCredentialProvider(let value):
                try value.validate(name: "\(name).iamCredentialProvider")
            case .oauthCredentialProvider(let value):
                try value.validate(name: "\(name).oauthCredentialProvider")
            }
        }

        private enum CodingKeys: String, CodingKey {
            case iamCredentialProvider = "iamCredentialProvider"
            case oauthCredentialProvider = "oauthCredentialProvider"
        }
    }

    public enum SourceDetails: AWSEncodableShape & AWSDecodableShape, Sendable {
        case agentcoreGateway(AgentCoreGatewaySourceDetails)
        case agentcoreRuntime(AgentCoreRuntimeSourceDetails)

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            guard container.allKeys.count == 1, let key = container.allKeys.first else {
                let context = DecodingError.Context(
                    codingPath: container.codingPath,
                    debugDescription: "Expected exactly one key, but got \(container.allKeys.count)"
                )
                throw DecodingError.dataCorrupted(context)
            }
            switch key {
            case .agentcoreGateway:
                let value = try container.decode(AgentCoreGatewaySourceDetails.self, forKey: .agentcoreGateway)
                self = .agentcoreGateway(value)
            case .agentcoreRuntime:
                let value = try container.decode(AgentCoreRuntimeSourceDetails.self, forKey: .agentcoreRuntime)
                self = .agentcoreRuntime(value)
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            switch self {
            case .agentcoreGateway(let value):
                try container.encode(value, forKey: .agentcoreGateway)
            case .agentcoreRuntime(let value):
                try container.encode(value, forKey: .agentcoreRuntime)
            }
        }

        public func validate(name: String) throws {
            switch self {
            case .agentcoreGateway(let value):
                try value.validate(name: "\(name).agentcoreGateway")
            case .agentcoreRuntime(let value):
                try value.validate(name: "\(name).agentcoreRuntime")
            }
        }

        private enum CodingKeys: String, CodingKey {
            case agentcoreGateway = "agentcoreGateway"
            case agentcoreRuntime = "agentcoreRuntime"
        }
    }

    // MARK: Shapes

    public struct A2aAgentCardDescriptor: AWSEncodableShape & AWSDecodableShape {
        /// The A2A agent card content, serialized as descriptor payload data.
        public let data: String?
        /// The schema version of the descriptor payload.
        public let dataSchemaVersion: String?
        /// The optional source configuration used to synchronize the A2A agent card descriptor content.
        public let source: DescriptorSource?

        @inlinable
        public init(data: String? = nil, dataSchemaVersion: String? = nil, source: DescriptorSource? = nil) {
            self.data = data
            self.dataSchemaVersion = dataSchemaVersion
            self.source = source
        }

        public func validate(name: String) throws {
            try self.validate(self.data, name: "data", parent: name, max: 102400)
            try self.validate(self.data, name: "data", parent: name, min: 1)
            try self.validate(self.dataSchemaVersion, name: "dataSchemaVersion", parent: name, max: 255)
            try self.validate(self.dataSchemaVersion, name: "dataSchemaVersion", parent: name, min: 1)
            try self.source?.validate(name: "\(name).source")
        }

        private enum CodingKeys: String, CodingKey {
            case data = "data"
            case dataSchemaVersion = "dataSchemaVersion"
            case source = "source"
        }
    }

    public struct AgUiDescriptor: AWSEncodableShape & AWSDecodableShape {
        public let source: DescriptorSource?

        @inlinable
        public init(source: DescriptorSource? = nil) {
            self.source = source
        }

        public func validate(name: String) throws {
            try self.source?.validate(name: "\(name).source")
        }

        private enum CodingKeys: String, CodingKey {
            case source = "source"
        }
    }

    public struct AgentCoreGatewaySourceDetails: AWSEncodableShape & AWSDecodableShape {
        public let authorizerConfiguration: AuthorizerConfiguration?
        /// The type of authorizer configured on the AgentCore Gateway resource that the registry record was detected from.
        public let authorizerType: String?
        public let protocolType: AgentCoreGatewayProtocolType?
        public let workloadIdentityDetails: WorkloadIdentityDetails?

        @inlinable
        public init(authorizerConfiguration: AuthorizerConfiguration? = nil, authorizerType: String? = nil, protocolType: AgentCoreGatewayProtocolType? = nil, workloadIdentityDetails: WorkloadIdentityDetails? = nil) {
            self.authorizerConfiguration = authorizerConfiguration
            self.authorizerType = authorizerType
            self.protocolType = protocolType
            self.workloadIdentityDetails = workloadIdentityDetails
        }

        public func validate(name: String) throws {
            try self.authorizerConfiguration?.validate(name: "\(name).authorizerConfiguration")
        }

        private enum CodingKeys: String, CodingKey {
            case authorizerConfiguration = "authorizerConfiguration"
            case authorizerType = "authorizerType"
            case protocolType = "protocolType"
            case workloadIdentityDetails = "workloadIdentityDetails"
        }
    }

    public struct AgentCoreRuntimeProtocolConfiguration: AWSEncodableShape & AWSDecodableShape {
        public let serverProtocol: AgentCoreRuntimeServerProtocol?

        @inlinable
        public init(serverProtocol: AgentCoreRuntimeServerProtocol? = nil) {
            self.serverProtocol = serverProtocol
        }

        private enum CodingKeys: String, CodingKey {
            case serverProtocol = "serverProtocol"
        }
    }

    public struct AgentCoreRuntimeSourceDetails: AWSEncodableShape & AWSDecodableShape {
        public let authorizerConfiguration: AuthorizerConfiguration?
        public let protocolConfiguration: AgentCoreRuntimeProtocolConfiguration?
        public let workloadIdentityDetails: WorkloadIdentityDetails?

        @inlinable
        public init(authorizerConfiguration: AuthorizerConfiguration? = nil, protocolConfiguration: AgentCoreRuntimeProtocolConfiguration? = nil, workloadIdentityDetails: WorkloadIdentityDetails? = nil) {
            self.authorizerConfiguration = authorizerConfiguration
            self.protocolConfiguration = protocolConfiguration
            self.workloadIdentityDetails = workloadIdentityDetails
        }

        public func validate(name: String) throws {
            try self.authorizerConfiguration?.validate(name: "\(name).authorizerConfiguration")
        }

        private enum CodingKeys: String, CodingKey {
            case authorizerConfiguration = "authorizerConfiguration"
            case protocolConfiguration = "protocolConfiguration"
            case workloadIdentityDetails = "workloadIdentityDetails"
        }
    }

    public struct AgentSkillsAdditionalData: AWSEncodableShape & AWSDecodableShape {
        /// The markdown skill content associated with an agent skills definition.
        public let skillMd: AgentSkillsMdDescriptor?

        @inlinable
        public init(skillMd: AgentSkillsMdDescriptor? = nil) {
            self.skillMd = skillMd
        }

        public func validate(name: String) throws {
            try self.skillMd?.validate(name: "\(name).skillMd")
        }

        private enum CodingKeys: String, CodingKey {
            case skillMd = "skillMd"
        }
    }

    public struct AgentSkillsDefinitionDescriptor: AWSEncodableShape & AWSDecodableShape {
        /// Additional data associated with the agent skills definition descriptor.
        public let additionalData: AgentSkillsAdditionalData?
        /// The agent skills definition content, serialized as descriptor payload data.
        public let data: String?
        /// The schema version of the descriptor payload.
        public let dataSchemaVersion: String?

        @inlinable
        public init(additionalData: AgentSkillsAdditionalData? = nil, data: String? = nil, dataSchemaVersion: String? = nil) {
            self.additionalData = additionalData
            self.data = data
            self.dataSchemaVersion = dataSchemaVersion
        }

        public func validate(name: String) throws {
            try self.additionalData?.validate(name: "\(name).additionalData")
            try self.validate(self.data, name: "data", parent: name, max: 102400)
            try self.validate(self.data, name: "data", parent: name, min: 1)
            try self.validate(self.dataSchemaVersion, name: "dataSchemaVersion", parent: name, max: 255)
            try self.validate(self.dataSchemaVersion, name: "dataSchemaVersion", parent: name, min: 1)
        }

        private enum CodingKeys: String, CodingKey {
            case additionalData = "additionalData"
            case data = "data"
            case dataSchemaVersion = "dataSchemaVersion"
        }
    }

    public struct AgentSkillsMdDescriptor: AWSEncodableShape & AWSDecodableShape {
        /// The agent skills markdown content, serialized as descriptor payload data.
        public let data: String?
        /// The schema version of the descriptor payload.
        public let dataSchemaVersion: String?
        /// The optional source configuration used to synchronize the agent skills markdown content.
        public let source: DescriptorSource?

        @inlinable
        public init(data: String? = nil, dataSchemaVersion: String? = nil, source: DescriptorSource? = nil) {
            self.data = data
            self.dataSchemaVersion = dataSchemaVersion
            self.source = source
        }

        public func validate(name: String) throws {
            try self.validate(self.data, name: "data", parent: name, max: 102400)
            try self.validate(self.data, name: "data", parent: name, min: 1)
            try self.validate(self.dataSchemaVersion, name: "dataSchemaVersion", parent: name, max: 255)
            try self.validate(self.dataSchemaVersion, name: "dataSchemaVersion", parent: name, min: 1)
            try self.source?.validate(name: "\(name).source")
        }

        private enum CodingKeys: String, CodingKey {
            case data = "data"
            case dataSchemaVersion = "dataSchemaVersion"
            case source = "source"
        }
    }

    public struct ApprovalConfiguration: AWSEncodableShape & AWSDecodableShape {
        /// The rules that determine which registry records are automatically approved on submission. When omitted or empty, submitted records require manual review.
        public let autoApprovalRules: [AutoApprovalRule]?

        @inlinable
        public init(autoApprovalRules: [AutoApprovalRule]? = nil) {
            self.autoApprovalRules = autoApprovalRules
        }

        public func validate(name: String) throws {
            try self.validate(self.autoApprovalRules, name: "autoApprovalRules", parent: name, max: 10)
        }

        private enum CodingKeys: String, CodingKey {
            case autoApprovalRules = "autoApprovalRules"
        }
    }

    public struct AuthorizingClaimMatchValueType: AWSEncodableShape & AWSDecodableShape {
        /// The operator used to compare the claim value against the expected value.
        public let claimMatchOperator: ClaimMatchOperatorType
        /// The expected value or values that the claim is compared against.
        public let claimMatchValue: ClaimMatchValueType

        @inlinable
        public init(claimMatchOperator: ClaimMatchOperatorType, claimMatchValue: ClaimMatchValueType) {
            self.claimMatchOperator = claimMatchOperator
            self.claimMatchValue = claimMatchValue
        }

        public func validate(name: String) throws {
            try self.claimMatchValue.validate(name: "\(name).claimMatchValue")
        }

        private enum CodingKeys: String, CodingKey {
            case claimMatchOperator = "claimMatchOperator"
            case claimMatchValue = "claimMatchValue"
        }
    }

    public struct AutoDetection: AWSDecodableShape {
        /// The auto-detection settings that control how resources are discovered for the registry.
        public let configuration: AutoDetectionConfiguration
        /// The current auto-detection status. ACTIVE indicates that the registry is actively being populated with detected resources. INACTIVE indicates that the preconditions required at the configured scope are not currently met.
        public let status: AutoDetectionStatus
        /// A human-readable explanation of the current auto-detection status. Typically populated when the status requires additional context.
        public let statusReason: String?

        @inlinable
        public init(configuration: AutoDetectionConfiguration, status: AutoDetectionStatus, statusReason: String? = nil) {
            self.configuration = configuration
            self.status = status
            self.statusReason = statusReason
        }

        private enum CodingKeys: String, CodingKey {
            case configuration = "configuration"
            case status = "status"
            case statusReason = "statusReason"
        }
    }

    public struct AutoDetectionConfiguration: AWSEncodableShape & AWSDecodableShape {
        /// Specifies whether auto-detection is requested for the registry. Setting this to true is necessary but not sufficient for auto-detection to become active; the preconditions of the configured scope must also be met.
        public let enabled: Bool
        /// The source from which resources are detected. For example, ORGANIZATION sources resources from all member accounts of an Amazon Web Services organization.
        public let scope: AutoDetectionScope

        @inlinable
        public init(enabled: Bool, scope: AutoDetectionScope) {
            self.enabled = enabled
            self.scope = scope
        }

        private enum CodingKeys: String, CodingKey {
            case enabled = "enabled"
            case scope = "scope"
        }
    }

    public struct CreateRegistryRecordRequest: AWSEncodableShape {
        /// Client token for idempotency
        public let clientToken: String?
        /// The description of the registry record
        public let description: String?
        /// The typed descriptor content for the registry record
        public let descriptors: Descriptors
        /// The human-readable display name of the registry record
        public let displayName: String?
        /// The name of the registry record
        public let name: String
        public let provenance: [Provenance]?
        /// The type of the registry record, which determines the descriptor format
        public let recordType: RecordType
        /// The version of the registry record
        public let recordVersion: String?
        /// The identifier of the registry in which to create the record (ARN or ID)
        public let registryId: String
        /// Tags to associate with the registry record
        public let tags: [String: String]?

        @inlinable
        public init(clientToken: String? = CreateRegistryRecordRequest.idempotencyToken(), description: String? = nil, descriptors: Descriptors, displayName: String? = nil, name: String, provenance: [Provenance]? = nil, recordType: RecordType, recordVersion: String? = nil, registryId: String, tags: [String: String]? = nil) {
            self.clientToken = clientToken
            self.description = description
            self.descriptors = descriptors
            self.displayName = displayName
            self.name = name
            self.provenance = provenance
            self.recordType = recordType
            self.recordVersion = recordVersion
            self.registryId = registryId
            self.tags = tags
        }

        public func encode(to encoder: Encoder) throws {
            let request = encoder.userInfo[.awsRequest]! as! RequestEncodingContainer
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encodeIfPresent(self.clientToken, forKey: .clientToken)
            try container.encodeIfPresent(self.description, forKey: .description)
            try container.encode(self.descriptors, forKey: .descriptors)
            try container.encodeIfPresent(self.displayName, forKey: .displayName)
            try container.encode(self.name, forKey: .name)
            try container.encodeIfPresent(self.provenance, forKey: .provenance)
            try container.encode(self.recordType, forKey: .recordType)
            try container.encodeIfPresent(self.recordVersion, forKey: .recordVersion)
            request.encodePath(self.registryId, key: "registryId")
            try container.encodeIfPresent(self.tags, forKey: .tags)
        }

        public func validate(name: String) throws {
            try self.validate(self.clientToken, name: "clientToken", parent: name, max: 256)
            try self.validate(self.clientToken, name: "clientToken", parent: name, min: 33)
            try self.validate(self.clientToken, name: "clientToken", parent: name, pattern: "^[a-zA-Z0-9](-*[a-zA-Z0-9]){0,256}$")
            try self.validate(self.description, name: "description", parent: name, max: 4096)
            try self.validate(self.description, name: "description", parent: name, min: 1)
            try self.descriptors.validate(name: "\(name).descriptors")
            try self.validate(self.displayName, name: "displayName", parent: name, max: 255)
            try self.validate(self.displayName, name: "displayName", parent: name, min: 1)
            try self.validate(self.name, name: "name", parent: name, max: 255)
            try self.validate(self.name, name: "name", parent: name, min: 1)
            try self.validate(self.name, name: "name", parent: name, pattern: "^[a-zA-Z0-9][a-zA-Z0-9_\\-\\.\\/]*$")
            try self.provenance?.forEach {
                try $0.validate(name: "\(name).provenance[]")
            }
            try self.validate(self.provenance, name: "provenance", parent: name, max: 1)
            try self.validate(self.recordVersion, name: "recordVersion", parent: name, max: 255)
            try self.validate(self.recordVersion, name: "recordVersion", parent: name, min: 1)
            try self.validate(self.recordVersion, name: "recordVersion", parent: name, pattern: "^[a-zA-Z0-9.-]+$")
            try self.validate(self.registryId, name: "registryId", parent: name, max: 2048)
            try self.validate(self.registryId, name: "registryId", parent: name, min: 1)
            try self.validate(self.registryId, name: "registryId", parent: name, pattern: "^(arn:aws(-[^:]+)?:agent-registry:[a-z0-9-]+:[0-9]{12}:registry/)?[a-zA-Z0-9]{12,16}$")
            try self.tags?.forEach {
                try validate($0.key, name: "tags.key", parent: name, max: 128)
                try validate($0.key, name: "tags.key", parent: name, min: 1)
                try validate($0.key, name: "tags.key", parent: name, pattern: "^[a-zA-Z0-9\\s._:/=+@-]*$")
                try validate($0.value, name: "tags[\"\($0.key)\"]", parent: name, max: 256)
                try validate($0.value, name: "tags[\"\($0.key)\"]", parent: name, pattern: "^[a-zA-Z0-9\\s._:/=+@-]*$")
            }
            try self.validate(self.tags, name: "tags", parent: name, max: 50)
            try self.validate(self.tags, name: "tags", parent: name, min: 1)
        }

        private enum CodingKeys: String, CodingKey {
            case clientToken = "clientToken"
            case description = "description"
            case descriptors = "descriptors"
            case displayName = "displayName"
            case name = "name"
            case provenance = "provenance"
            case recordType = "recordType"
            case recordVersion = "recordVersion"
            case tags = "tags"
        }
    }

    public struct CreateRegistryRecordResponse: AWSDecodableShape {
        /// The ARN of the created registry record
        public let recordArn: String
        /// The status of the registry record, set to CREATING while the asynchronous workflow is in progress
        public let status: RegistryRecordStatus

        @inlinable
        public init(recordArn: String, status: RegistryRecordStatus) {
            self.recordArn = recordArn
            self.status = status
        }

        private enum CodingKeys: String, CodingKey {
            case recordArn = "recordArn"
            case status = "status"
        }
    }

    public struct CreateRegistryRequest: AWSEncodableShape {
        /// Approval configuration for registry records
        public let approvalConfiguration: ApprovalConfiguration?
        /// The optional auto-detection configuration for the registry. When provided, the registry is automatically populated with resources discovered according to the configuration. Omit this field for registries whose records are managed exclusively through the Agent Registry Control API.
        public let autoDetectionConfiguration: AutoDetectionConfiguration?
        /// A unique, case-sensitive identifier to ensure that the operation completes no more than one time. If this token matches a previous request, the service ignores the request, but does not return an error.
        public let clientToken: String?
        /// The description of the registry
        public let description: String?
        /// Discovery configuration for the registry
        public let discoveryConfiguration: DiscoveryConfiguration?
        /// The optional server-side encryption configuration for the registry. When you provide this field, the specified customer-managed Amazon Web Services KMS key encrypts the registry's content. Omit this field to use an Amazon Web Services-owned encryption key. You cannot change the encryption configuration after registry creation.
        public let encryptionConfiguration: EncryptionConfiguration?
        /// The name of the registry
        public let name: String
        /// Tags to associate with the registry
        public let tags: [String: String]?

        @inlinable
        public init(approvalConfiguration: ApprovalConfiguration? = nil, autoDetectionConfiguration: AutoDetectionConfiguration? = nil, clientToken: String? = CreateRegistryRequest.idempotencyToken(), description: String? = nil, discoveryConfiguration: DiscoveryConfiguration? = nil, encryptionConfiguration: EncryptionConfiguration? = nil, name: String, tags: [String: String]? = nil) {
            self.approvalConfiguration = approvalConfiguration
            self.autoDetectionConfiguration = autoDetectionConfiguration
            self.clientToken = clientToken
            self.description = description
            self.discoveryConfiguration = discoveryConfiguration
            self.encryptionConfiguration = encryptionConfiguration
            self.name = name
            self.tags = tags
        }

        public func validate(name: String) throws {
            try self.approvalConfiguration?.validate(name: "\(name).approvalConfiguration")
            try self.validate(self.clientToken, name: "clientToken", parent: name, max: 256)
            try self.validate(self.clientToken, name: "clientToken", parent: name, min: 33)
            try self.validate(self.clientToken, name: "clientToken", parent: name, pattern: "^[a-zA-Z0-9](-*[a-zA-Z0-9]){0,256}$")
            try self.validate(self.description, name: "description", parent: name, max: 4096)
            try self.validate(self.description, name: "description", parent: name, min: 1)
            try self.discoveryConfiguration?.validate(name: "\(name).discoveryConfiguration")
            try self.encryptionConfiguration?.validate(name: "\(name).encryptionConfiguration")
            try self.validate(self.name, name: "name", parent: name, max: 64)
            try self.validate(self.name, name: "name", parent: name, min: 1)
            try self.validate(self.name, name: "name", parent: name, pattern: "^[a-zA-Z0-9][a-zA-Z0-9_\\-\\.\\/]*$")
            try self.tags?.forEach {
                try validate($0.key, name: "tags.key", parent: name, max: 128)
                try validate($0.key, name: "tags.key", parent: name, min: 1)
                try validate($0.key, name: "tags.key", parent: name, pattern: "^[a-zA-Z0-9\\s._:/=+@-]*$")
                try validate($0.value, name: "tags[\"\($0.key)\"]", parent: name, max: 256)
                try validate($0.value, name: "tags[\"\($0.key)\"]", parent: name, pattern: "^[a-zA-Z0-9\\s._:/=+@-]*$")
            }
            try self.validate(self.tags, name: "tags", parent: name, max: 50)
            try self.validate(self.tags, name: "tags", parent: name, min: 1)
        }

        private enum CodingKeys: String, CodingKey {
            case approvalConfiguration = "approvalConfiguration"
            case autoDetectionConfiguration = "autoDetectionConfiguration"
            case clientToken = "clientToken"
            case description = "description"
            case discoveryConfiguration = "discoveryConfiguration"
            case encryptionConfiguration = "encryptionConfiguration"
            case name = "name"
            case tags = "tags"
        }
    }

    public struct CreateRegistryResponse: AWSDecodableShape {
        /// The ARN of the created registry
        public let registryArn: String

        @inlinable
        public init(registryArn: String) {
            self.registryArn = registryArn
        }

        private enum CodingKeys: String, CodingKey {
            case registryArn = "registryArn"
        }
    }

    public struct CustomClaimValidationType: AWSEncodableShape & AWSDecodableShape {
        /// The value and match operator used to authorize the claim.
        public let authorizingClaimMatchValue: AuthorizingClaimMatchValueType
        /// The name of the claim in the inbound token to validate.
        public let inboundTokenClaimName: String
        /// The value type of the claim in the inbound token, either a string or an array of strings.
        public let inboundTokenClaimValueType: InboundTokenClaimValueType

        @inlinable
        public init(authorizingClaimMatchValue: AuthorizingClaimMatchValueType, inboundTokenClaimName: String, inboundTokenClaimValueType: InboundTokenClaimValueType) {
            self.authorizingClaimMatchValue = authorizingClaimMatchValue
            self.inboundTokenClaimName = inboundTokenClaimName
            self.inboundTokenClaimValueType = inboundTokenClaimValueType
        }

        public func validate(name: String) throws {
            try self.authorizingClaimMatchValue.validate(name: "\(name).authorizingClaimMatchValue")
            try self.validate(self.inboundTokenClaimName, name: "inboundTokenClaimName", parent: name, max: 255)
            try self.validate(self.inboundTokenClaimName, name: "inboundTokenClaimName", parent: name, min: 1)
            try self.validate(self.inboundTokenClaimName, name: "inboundTokenClaimName", parent: name, pattern: "^[A-Za-z0-9_.-:]+$")
        }

        private enum CodingKeys: String, CodingKey {
            case authorizingClaimMatchValue = "authorizingClaimMatchValue"
            case inboundTokenClaimName = "inboundTokenClaimName"
            case inboundTokenClaimValueType = "inboundTokenClaimValueType"
        }
    }

    public struct CustomDescriptor: AWSEncodableShape & AWSDecodableShape {
        /// The custom descriptor content, serialized as descriptor payload data.
        public let data: String?

        @inlinable
        public init(data: String? = nil) {
            self.data = data
        }

        public func validate(name: String) throws {
            try self.validate(self.data, name: "data", parent: name, max: 102400)
            try self.validate(self.data, name: "data", parent: name, min: 1)
        }

        private enum CodingKeys: String, CodingKey {
            case data = "data"
        }
    }

    public struct CustomJWTAuthorizerConfiguration: AWSEncodableShape & AWSDecodableShape {
        /// The audience values accepted during JWT validation. A token is rejected if none of its audience claims match.
        public let allowedAudience: [String]?
        /// The client identifiers accepted during JWT validation. A token is rejected if it was not issued to one of these clients.
        public let allowedClients: [String]?
        /// The scopes accepted during JWT validation. A token is rejected if it does not carry one of these scopes.
        public let allowedScopes: [String]?
        /// Additional custom claim validations applied to the inbound JWT.
        public let customClaims: [CustomClaimValidationType]?
        /// The OpenID Connect discovery URL used to retrieve the identity provider's metadata and signing keys.
        public let discoveryUrl: String
        /// The private endpoint used to reach the identity provider's discovery URL over a private network path.
        public let privateEndpoint: PrivateEndpoint?
        /// Per-domain private endpoint overrides that route specific identity provider domains through distinct private endpoints.
        public let privateEndpointOverrides: [PrivateEndpointOverride]?

        @inlinable
        public init(allowedAudience: [String]? = nil, allowedClients: [String]? = nil, allowedScopes: [String]? = nil, customClaims: [CustomClaimValidationType]? = nil, discoveryUrl: String, privateEndpoint: PrivateEndpoint? = nil, privateEndpointOverrides: [PrivateEndpointOverride]? = nil) {
            self.allowedAudience = allowedAudience
            self.allowedClients = allowedClients
            self.allowedScopes = allowedScopes
            self.customClaims = customClaims
            self.discoveryUrl = discoveryUrl
            self.privateEndpoint = privateEndpoint
            self.privateEndpointOverrides = privateEndpointOverrides
        }

        public func validate(name: String) throws {
            try self.allowedAudience?.forEach {
                try validate($0, name: "allowedAudience[]", parent: name, max: 255)
                try validate($0, name: "allowedAudience[]", parent: name, min: 1)
            }
            try self.validate(self.allowedAudience, name: "allowedAudience", parent: name, min: 1)
            try self.allowedClients?.forEach {
                try validate($0, name: "allowedClients[]", parent: name, max: 255)
                try validate($0, name: "allowedClients[]", parent: name, min: 1)
            }
            try self.validate(self.allowedClients, name: "allowedClients", parent: name, min: 1)
            try self.allowedScopes?.forEach {
                try validate($0, name: "allowedScopes[]", parent: name, max: 255)
                try validate($0, name: "allowedScopes[]", parent: name, min: 1)
                try validate($0, name: "allowedScopes[]", parent: name, pattern: "^[\\x21\\x23-\\x5B\\x5D-\\x7E]+$")
            }
            try self.validate(self.allowedScopes, name: "allowedScopes", parent: name, min: 1)
            try self.customClaims?.forEach {
                try $0.validate(name: "\(name).customClaims[]")
            }
            try self.validate(self.customClaims, name: "customClaims", parent: name, min: 1)
            try self.validate(self.discoveryUrl, name: "discoveryUrl", parent: name, max: 2048)
            try self.validate(self.discoveryUrl, name: "discoveryUrl", parent: name, min: 1)
            try self.validate(self.discoveryUrl, name: "discoveryUrl", parent: name, pattern: "^.+/\\.well-known/openid-configuration$")
            try self.privateEndpoint?.validate(name: "\(name).privateEndpoint")
            try self.privateEndpointOverrides?.forEach {
                try $0.validate(name: "\(name).privateEndpointOverrides[]")
            }
            try self.validate(self.privateEndpointOverrides, name: "privateEndpointOverrides", parent: name, max: 5)
        }

        private enum CodingKeys: String, CodingKey {
            case allowedAudience = "allowedAudience"
            case allowedClients = "allowedClients"
            case allowedScopes = "allowedScopes"
            case customClaims = "customClaims"
            case discoveryUrl = "discoveryUrl"
            case privateEndpoint = "privateEndpoint"
            case privateEndpointOverrides = "privateEndpointOverrides"
        }
    }

    public struct DeleteRegistryRecordRequest: AWSEncodableShape {
        /// The identifier of the registry record to delete (ARN or ID)
        public let recordId: String
        /// The identifier of the registry containing the record (ARN or ID)
        public let registryId: String

        @inlinable
        public init(recordId: String, registryId: String) {
            self.recordId = recordId
            self.registryId = registryId
        }

        public func encode(to encoder: Encoder) throws {
            let request = encoder.userInfo[.awsRequest]! as! RequestEncodingContainer
            _ = encoder.container(keyedBy: CodingKeys.self)
            request.encodePath(self.recordId, key: "recordId")
            request.encodePath(self.registryId, key: "registryId")
        }

        public func validate(name: String) throws {
            try self.validate(self.recordId, name: "recordId", parent: name, max: 2048)
            try self.validate(self.recordId, name: "recordId", parent: name, min: 1)
            try self.validate(self.recordId, name: "recordId", parent: name, pattern: "^(arn:aws(-[^:]+)?:agent-registry:[a-z0-9-]+:[0-9]{12}:registry/[a-zA-Z0-9]{12,16}/record/)?[a-zA-Z0-9]{12}$")
            try self.validate(self.registryId, name: "registryId", parent: name, max: 2048)
            try self.validate(self.registryId, name: "registryId", parent: name, min: 1)
            try self.validate(self.registryId, name: "registryId", parent: name, pattern: "^(arn:aws(-[^:]+)?:agent-registry:[a-z0-9-]+:[0-9]{12}:registry/)?[a-zA-Z0-9]{12,16}$")
        }

        private enum CodingKeys: CodingKey {}
    }

    public struct DeleteRegistryRecordResponse: AWSDecodableShape {
        public init() {}
    }

    public struct DeleteRegistryRequest: AWSEncodableShape {
        /// The identifier of the registry to delete (ARN or ID)
        public let registryId: String

        @inlinable
        public init(registryId: String) {
            self.registryId = registryId
        }

        public func encode(to encoder: Encoder) throws {
            let request = encoder.userInfo[.awsRequest]! as! RequestEncodingContainer
            _ = encoder.container(keyedBy: CodingKeys.self)
            request.encodePath(self.registryId, key: "registryId")
        }

        public func validate(name: String) throws {
            try self.validate(self.registryId, name: "registryId", parent: name, max: 2048)
            try self.validate(self.registryId, name: "registryId", parent: name, min: 1)
            try self.validate(self.registryId, name: "registryId", parent: name, pattern: "^(arn:aws(-[^:]+)?:agent-registry:[a-z0-9-]+:[0-9]{12}:registry/)?[a-zA-Z0-9]{12,16}$")
        }

        private enum CodingKeys: CodingKey {}
    }

    public struct DeleteRegistryResponse: AWSDecodableShape {
        /// Current status of the registry, set to DELETING when deletion is initiated
        public let status: RegistryStatus

        @inlinable
        public init(status: RegistryStatus) {
            self.status = status
        }

        private enum CodingKeys: String, CodingKey {
            case status = "status"
        }
    }

    public struct DescriptorSource: AWSEncodableShape & AWSDecodableShape {
        /// URL-based descriptor source, populated when descriptor content is synchronized from a URL.
        public let fromUrl: DescriptorSourceFromUrl?

        @inlinable
        public init(fromUrl: DescriptorSourceFromUrl? = nil) {
            self.fromUrl = fromUrl
        }

        public func validate(name: String) throws {
            try self.fromUrl?.validate(name: "\(name).fromUrl")
        }

        private enum CodingKeys: String, CodingKey {
            case fromUrl = "fromUrl"
        }
    }

    public struct DescriptorSourceFromUrl: AWSEncodableShape & AWSDecodableShape {
        /// The credential providers used to authenticate when fetching descriptor content from the source URL.
        public let credentialProviderConfigurations: [RegistryRecordCredentialProviderConfiguration]?
        /// The URL from which the descriptor content is retrieved.
        public let url: String

        @inlinable
        public init(credentialProviderConfigurations: [RegistryRecordCredentialProviderConfiguration]? = nil, url: String) {
            self.credentialProviderConfigurations = credentialProviderConfigurations
            self.url = url
        }

        public func validate(name: String) throws {
            try self.credentialProviderConfigurations?.forEach {
                try $0.validate(name: "\(name).credentialProviderConfigurations[]")
            }
            try self.validate(self.credentialProviderConfigurations, name: "credentialProviderConfigurations", parent: name, max: 1)
            try self.validate(self.url, name: "url", parent: name, max: 2048)
            try self.validate(self.url, name: "url", parent: name, min: 1)
            try self.validate(self.url, name: "url", parent: name, pattern: "^https://.*$")
        }

        private enum CodingKeys: String, CodingKey {
            case credentialProviderConfigurations = "credentialProviderConfigurations"
            case url = "url"
        }
    }

    public struct Descriptors: AWSEncodableShape & AWSDecodableShape {
        /// The A2A agent card descriptor, populated when the record type is AGENT.
        public let a2aAgentCard: A2aAgentCardDescriptor?
        /// The agent skills definition descriptor, populated when the record type is SKILL.
        public let agentSkillsDefinition: AgentSkillsDefinitionDescriptor?
        /// The AG-UI descriptor, populated for records detected from an AG-UI protocol source.
        public let agui: AgUiDescriptor?
        /// The custom descriptor, populated when the record type is CUSTOM.
        public let custom: CustomDescriptor?
        /// The HTTP descriptor, populated for records detected from an HTTP protocol source.
        public let http: HttpDescriptor?
        /// The MCP server descriptor, populated when the record type is MCP.
        public let mcpServer: McpServerDescriptor?

        @inlinable
        public init(a2aAgentCard: A2aAgentCardDescriptor? = nil, agentSkillsDefinition: AgentSkillsDefinitionDescriptor? = nil, agui: AgUiDescriptor? = nil, custom: CustomDescriptor? = nil, http: HttpDescriptor? = nil, mcpServer: McpServerDescriptor? = nil) {
            self.a2aAgentCard = a2aAgentCard
            self.agentSkillsDefinition = agentSkillsDefinition
            self.agui = agui
            self.custom = custom
            self.http = http
            self.mcpServer = mcpServer
        }

        public func validate(name: String) throws {
            try self.a2aAgentCard?.validate(name: "\(name).a2aAgentCard")
            try self.agentSkillsDefinition?.validate(name: "\(name).agentSkillsDefinition")
            try self.agui?.validate(name: "\(name).agui")
            try self.custom?.validate(name: "\(name).custom")
            try self.http?.validate(name: "\(name).http")
            try self.mcpServer?.validate(name: "\(name).mcpServer")
        }

        private enum CodingKeys: String, CodingKey {
            case a2aAgentCard = "a2aAgentCard"
            case agentSkillsDefinition = "agentSkillsDefinition"
            case agui = "agui"
            case custom = "custom"
            case http = "http"
            case mcpServer = "mcpServer"
        }
    }

    public struct DiscoveryConfiguration: AWSEncodableShape & AWSDecodableShape {
        /// The authorizer configuration for the registry. Required when authorizerType is CUSTOM_JWT.
        public let authorizerConfiguration: AuthorizerConfiguration?
        /// The type of authorizer that controls how consumers access the registry's search and MCP invoke operations.
        public let authorizerType: RegistryAuthorizerType?

        @inlinable
        public init(authorizerConfiguration: AuthorizerConfiguration? = nil, authorizerType: RegistryAuthorizerType? = nil) {
            self.authorizerConfiguration = authorizerConfiguration
            self.authorizerType = authorizerType
        }

        public func validate(name: String) throws {
            try self.authorizerConfiguration?.validate(name: "\(name).authorizerConfiguration")
        }

        private enum CodingKeys: String, CodingKey {
            case authorizerConfiguration = "authorizerConfiguration"
            case authorizerType = "authorizerType"
        }
    }

    public struct EncryptionConfiguration: AWSEncodableShape & AWSDecodableShape {
        /// The Amazon Resource Name (ARN) of the customer-managed Amazon Web Services KMS key used to encrypt the registry's content. The key must be a symmetric encryption key in the same Amazon Web Services account and Region as the registry.
        public let kmsKeyArn: String

        @inlinable
        public init(kmsKeyArn: String) {
            self.kmsKeyArn = kmsKeyArn
        }

        public func validate(name: String) throws {
            try self.validate(self.kmsKeyArn, name: "kmsKeyArn", parent: name, max: 2048)
            try self.validate(self.kmsKeyArn, name: "kmsKeyArn", parent: name, min: 1)
            try self.validate(self.kmsKeyArn, name: "kmsKeyArn", parent: name, pattern: "^arn:aws(|-cn|-us-gov):kms:[a-zA-Z0-9-]*:[0-9]{12}:key/[a-zA-Z0-9-]{36}$")
        }

        private enum CodingKeys: String, CodingKey {
            case kmsKeyArn = "kmsKeyArn"
        }
    }

    public struct GetRegistryRecordRequest: AWSEncodableShape {
        /// The identifier of the registry record to retrieve (ARN or ID)
        public let recordId: String
        /// The identifier of the registry containing the record (ARN or ID)
        public let registryId: String

        @inlinable
        public init(recordId: String, registryId: String) {
            self.recordId = recordId
            self.registryId = registryId
        }

        public func encode(to encoder: Encoder) throws {
            let request = encoder.userInfo[.awsRequest]! as! RequestEncodingContainer
            _ = encoder.container(keyedBy: CodingKeys.self)
            request.encodePath(self.recordId, key: "recordId")
            request.encodePath(self.registryId, key: "registryId")
        }

        public func validate(name: String) throws {
            try self.validate(self.recordId, name: "recordId", parent: name, max: 2048)
            try self.validate(self.recordId, name: "recordId", parent: name, min: 1)
            try self.validate(self.recordId, name: "recordId", parent: name, pattern: "^(arn:aws(-[^:]+)?:agent-registry:[a-z0-9-]+:[0-9]{12}:registry/[a-zA-Z0-9]{12,16}/record/)?[a-zA-Z0-9]{12}$")
            try self.validate(self.registryId, name: "registryId", parent: name, max: 2048)
            try self.validate(self.registryId, name: "registryId", parent: name, min: 1)
            try self.validate(self.registryId, name: "registryId", parent: name, pattern: "^(arn:aws(-[^:]+)?:agent-registry:[a-z0-9-]+:[0-9]{12}:registry/)?[a-zA-Z0-9]{12,16}$")
        }

        private enum CodingKeys: CodingKey {}
    }

    public struct GetRegistryRecordResponse: AWSDecodableShape {
        /// The timestamp when the registry record was created.
        @CustomCoding<ISO8601DateCoder>
        public var createdAt: Date
        /// The ID of the Amazon Web Services account that created the registry record.
        public let createdBy: String?
        /// Specifies whether the registry record was created by auto-detection. true indicates the record was automatically created by the service based on the registry's auto-detection configuration; false indicates the record was created through a control-plane API call.
        public let createdByAutoDetection: Bool?
        /// A description of the registry record.
        public let description: String?
        /// The typed descriptors that define the content of the registry record.
        public let descriptors: Descriptors?
        /// The human-readable display name of the registry record.
        public let displayName: String?
        /// The name of the registry record. Names are unique within a registry.
        public let name: String
        public let provenance: [Provenance]?
        /// The Amazon Resource Name (ARN) of the registry record.
        public let recordArn: String
        /// The unique identifier of the registry record.
        public let recordId: String
        /// The type of the registry record, such as MCP, AGENT, SKILL, or CUSTOM.
        public let recordType: RecordType
        /// The version identifier of the registry record.
        public let recordVersion: String?
        /// The Amazon Resource Name (ARN) of the parent registry that owns the record.
        public let registryArn: String
        /// The lifecycle status of the registry record.
        public let status: RegistryRecordStatus
        /// The reason for the current status. Typically populated when the status indicates a failure state.
        public let statusReason: String?
        /// The timestamp when the registry record was last updated.
        @CustomCoding<ISO8601DateCoder>
        public var updatedAt: Date

        @inlinable
        public init(createdAt: Date, createdBy: String? = nil, createdByAutoDetection: Bool? = nil, description: String? = nil, descriptors: Descriptors? = nil, displayName: String? = nil, name: String, provenance: [Provenance]? = nil, recordArn: String, recordId: String, recordType: RecordType, recordVersion: String? = nil, registryArn: String, status: RegistryRecordStatus, statusReason: String? = nil, updatedAt: Date) {
            self.createdAt = createdAt
            self.createdBy = createdBy
            self.createdByAutoDetection = createdByAutoDetection
            self.description = description
            self.descriptors = descriptors
            self.displayName = displayName
            self.name = name
            self.provenance = provenance
            self.recordArn = recordArn
            self.recordId = recordId
            self.recordType = recordType
            self.recordVersion = recordVersion
            self.registryArn = registryArn
            self.status = status
            self.statusReason = statusReason
            self.updatedAt = updatedAt
        }

        private enum CodingKeys: String, CodingKey {
            case createdAt = "createdAt"
            case createdBy = "createdBy"
            case createdByAutoDetection = "createdByAutoDetection"
            case description = "description"
            case descriptors = "descriptors"
            case displayName = "displayName"
            case name = "name"
            case provenance = "provenance"
            case recordArn = "recordArn"
            case recordId = "recordId"
            case recordType = "recordType"
            case recordVersion = "recordVersion"
            case registryArn = "registryArn"
            case status = "status"
            case statusReason = "statusReason"
            case updatedAt = "updatedAt"
        }
    }

    public struct GetRegistryRequest: AWSEncodableShape {
        /// The identifier of the registry to retrieve (ARN or ID)
        public let registryId: String

        @inlinable
        public init(registryId: String) {
            self.registryId = registryId
        }

        public func encode(to encoder: Encoder) throws {
            let request = encoder.userInfo[.awsRequest]! as! RequestEncodingContainer
            _ = encoder.container(keyedBy: CodingKeys.self)
            request.encodePath(self.registryId, key: "registryId")
        }

        public func validate(name: String) throws {
            try self.validate(self.registryId, name: "registryId", parent: name, max: 2048)
            try self.validate(self.registryId, name: "registryId", parent: name, min: 1)
            try self.validate(self.registryId, name: "registryId", parent: name, pattern: "^(arn:aws(-[^:]+)?:agent-registry:[a-z0-9-]+:[0-9]{12}:registry/)?[a-zA-Z0-9]{12,16}$")
        }

        private enum CodingKeys: CodingKey {}
    }

    public struct GetRegistryResponse: AWSDecodableShape {
        /// Approval configuration for registry records
        public let approvalConfiguration: ApprovalConfiguration?
        /// The registry's auto-detection properties, including the requested configuration and the current detection status. Present only when auto-detection was configured for the registry.
        public let autoDetection: AutoDetection?
        /// The timestamp when the registry was created
        @CustomCoding<ISO8601DateCoder>
        public var createdAt: Date
        /// The description of the registry
        public let description: String?
        /// Discovery configuration for the registry
        public let discoveryConfiguration: DiscoveryConfiguration?
        /// The server-side encryption configuration for the registry. Appears only when a customer-managed Amazon Web Services KMS key encrypts the registry.
        public let encryptionConfiguration: EncryptionConfiguration?
        /// The name of the registry
        public let name: String
        /// The ARN of the registry
        public let registryArn: String
        /// The unique identifier of the registry
        public let registryId: String
        /// Current status of the registry
        public let status: RegistryStatus
        /// The reason for the current status. Typically populated when the status indicates a failure state.
        public let statusReason: String?
        /// The timestamp when the registry was last updated
        @CustomCoding<ISO8601DateCoder>
        public var updatedAt: Date

        @inlinable
        public init(approvalConfiguration: ApprovalConfiguration? = nil, autoDetection: AutoDetection? = nil, createdAt: Date, description: String? = nil, discoveryConfiguration: DiscoveryConfiguration? = nil, encryptionConfiguration: EncryptionConfiguration? = nil, name: String, registryArn: String, registryId: String, status: RegistryStatus, statusReason: String? = nil, updatedAt: Date) {
            self.approvalConfiguration = approvalConfiguration
            self.autoDetection = autoDetection
            self.createdAt = createdAt
            self.description = description
            self.discoveryConfiguration = discoveryConfiguration
            self.encryptionConfiguration = encryptionConfiguration
            self.name = name
            self.registryArn = registryArn
            self.registryId = registryId
            self.status = status
            self.statusReason = statusReason
            self.updatedAt = updatedAt
        }

        private enum CodingKeys: String, CodingKey {
            case approvalConfiguration = "approvalConfiguration"
            case autoDetection = "autoDetection"
            case createdAt = "createdAt"
            case description = "description"
            case discoveryConfiguration = "discoveryConfiguration"
            case encryptionConfiguration = "encryptionConfiguration"
            case name = "name"
            case registryArn = "registryArn"
            case registryId = "registryId"
            case status = "status"
            case statusReason = "statusReason"
            case updatedAt = "updatedAt"
        }
    }

    public struct HttpDescriptor: AWSEncodableShape & AWSDecodableShape {
        public let source: DescriptorSource?

        @inlinable
        public init(source: DescriptorSource? = nil) {
            self.source = source
        }

        public func validate(name: String) throws {
            try self.source?.validate(name: "\(name).source")
        }

        private enum CodingKeys: String, CodingKey {
            case source = "source"
        }
    }

    public struct ListRegistriesRequest: AWSEncodableShape {
        /// Filters to apply to the registry list
        public let filters: [RegistryFilter]?
        /// Maximum number of results to return
        public let maxResults: Int?
        /// Token for pagination
        public let nextToken: String?

        @inlinable
        public init(filters: [RegistryFilter]? = nil, maxResults: Int? = nil, nextToken: String? = nil) {
            self.filters = filters
            self.maxResults = maxResults
            self.nextToken = nextToken
        }

        public func validate(name: String) throws {
            try self.filters?.forEach {
                try $0.validate(name: "\(name).filters[]")
            }
            try self.validate(self.filters, name: "filters", parent: name, max: 10)
            try self.validate(self.maxResults, name: "maxResults", parent: name, max: 100)
            try self.validate(self.maxResults, name: "maxResults", parent: name, min: 1)
            try self.validate(self.nextToken, name: "nextToken", parent: name, max: 2048)
            try self.validate(self.nextToken, name: "nextToken", parent: name, min: 1)
            try self.validate(self.nextToken, name: "nextToken", parent: name, pattern: "^\\S*$")
        }

        private enum CodingKeys: String, CodingKey {
            case filters = "filters"
            case maxResults = "maxResults"
            case nextToken = "nextToken"
        }
    }

    public struct ListRegistriesResponse: AWSDecodableShape {
        /// Token for next page of results
        public let nextToken: String?
        /// List of registry summaries
        public let registries: [RegistrySummary]

        @inlinable
        public init(nextToken: String? = nil, registries: [RegistrySummary]) {
            self.nextToken = nextToken
            self.registries = registries
        }

        private enum CodingKeys: String, CodingKey {
            case nextToken = "nextToken"
            case registries = "registries"
        }
    }

    public struct ListRegistryRecordsRequest: AWSEncodableShape {
        /// Filters to apply to the registry record list
        public let filters: [RegistryRecordFilter]?
        /// Maximum number of records to return
        public let maxResults: Int?
        /// Token for pagination
        public let nextToken: String?
        /// The identifier of the registry to list records from (ARN or ID)
        public let registryId: String

        @inlinable
        public init(filters: [RegistryRecordFilter]? = nil, maxResults: Int? = nil, nextToken: String? = nil, registryId: String) {
            self.filters = filters
            self.maxResults = maxResults
            self.nextToken = nextToken
            self.registryId = registryId
        }

        public func encode(to encoder: Encoder) throws {
            let request = encoder.userInfo[.awsRequest]! as! RequestEncodingContainer
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encodeIfPresent(self.filters, forKey: .filters)
            try container.encodeIfPresent(self.maxResults, forKey: .maxResults)
            try container.encodeIfPresent(self.nextToken, forKey: .nextToken)
            request.encodePath(self.registryId, key: "registryId")
        }

        public func validate(name: String) throws {
            try self.filters?.forEach {
                try $0.validate(name: "\(name).filters[]")
            }
            try self.validate(self.filters, name: "filters", parent: name, max: 10)
            try self.validate(self.maxResults, name: "maxResults", parent: name, max: 100)
            try self.validate(self.maxResults, name: "maxResults", parent: name, min: 1)
            try self.validate(self.nextToken, name: "nextToken", parent: name, max: 2048)
            try self.validate(self.nextToken, name: "nextToken", parent: name, min: 1)
            try self.validate(self.nextToken, name: "nextToken", parent: name, pattern: "^\\S*$")
            try self.validate(self.registryId, name: "registryId", parent: name, max: 2048)
            try self.validate(self.registryId, name: "registryId", parent: name, min: 1)
            try self.validate(self.registryId, name: "registryId", parent: name, pattern: "^(arn:aws(-[^:]+)?:agent-registry:[a-z0-9-]+:[0-9]{12}:registry/)?[a-zA-Z0-9]{12,16}$")
        }

        private enum CodingKeys: String, CodingKey {
            case filters = "filters"
            case maxResults = "maxResults"
            case nextToken = "nextToken"
        }
    }

    public struct ListRegistryRecordsResponse: AWSDecodableShape {
        /// Token for next page of results
        public let nextToken: String?
        /// List of registry record summaries
        public let registryRecords: [RegistryRecordSummary]

        @inlinable
        public init(nextToken: String? = nil, registryRecords: [RegistryRecordSummary]) {
            self.nextToken = nextToken
            self.registryRecords = registryRecords
        }

        private enum CodingKeys: String, CodingKey {
            case nextToken = "nextToken"
            case registryRecords = "registryRecords"
        }
    }

    public struct ListTagsForResourceRequest: AWSEncodableShape {
        /// The Amazon Resource Name (ARN) of the resource to list tags for. Supported resources include registries and registry records.
        public let resourceArn: String

        @inlinable
        public init(resourceArn: String) {
            self.resourceArn = resourceArn
        }

        public func encode(to encoder: Encoder) throws {
            let request = encoder.userInfo[.awsRequest]! as! RequestEncodingContainer
            _ = encoder.container(keyedBy: CodingKeys.self)
            request.encodePath(self.resourceArn, key: "resourceArn")
        }

        public func validate(name: String) throws {
            try self.validate(self.resourceArn, name: "resourceArn", parent: name, max: 1011)
            try self.validate(self.resourceArn, name: "resourceArn", parent: name, min: 1)
        }

        private enum CodingKeys: CodingKey {}
    }

    public struct ListTagsForResourceResponse: AWSDecodableShape {
        /// The tags currently associated with the resource, as a map of tag keys to tag values.
        public let tags: [String: String]?

        @inlinable
        public init(tags: [String: String]? = nil) {
            self.tags = tags
        }

        private enum CodingKeys: String, CodingKey {
            case tags = "tags"
        }
    }

    public struct ManagedVpcResource: AWSEncodableShape & AWSDecodableShape {
        /// The IP address type used by the private endpoint, either IPV4 or IPV6.
        public let endpointIpAddressType: EndpointIpAddressType
        /// The routing domain used to resolve traffic through the private endpoint.
        public let routingDomain: String?
        /// The identifiers of the security groups associated with the private endpoint network interfaces.
        public let securityGroupIds: [String]?
        /// The identifiers of the subnets in which the private endpoint network interfaces are placed.
        public let subnetIds: [String]
        /// The tags applied to the service-managed VPC resource.
        public let tags: [String: String]?
        /// The identifier of the VPC in which the private endpoint is provisioned.
        public let vpcIdentifier: String

        @inlinable
        public init(endpointIpAddressType: EndpointIpAddressType, routingDomain: String? = nil, securityGroupIds: [String]? = nil, subnetIds: [String], tags: [String: String]? = nil, vpcIdentifier: String) {
            self.endpointIpAddressType = endpointIpAddressType
            self.routingDomain = routingDomain
            self.securityGroupIds = securityGroupIds
            self.subnetIds = subnetIds
            self.tags = tags
            self.vpcIdentifier = vpcIdentifier
        }

        public func validate(name: String) throws {
            try self.validate(self.routingDomain, name: "routingDomain", parent: name, max: 255)
            try self.validate(self.routingDomain, name: "routingDomain", parent: name, min: 3)
            try self.securityGroupIds?.forEach {
                try validate($0, name: "securityGroupIds[]", parent: name, max: 48)
                try validate($0, name: "securityGroupIds[]", parent: name, min: 10)
                try validate($0, name: "securityGroupIds[]", parent: name, pattern: "^sg-(([0-9a-z]{8})|([0-9a-z]{17}))$")
            }
            try self.validate(self.securityGroupIds, name: "securityGroupIds", parent: name, max: 5)
            try self.subnetIds.forEach {
                try validate($0, name: "subnetIds[]", parent: name, max: 24)
                try validate($0, name: "subnetIds[]", parent: name, min: 15)
                try validate($0, name: "subnetIds[]", parent: name, pattern: "^subnet-[0-9a-zA-Z]{8,17}$")
            }
            try self.tags?.forEach {
                try validate($0.key, name: "tags.key", parent: name, max: 128)
                try validate($0.key, name: "tags.key", parent: name, min: 1)
                try validate($0.key, name: "tags.key", parent: name, pattern: "^[a-zA-Z0-9\\s._:/=+@-]*$")
                try validate($0.value, name: "tags[\"\($0.key)\"]", parent: name, max: 256)
                try validate($0.value, name: "tags[\"\($0.key)\"]", parent: name, pattern: "^[a-zA-Z0-9\\s._:/=+@-]*$")
            }
            try self.validate(self.tags, name: "tags", parent: name, max: 50)
            try self.validate(self.tags, name: "tags", parent: name, min: 1)
            try self.validate(self.vpcIdentifier, name: "vpcIdentifier", parent: name, max: 48)
            try self.validate(self.vpcIdentifier, name: "vpcIdentifier", parent: name, min: 10)
            try self.validate(self.vpcIdentifier, name: "vpcIdentifier", parent: name, pattern: "^vpc-(([0-9a-z]{8})|([0-9a-z]{17}))$")
        }

        private enum CodingKeys: String, CodingKey {
            case endpointIpAddressType = "endpointIpAddressType"
            case routingDomain = "routingDomain"
            case securityGroupIds = "securityGroupIds"
            case subnetIds = "subnetIds"
            case tags = "tags"
            case vpcIdentifier = "vpcIdentifier"
        }
    }

    public struct McpServerAdditionalData: AWSEncodableShape & AWSDecodableShape {
        /// The MCP tools descriptor that defines the tools exposed by the MCP server.
        public let tools: McpToolsDescriptor?

        @inlinable
        public init(tools: McpToolsDescriptor? = nil) {
            self.tools = tools
        }

        public func validate(name: String) throws {
            try self.tools?.validate(name: "\(name).tools")
        }

        private enum CodingKeys: String, CodingKey {
            case tools = "tools"
        }
    }

    public struct McpServerDescriptor: AWSEncodableShape & AWSDecodableShape {
        /// Additional data associated with the MCP server descriptor, such as tool definitions.
        public let additionalData: McpServerAdditionalData?
        /// The MCP server descriptor content, serialized as descriptor payload data.
        public let data: String?
        /// The schema version of the descriptor payload.
        public let dataSchemaVersion: String?
        /// The optional source configuration used to synchronize the MCP server descriptor content.
        public let source: DescriptorSource?

        @inlinable
        public init(additionalData: McpServerAdditionalData? = nil, data: String? = nil, dataSchemaVersion: String? = nil, source: DescriptorSource? = nil) {
            self.additionalData = additionalData
            self.data = data
            self.dataSchemaVersion = dataSchemaVersion
            self.source = source
        }

        public func validate(name: String) throws {
            try self.additionalData?.validate(name: "\(name).additionalData")
            try self.validate(self.data, name: "data", parent: name, max: 102400)
            try self.validate(self.data, name: "data", parent: name, min: 1)
            try self.validate(self.dataSchemaVersion, name: "dataSchemaVersion", parent: name, max: 255)
            try self.validate(self.dataSchemaVersion, name: "dataSchemaVersion", parent: name, min: 1)
            try self.source?.validate(name: "\(name).source")
        }

        private enum CodingKeys: String, CodingKey {
            case additionalData = "additionalData"
            case data = "data"
            case dataSchemaVersion = "dataSchemaVersion"
            case source = "source"
        }
    }

    public struct McpToolsDescriptor: AWSEncodableShape & AWSDecodableShape {
        /// The MCP tools descriptor content, serialized as descriptor payload data.
        public let data: String?
        /// The schema version of the descriptor payload.
        public let dataSchemaVersion: String?

        @inlinable
        public init(data: String? = nil, dataSchemaVersion: String? = nil) {
            self.data = data
            self.dataSchemaVersion = dataSchemaVersion
        }

        public func validate(name: String) throws {
            try self.validate(self.data, name: "data", parent: name, max: 102400)
            try self.validate(self.data, name: "data", parent: name, min: 1)
            try self.validate(self.dataSchemaVersion, name: "dataSchemaVersion", parent: name, max: 255)
            try self.validate(self.dataSchemaVersion, name: "dataSchemaVersion", parent: name, min: 1)
        }

        private enum CodingKeys: String, CodingKey {
            case data = "data"
            case dataSchemaVersion = "dataSchemaVersion"
        }
    }

    public struct PrivateEndpointOverride: AWSEncodableShape & AWSDecodableShape {
        /// The domain name to which this private endpoint override applies.
        public let domain: String
        /// The private endpoint used to reach the specified domain.
        public let privateEndpoint: PrivateEndpoint

        @inlinable
        public init(domain: String, privateEndpoint: PrivateEndpoint) {
            self.domain = domain
            self.privateEndpoint = privateEndpoint
        }

        public func validate(name: String) throws {
            try self.validate(self.domain, name: "domain", parent: name, max: 253)
            try self.validate(self.domain, name: "domain", parent: name, min: 1)
            try self.privateEndpoint.validate(name: "\(name).privateEndpoint")
        }

        private enum CodingKeys: String, CodingKey {
            case domain = "domain"
            case privateEndpoint = "privateEndpoint"
        }
    }

    public struct Provenance: AWSEncodableShape & AWSDecodableShape {
        public let relation: ProvenanceRelation
        /// Additional details about the upstream source that the registry record was detected from, such as the AgentCore Gateway or Runtime configuration. The populated member corresponds to the source type.
        public let sourceDetails: SourceDetails?
        /// The identifier of the upstream source that the registry record was detected from.
        public let sourceId: String
        /// The type of the upstream source that the registry record was detected from.
        public let sourceType: SourceType?

        @inlinable
        public init(relation: ProvenanceRelation, sourceDetails: SourceDetails? = nil, sourceId: String, sourceType: SourceType? = nil) {
            self.relation = relation
            self.sourceDetails = sourceDetails
            self.sourceId = sourceId
            self.sourceType = sourceType
        }

        public func validate(name: String) throws {
            try self.sourceDetails?.validate(name: "\(name).sourceDetails")
            try self.validate(self.sourceId, name: "sourceId", parent: name, max: 2048)
            try self.validate(self.sourceId, name: "sourceId", parent: name, min: 1)
            try self.validate(self.sourceId, name: "sourceId", parent: name, pattern: "^arn:aws(-[^:]+)?:[a-zA-Z0-9-]+:[a-z0-9-]*:[0-9]{12}:.+$")
        }

        private enum CodingKeys: String, CodingKey {
            case relation = "relation"
            case sourceDetails = "sourceDetails"
            case sourceId = "sourceId"
            case sourceType = "sourceType"
        }
    }

    public struct ProvenanceSummary: AWSDecodableShape {
        public let relation: ProvenanceRelation
        /// The identifier of the upstream source that the registry record was detected from.
        public let sourceId: String
        /// The type of the upstream source that the registry record was detected from.
        public let sourceType: SourceType?

        @inlinable
        public init(relation: ProvenanceRelation, sourceId: String, sourceType: SourceType? = nil) {
            self.relation = relation
            self.sourceId = sourceId
            self.sourceType = sourceType
        }

        private enum CodingKeys: String, CodingKey {
            case relation = "relation"
            case sourceId = "sourceId"
            case sourceType = "sourceType"
        }
    }

    public struct RegistryFilter: AWSEncodableShape {
        /// The attribute to filter on
        public let name: RegistryFilterName
        /// The values to match for the attribute
        public let values: [String]

        @inlinable
        public init(name: RegistryFilterName, values: [String]) {
            self.name = name
            self.values = values
        }

        public func validate(name: String) throws {
            try self.values.forEach {
                try validate($0, name: "values[]", parent: name, max: 255)
                try validate($0, name: "values[]", parent: name, min: 1)
            }
            try self.validate(self.values, name: "values", parent: name, max: 1)
            try self.validate(self.values, name: "values", parent: name, min: 1)
        }

        private enum CodingKeys: String, CodingKey {
            case name = "name"
            case values = "values"
        }
    }

    public struct RegistryRecordCredentialProviderConfiguration: AWSEncodableShape & AWSDecodableShape {
        /// The credential provider details corresponding to the specified credential provider type.
        public let credentialProvider: RegistryRecordCredentialProviderUnion
        /// The type of credential provider.
        public let credentialProviderType: RegistryRecordCredentialProviderType

        @inlinable
        public init(credentialProvider: RegistryRecordCredentialProviderUnion, credentialProviderType: RegistryRecordCredentialProviderType) {
            self.credentialProvider = credentialProvider
            self.credentialProviderType = credentialProviderType
        }

        public func validate(name: String) throws {
            try self.credentialProvider.validate(name: "\(name).credentialProvider")
        }

        private enum CodingKeys: String, CodingKey {
            case credentialProvider = "credentialProvider"
            case credentialProviderType = "credentialProviderType"
        }
    }

    public struct RegistryRecordFilter: AWSEncodableShape {
        /// The attribute to filter on
        public let name: RegistryRecordFilterName
        /// The values to match for the attribute
        public let values: [String]

        @inlinable
        public init(name: RegistryRecordFilterName, values: [String]) {
            self.name = name
            self.values = values
        }

        public func validate(name: String) throws {
            try self.values.forEach {
                try validate($0, name: "values[]", parent: name, max: 255)
                try validate($0, name: "values[]", parent: name, min: 1)
            }
            try self.validate(self.values, name: "values", parent: name, max: 1)
            try self.validate(self.values, name: "values", parent: name, min: 1)
        }

        private enum CodingKeys: String, CodingKey {
            case name = "name"
            case values = "values"
        }
    }

    public struct RegistryRecordIamCredentialProvider: AWSEncodableShape & AWSDecodableShape {
        /// The Amazon Web Services Region to use for request signing. If not specified, the Region is derived from the source URL hostname, falling back to the Region of the registry.
        public let region: String?
        /// The Amazon Resource Name (ARN) of the IAM role to assume for request signing.
        public let roleArn: String?
        /// The service name to use for request signing, such as execute-api.
        public let service: String?

        @inlinable
        public init(region: String? = nil, roleArn: String? = nil, service: String? = nil) {
            self.region = region
            self.roleArn = roleArn
            self.service = service
        }

        public func validate(name: String) throws {
            try self.validate(self.region, name: "region", parent: name, max: 64)
            try self.validate(self.region, name: "region", parent: name, min: 1)
            try self.validate(self.region, name: "region", parent: name, pattern: "^[a-z0-9-]+$")
            try self.validate(self.roleArn, name: "roleArn", parent: name, max: 2048)
            try self.validate(self.roleArn, name: "roleArn", parent: name, min: 20)
            try self.validate(self.roleArn, name: "roleArn", parent: name, pattern: "^arn:aws(-[^:]+)?:iam::[0-9]{12}:role/.+$")
            try self.validate(self.service, name: "service", parent: name, max: 128)
            try self.validate(self.service, name: "service", parent: name, min: 1)
            try self.validate(self.service, name: "service", parent: name, pattern: "^[a-zA-Z0-9_-]+$")
        }

        private enum CodingKeys: String, CodingKey {
            case region = "region"
            case roleArn = "roleArn"
            case service = "service"
        }
    }

    public struct RegistryRecordOAuthCredentialProvider: AWSEncodableShape & AWSDecodableShape {
        /// Additional parameters to include in the OAuth 2.0 token request.
        public let customParameters: [String: String]?
        /// The OAuth 2.0 grant type used to obtain access tokens.
        public let grantType: RegistryRecordOAuthGrantType?
        /// The Amazon Resource Name (ARN) of the OAuth 2.0 credential provider resource in Amazon Bedrock AgentCore Identity.
        public let providerArn: String
        /// The OAuth 2.0 scopes to request when obtaining access tokens.
        public let scopes: [String]?

        @inlinable
        public init(customParameters: [String: String]? = nil, grantType: RegistryRecordOAuthGrantType? = nil, providerArn: String, scopes: [String]? = nil) {
            self.customParameters = customParameters
            self.grantType = grantType
            self.providerArn = providerArn
            self.scopes = scopes
        }

        public func validate(name: String) throws {
            try self.validate(self.providerArn, name: "providerArn", parent: name, max: 2048)
            try self.validate(self.providerArn, name: "providerArn", parent: name, min: 1)
            try self.validate(self.providerArn, name: "providerArn", parent: name, pattern: "^arn:aws(-[^:]+)?:bedrock-agentcore:[a-z0-9-]+:[0-9]{12}:.*$")
        }

        private enum CodingKeys: String, CodingKey {
            case customParameters = "customParameters"
            case grantType = "grantType"
            case providerArn = "providerArn"
            case scopes = "scopes"
        }
    }

    public struct RegistryRecordSummary: AWSDecodableShape {
        /// The timestamp when the registry record was created.
        @CustomCoding<ISO8601DateCoder>
        public var createdAt: Date
        /// The ID of the Amazon Web Services account that created the registry record.
        public let createdBy: String?
        /// Specifies whether the registry record was created by auto-detection. true indicates the record was automatically created by the service based on the registry's auto-detection configuration; false indicates the record was created through a control-plane API call.
        public let createdByAutoDetection: Bool?
        /// A description of the registry record.
        public let description: String?
        /// The human-readable display name of the registry record.
        public let displayName: String?
        /// The name of the registry record. Names are unique within a registry.
        public let name: String
        public let provenanceSummaryList: [ProvenanceSummary]?
        /// The Amazon Resource Name (ARN) of the registry record.
        public let recordArn: String
        /// The unique identifier of the registry record.
        public let recordId: String
        /// The type of the registry record, such as MCP, AGENT, SKILL, or CUSTOM.
        public let recordType: RecordType
        /// The version identifier of the registry record.
        public let recordVersion: String
        /// The Amazon Resource Name (ARN) of the parent registry that owns the record.
        public let registryArn: String
        /// The lifecycle status of the registry record.
        public let status: RegistryRecordStatus
        /// The timestamp when the registry record was last updated.
        @CustomCoding<ISO8601DateCoder>
        public var updatedAt: Date

        @inlinable
        public init(createdAt: Date, createdBy: String? = nil, createdByAutoDetection: Bool? = nil, description: String? = nil, displayName: String? = nil, name: String, provenanceSummaryList: [ProvenanceSummary]? = nil, recordArn: String, recordId: String, recordType: RecordType, recordVersion: String, registryArn: String, status: RegistryRecordStatus, updatedAt: Date) {
            self.createdAt = createdAt
            self.createdBy = createdBy
            self.createdByAutoDetection = createdByAutoDetection
            self.description = description
            self.displayName = displayName
            self.name = name
            self.provenanceSummaryList = provenanceSummaryList
            self.recordArn = recordArn
            self.recordId = recordId
            self.recordType = recordType
            self.recordVersion = recordVersion
            self.registryArn = registryArn
            self.status = status
            self.updatedAt = updatedAt
        }

        private enum CodingKeys: String, CodingKey {
            case createdAt = "createdAt"
            case createdBy = "createdBy"
            case createdByAutoDetection = "createdByAutoDetection"
            case description = "description"
            case displayName = "displayName"
            case name = "name"
            case provenanceSummaryList = "provenanceSummaryList"
            case recordArn = "recordArn"
            case recordId = "recordId"
            case recordType = "recordType"
            case recordVersion = "recordVersion"
            case registryArn = "registryArn"
            case status = "status"
            case updatedAt = "updatedAt"
        }
    }

    public struct RegistrySummary: AWSDecodableShape {
        /// The registry's auto-detection properties, including the requested configuration and the current detection status. Present only when auto-detection was configured for the registry.
        public let autoDetection: AutoDetection?
        /// The timestamp when the registry was created
        @CustomCoding<ISO8601DateCoder>
        public var createdAt: Date
        /// Registry description
        public let description: String?
        /// Discovery configuration for the registry
        public let discoveryConfiguration: DiscoveryConfiguration?
        /// Registry name
        public let name: String
        /// Registry Amazon Resource Name
        public let registryArn: String
        /// Unique registry identifier
        public let registryId: String
        /// Current status of the registry
        public let status: RegistryStatus
        /// The reason for the current status. Typically populated when the status indicates a failure state.
        public let statusReason: String?
        /// The timestamp when the registry was last updated
        @CustomCoding<ISO8601DateCoder>
        public var updatedAt: Date

        @inlinable
        public init(autoDetection: AutoDetection? = nil, createdAt: Date, description: String? = nil, discoveryConfiguration: DiscoveryConfiguration? = nil, name: String, registryArn: String, registryId: String, status: RegistryStatus, statusReason: String? = nil, updatedAt: Date) {
            self.autoDetection = autoDetection
            self.createdAt = createdAt
            self.description = description
            self.discoveryConfiguration = discoveryConfiguration
            self.name = name
            self.registryArn = registryArn
            self.registryId = registryId
            self.status = status
            self.statusReason = statusReason
            self.updatedAt = updatedAt
        }

        private enum CodingKeys: String, CodingKey {
            case autoDetection = "autoDetection"
            case createdAt = "createdAt"
            case description = "description"
            case discoveryConfiguration = "discoveryConfiguration"
            case name = "name"
            case registryArn = "registryArn"
            case registryId = "registryId"
            case status = "status"
            case statusReason = "statusReason"
            case updatedAt = "updatedAt"
        }
    }

    public struct SubmitRegistryRecordForApprovalRequest: AWSEncodableShape {
        /// The identifier of the registry record to submit for approval (ARN or ID)
        public let recordId: String
        /// The identifier of the registry containing the record (ARN or ID)
        public let registryId: String

        @inlinable
        public init(recordId: String, registryId: String) {
            self.recordId = recordId
            self.registryId = registryId
        }

        public func encode(to encoder: Encoder) throws {
            let request = encoder.userInfo[.awsRequest]! as! RequestEncodingContainer
            _ = encoder.container(keyedBy: CodingKeys.self)
            request.encodePath(self.recordId, key: "recordId")
            request.encodePath(self.registryId, key: "registryId")
        }

        public func validate(name: String) throws {
            try self.validate(self.recordId, name: "recordId", parent: name, max: 2048)
            try self.validate(self.recordId, name: "recordId", parent: name, min: 1)
            try self.validate(self.recordId, name: "recordId", parent: name, pattern: "^(arn:aws(-[^:]+)?:agent-registry:[a-z0-9-]+:[0-9]{12}:registry/[a-zA-Z0-9]{12,16}/record/)?[a-zA-Z0-9]{12}$")
            try self.validate(self.registryId, name: "registryId", parent: name, max: 2048)
            try self.validate(self.registryId, name: "registryId", parent: name, min: 1)
            try self.validate(self.registryId, name: "registryId", parent: name, pattern: "^(arn:aws(-[^:]+)?:agent-registry:[a-z0-9-]+:[0-9]{12}:registry/)?[a-zA-Z0-9]{12,16}$")
        }

        private enum CodingKeys: CodingKey {}
    }

    public struct SubmitRegistryRecordForApprovalResponse: AWSDecodableShape {
        /// The ARN of the registry record
        public let recordArn: String
        /// The ID of the registry record
        public let recordId: String
        /// The ARN of the registry
        public let registryArn: String
        /// The resulting status of the registry record
        public let status: RegistryRecordStatus
        /// The timestamp when the record was last updated
        @CustomCoding<ISO8601DateCoder>
        public var updatedAt: Date

        @inlinable
        public init(recordArn: String, recordId: String, registryArn: String, status: RegistryRecordStatus, updatedAt: Date) {
            self.recordArn = recordArn
            self.recordId = recordId
            self.registryArn = registryArn
            self.status = status
            self.updatedAt = updatedAt
        }

        private enum CodingKeys: String, CodingKey {
            case recordArn = "recordArn"
            case recordId = "recordId"
            case registryArn = "registryArn"
            case status = "status"
            case updatedAt = "updatedAt"
        }
    }

    public struct TagResourceRequest: AWSEncodableShape {
        /// The Amazon Resource Name (ARN) of the resource to tag. Supported resources include registries and registry records.
        public let resourceArn: String
        /// The tags to apply to the resource, as a map of tag keys to tag values. Tag keys must be unique within the request.
        public let tags: [String: String]

        @inlinable
        public init(resourceArn: String, tags: [String: String]) {
            self.resourceArn = resourceArn
            self.tags = tags
        }

        public func encode(to encoder: Encoder) throws {
            let request = encoder.userInfo[.awsRequest]! as! RequestEncodingContainer
            var container = encoder.container(keyedBy: CodingKeys.self)
            request.encodePath(self.resourceArn, key: "resourceArn")
            try container.encode(self.tags, forKey: .tags)
        }

        public func validate(name: String) throws {
            try self.validate(self.resourceArn, name: "resourceArn", parent: name, max: 1011)
            try self.validate(self.resourceArn, name: "resourceArn", parent: name, min: 1)
            try self.tags.forEach {
                try validate($0.key, name: "tags.key", parent: name, max: 128)
                try validate($0.key, name: "tags.key", parent: name, min: 1)
                try validate($0.key, name: "tags.key", parent: name, pattern: "^[a-zA-Z0-9\\s._:/=+@-]*$")
                try validate($0.value, name: "tags[\"\($0.key)\"]", parent: name, max: 256)
                try validate($0.value, name: "tags[\"\($0.key)\"]", parent: name, pattern: "^[a-zA-Z0-9\\s._:/=+@-]*$")
            }
            try self.validate(self.tags, name: "tags", parent: name, max: 50)
            try self.validate(self.tags, name: "tags", parent: name, min: 1)
        }

        private enum CodingKeys: String, CodingKey {
            case tags = "tags"
        }
    }

    public struct TagResourceResponse: AWSDecodableShape {
        public init() {}
    }

    public struct UntagResourceRequest: AWSEncodableShape {
        /// The Amazon Resource Name (ARN) of the resource to remove tags from. Supported resources include registries and registry records.
        public let resourceArn: String
        /// The keys of the tags to remove from the resource. Tags with keys not included in this list remain on the resource.
        public let tagKeys: [String]

        @inlinable
        public init(resourceArn: String, tagKeys: [String]) {
            self.resourceArn = resourceArn
            self.tagKeys = tagKeys
        }

        public func encode(to encoder: Encoder) throws {
            let request = encoder.userInfo[.awsRequest]! as! RequestEncodingContainer
            _ = encoder.container(keyedBy: CodingKeys.self)
            request.encodePath(self.resourceArn, key: "resourceArn")
            request.encodeQuery(self.tagKeys, key: "tagKeys")
        }

        public func validate(name: String) throws {
            try self.validate(self.resourceArn, name: "resourceArn", parent: name, max: 1011)
            try self.validate(self.resourceArn, name: "resourceArn", parent: name, min: 1)
            try self.tagKeys.forEach {
                try validate($0, name: "tagKeys[]", parent: name, max: 128)
                try validate($0, name: "tagKeys[]", parent: name, min: 1)
                try validate($0, name: "tagKeys[]", parent: name, pattern: "^[a-zA-Z0-9\\s._:/=+@-]*$")
            }
            try self.validate(self.tagKeys, name: "tagKeys", parent: name, max: 50)
            try self.validate(self.tagKeys, name: "tagKeys", parent: name, min: 1)
        }

        private enum CodingKeys: CodingKey {}
    }

    public struct UntagResourceResponse: AWSDecodableShape {
        public init() {}
    }

    public struct UpdateRegistryRecordRequest: AWSEncodableShape {
        /// The updated description of the registry record. Omit to leave the description unchanged; provide an empty wrapper to unset it.
        public let description: UpdatedDescription?
        /// The updated typed descriptor content for the registry record. Omit to leave the descriptors unchanged.
        public let descriptors: UpdatedDescriptors?
        /// The updated display name of the registry record. Omit to leave the display name unchanged; provide an empty wrapper to unset it.
        public let displayName: UpdatedDisplayName?
        /// The updated name of the registry record. Omit to leave the name unchanged.
        public let name: String?
        public let provenance: [Provenance]?
        /// The identifier of the registry record to update (ARN or ID)
        public let recordId: String
        /// The updated type of the registry record. Omit to leave the record type unchanged.
        public let recordType: RecordType?
        /// The updated version of the registry record. Omit to leave the version unchanged.
        public let recordVersion: String?
        /// The identifier of the registry containing the record (ARN or ID)
        public let registryId: String
        /// Whether to trigger synchronization of the record's descriptor content from its source
        public let triggerSynchronization: Bool?

        @inlinable
        public init(description: UpdatedDescription? = nil, descriptors: UpdatedDescriptors? = nil, displayName: UpdatedDisplayName? = nil, name: String? = nil, provenance: [Provenance]? = nil, recordId: String, recordType: RecordType? = nil, recordVersion: String? = nil, registryId: String, triggerSynchronization: Bool? = nil) {
            self.description = description
            self.descriptors = descriptors
            self.displayName = displayName
            self.name = name
            self.provenance = provenance
            self.recordId = recordId
            self.recordType = recordType
            self.recordVersion = recordVersion
            self.registryId = registryId
            self.triggerSynchronization = triggerSynchronization
        }

        public func encode(to encoder: Encoder) throws {
            let request = encoder.userInfo[.awsRequest]! as! RequestEncodingContainer
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encodeIfPresent(self.description, forKey: .description)
            try container.encodeIfPresent(self.descriptors, forKey: .descriptors)
            try container.encodeIfPresent(self.displayName, forKey: .displayName)
            try container.encodeIfPresent(self.name, forKey: .name)
            try container.encodeIfPresent(self.provenance, forKey: .provenance)
            request.encodePath(self.recordId, key: "recordId")
            try container.encodeIfPresent(self.recordType, forKey: .recordType)
            try container.encodeIfPresent(self.recordVersion, forKey: .recordVersion)
            request.encodePath(self.registryId, key: "registryId")
            try container.encodeIfPresent(self.triggerSynchronization, forKey: .triggerSynchronization)
        }

        public func validate(name: String) throws {
            try self.description?.validate(name: "\(name).description")
            try self.descriptors?.validate(name: "\(name).descriptors")
            try self.displayName?.validate(name: "\(name).displayName")
            try self.validate(self.name, name: "name", parent: name, max: 255)
            try self.validate(self.name, name: "name", parent: name, min: 1)
            try self.validate(self.name, name: "name", parent: name, pattern: "^[a-zA-Z0-9][a-zA-Z0-9_\\-\\.\\/]*$")
            try self.provenance?.forEach {
                try $0.validate(name: "\(name).provenance[]")
            }
            try self.validate(self.provenance, name: "provenance", parent: name, max: 1)
            try self.validate(self.recordId, name: "recordId", parent: name, max: 2048)
            try self.validate(self.recordId, name: "recordId", parent: name, min: 1)
            try self.validate(self.recordId, name: "recordId", parent: name, pattern: "^(arn:aws(-[^:]+)?:agent-registry:[a-z0-9-]+:[0-9]{12}:registry/[a-zA-Z0-9]{12,16}/record/)?[a-zA-Z0-9]{12}$")
            try self.validate(self.recordVersion, name: "recordVersion", parent: name, max: 255)
            try self.validate(self.recordVersion, name: "recordVersion", parent: name, min: 1)
            try self.validate(self.recordVersion, name: "recordVersion", parent: name, pattern: "^[a-zA-Z0-9.-]+$")
            try self.validate(self.registryId, name: "registryId", parent: name, max: 2048)
            try self.validate(self.registryId, name: "registryId", parent: name, min: 1)
            try self.validate(self.registryId, name: "registryId", parent: name, pattern: "^(arn:aws(-[^:]+)?:agent-registry:[a-z0-9-]+:[0-9]{12}:registry/)?[a-zA-Z0-9]{12,16}$")
        }

        private enum CodingKeys: String, CodingKey {
            case description = "description"
            case descriptors = "descriptors"
            case displayName = "displayName"
            case name = "name"
            case provenance = "provenance"
            case recordType = "recordType"
            case recordVersion = "recordVersion"
            case triggerSynchronization = "triggerSynchronization"
        }
    }

    public struct UpdateRegistryRecordResponse: AWSDecodableShape {
        /// The timestamp when the registry record was created.
        @CustomCoding<ISO8601DateCoder>
        public var createdAt: Date
        /// The ID of the Amazon Web Services account that created the registry record.
        public let createdBy: String?
        /// Specifies whether the registry record was created by auto-detection. true indicates the record was automatically created by the service based on the registry's auto-detection configuration; false indicates the record was created through a control-plane API call.
        public let createdByAutoDetection: Bool?
        /// A description of the registry record.
        public let description: String?
        /// The typed descriptors that define the content of the registry record.
        public let descriptors: Descriptors?
        /// The human-readable display name of the registry record.
        public let displayName: String?
        /// The name of the registry record. Names are unique within a registry.
        public let name: String
        public let provenance: [Provenance]?
        /// The Amazon Resource Name (ARN) of the registry record.
        public let recordArn: String
        /// The unique identifier of the registry record.
        public let recordId: String
        /// The type of the registry record, such as MCP, AGENT, SKILL, or CUSTOM.
        public let recordType: RecordType
        /// The version identifier of the registry record.
        public let recordVersion: String?
        /// The Amazon Resource Name (ARN) of the parent registry that owns the record.
        public let registryArn: String
        /// The lifecycle status of the registry record.
        public let status: RegistryRecordStatus
        /// The reason for the current status. Typically populated when the status indicates a failure state.
        public let statusReason: String?
        /// The timestamp when the registry record was last updated.
        @CustomCoding<ISO8601DateCoder>
        public var updatedAt: Date

        @inlinable
        public init(createdAt: Date, createdBy: String? = nil, createdByAutoDetection: Bool? = nil, description: String? = nil, descriptors: Descriptors? = nil, displayName: String? = nil, name: String, provenance: [Provenance]? = nil, recordArn: String, recordId: String, recordType: RecordType, recordVersion: String? = nil, registryArn: String, status: RegistryRecordStatus, statusReason: String? = nil, updatedAt: Date) {
            self.createdAt = createdAt
            self.createdBy = createdBy
            self.createdByAutoDetection = createdByAutoDetection
            self.description = description
            self.descriptors = descriptors
            self.displayName = displayName
            self.name = name
            self.provenance = provenance
            self.recordArn = recordArn
            self.recordId = recordId
            self.recordType = recordType
            self.recordVersion = recordVersion
            self.registryArn = registryArn
            self.status = status
            self.statusReason = statusReason
            self.updatedAt = updatedAt
        }

        private enum CodingKeys: String, CodingKey {
            case createdAt = "createdAt"
            case createdBy = "createdBy"
            case createdByAutoDetection = "createdByAutoDetection"
            case description = "description"
            case descriptors = "descriptors"
            case displayName = "displayName"
            case name = "name"
            case provenance = "provenance"
            case recordArn = "recordArn"
            case recordId = "recordId"
            case recordType = "recordType"
            case recordVersion = "recordVersion"
            case registryArn = "registryArn"
            case status = "status"
            case statusReason = "statusReason"
            case updatedAt = "updatedAt"
        }
    }

    public struct UpdateRegistryRecordStatusRequest: AWSEncodableShape {
        /// The identifier of the registry record to update the status of (ARN or ID)
        public let recordId: String
        /// The identifier of the registry containing the record (ARN or ID)
        public let registryId: String
        /// The target status for the registry record
        public let status: RegistryRecordStatus
        /// The reason for the status change, for example why the record was approved, rejected, or deprecated
        public let statusReason: String

        @inlinable
        public init(recordId: String, registryId: String, status: RegistryRecordStatus, statusReason: String) {
            self.recordId = recordId
            self.registryId = registryId
            self.status = status
            self.statusReason = statusReason
        }

        public func encode(to encoder: Encoder) throws {
            let request = encoder.userInfo[.awsRequest]! as! RequestEncodingContainer
            var container = encoder.container(keyedBy: CodingKeys.self)
            request.encodePath(self.recordId, key: "recordId")
            request.encodePath(self.registryId, key: "registryId")
            try container.encode(self.status, forKey: .status)
            try container.encode(self.statusReason, forKey: .statusReason)
        }

        public func validate(name: String) throws {
            try self.validate(self.recordId, name: "recordId", parent: name, max: 2048)
            try self.validate(self.recordId, name: "recordId", parent: name, min: 1)
            try self.validate(self.recordId, name: "recordId", parent: name, pattern: "^(arn:aws(-[^:]+)?:agent-registry:[a-z0-9-]+:[0-9]{12}:registry/[a-zA-Z0-9]{12,16}/record/)?[a-zA-Z0-9]{12}$")
            try self.validate(self.registryId, name: "registryId", parent: name, max: 2048)
            try self.validate(self.registryId, name: "registryId", parent: name, min: 1)
            try self.validate(self.registryId, name: "registryId", parent: name, pattern: "^(arn:aws(-[^:]+)?:agent-registry:[a-z0-9-]+:[0-9]{12}:registry/)?[a-zA-Z0-9]{12,16}$")
        }

        private enum CodingKeys: String, CodingKey {
            case status = "status"
            case statusReason = "statusReason"
        }
    }

    public struct UpdateRegistryRecordStatusResponse: AWSDecodableShape {
        /// The ARN of the registry record
        public let recordArn: String
        /// The ID of the registry record
        public let recordId: String
        /// The ARN of the registry
        public let registryArn: String
        /// The resulting status of the registry record
        public let status: RegistryRecordStatus
        /// The reason for the status change
        public let statusReason: String
        /// The timestamp when the record was last updated
        @CustomCoding<ISO8601DateCoder>
        public var updatedAt: Date

        @inlinable
        public init(recordArn: String, recordId: String, registryArn: String, status: RegistryRecordStatus, statusReason: String, updatedAt: Date) {
            self.recordArn = recordArn
            self.recordId = recordId
            self.registryArn = registryArn
            self.status = status
            self.statusReason = statusReason
            self.updatedAt = updatedAt
        }

        private enum CodingKeys: String, CodingKey {
            case recordArn = "recordArn"
            case recordId = "recordId"
            case registryArn = "registryArn"
            case status = "status"
            case statusReason = "statusReason"
            case updatedAt = "updatedAt"
        }
    }

    public struct UpdateRegistryRequest: AWSEncodableShape {
        /// The updated approval configuration. The change applies only to records that move to PENDING_APPROVAL after the update; records already in PENDING_APPROVAL are unaffected.
        public let approvalConfiguration: UpdatedApprovalConfiguration?
        /// The updated auto-detection configuration for the registry, with PATCH semantics. Omit this field to leave the current configuration unchanged. Supply an empty wrapper to unset it. Supply optionalValue to replace it.
        public let autoDetectionConfiguration: UpdatedAutoDetectionConfiguration?
        /// The updated description of the registry
        public let description: UpdatedDescription?
        /// The updated discovery configuration. Changing the discovery authorization can break existing consumers that rely on the previous authorization type.
        public let discoveryConfiguration: UpdatedDiscoveryConfiguration?
        /// The updated name of the registry
        public let name: String?
        /// The identifier of the registry to update (ARN or ID)
        public let registryId: String

        @inlinable
        public init(approvalConfiguration: UpdatedApprovalConfiguration? = nil, autoDetectionConfiguration: UpdatedAutoDetectionConfiguration? = nil, description: UpdatedDescription? = nil, discoveryConfiguration: UpdatedDiscoveryConfiguration? = nil, name: String? = nil, registryId: String) {
            self.approvalConfiguration = approvalConfiguration
            self.autoDetectionConfiguration = autoDetectionConfiguration
            self.description = description
            self.discoveryConfiguration = discoveryConfiguration
            self.name = name
            self.registryId = registryId
        }

        public func encode(to encoder: Encoder) throws {
            let request = encoder.userInfo[.awsRequest]! as! RequestEncodingContainer
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encodeIfPresent(self.approvalConfiguration, forKey: .approvalConfiguration)
            try container.encodeIfPresent(self.autoDetectionConfiguration, forKey: .autoDetectionConfiguration)
            try container.encodeIfPresent(self.description, forKey: .description)
            try container.encodeIfPresent(self.discoveryConfiguration, forKey: .discoveryConfiguration)
            try container.encodeIfPresent(self.name, forKey: .name)
            request.encodePath(self.registryId, key: "registryId")
        }

        public func validate(name: String) throws {
            try self.approvalConfiguration?.validate(name: "\(name).approvalConfiguration")
            try self.description?.validate(name: "\(name).description")
            try self.discoveryConfiguration?.validate(name: "\(name).discoveryConfiguration")
            try self.validate(self.name, name: "name", parent: name, max: 64)
            try self.validate(self.name, name: "name", parent: name, min: 1)
            try self.validate(self.name, name: "name", parent: name, pattern: "^[a-zA-Z0-9][a-zA-Z0-9_\\-\\.\\/]*$")
            try self.validate(self.registryId, name: "registryId", parent: name, max: 2048)
            try self.validate(self.registryId, name: "registryId", parent: name, min: 1)
            try self.validate(self.registryId, name: "registryId", parent: name, pattern: "^(arn:aws(-[^:]+)?:agent-registry:[a-z0-9-]+:[0-9]{12}:registry/)?[a-zA-Z0-9]{12,16}$")
        }

        private enum CodingKeys: String, CodingKey {
            case approvalConfiguration = "approvalConfiguration"
            case autoDetectionConfiguration = "autoDetectionConfiguration"
            case description = "description"
            case discoveryConfiguration = "discoveryConfiguration"
            case name = "name"
        }
    }

    public struct UpdateRegistryResponse: AWSDecodableShape {
        /// Approval configuration for registry records
        public let approvalConfiguration: ApprovalConfiguration?
        /// The registry's auto-detection properties, including the requested configuration and the current detection status. Present only when auto-detection was configured for the registry.
        public let autoDetection: AutoDetection?
        /// The timestamp when the registry was created
        @CustomCoding<ISO8601DateCoder>
        public var createdAt: Date
        /// The description of the registry
        public let description: String?
        /// Discovery configuration for the registry
        public let discoveryConfiguration: DiscoveryConfiguration?
        /// The server-side encryption configuration for the registry. Appears only when a customer-managed Amazon Web Services KMS key encrypts the registry.
        public let encryptionConfiguration: EncryptionConfiguration?
        /// The name of the registry
        public let name: String
        /// The ARN of the registry
        public let registryArn: String
        /// The unique identifier of the registry
        public let registryId: String
        /// Current status of the registry
        public let status: RegistryStatus
        /// The reason for the current status. Typically populated when the status indicates a failure state.
        public let statusReason: String?
        /// The timestamp when the registry was last updated
        @CustomCoding<ISO8601DateCoder>
        public var updatedAt: Date

        @inlinable
        public init(approvalConfiguration: ApprovalConfiguration? = nil, autoDetection: AutoDetection? = nil, createdAt: Date, description: String? = nil, discoveryConfiguration: DiscoveryConfiguration? = nil, encryptionConfiguration: EncryptionConfiguration? = nil, name: String, registryArn: String, registryId: String, status: RegistryStatus, statusReason: String? = nil, updatedAt: Date) {
            self.approvalConfiguration = approvalConfiguration
            self.autoDetection = autoDetection
            self.createdAt = createdAt
            self.description = description
            self.discoveryConfiguration = discoveryConfiguration
            self.encryptionConfiguration = encryptionConfiguration
            self.name = name
            self.registryArn = registryArn
            self.registryId = registryId
            self.status = status
            self.statusReason = statusReason
            self.updatedAt = updatedAt
        }

        private enum CodingKeys: String, CodingKey {
            case approvalConfiguration = "approvalConfiguration"
            case autoDetection = "autoDetection"
            case createdAt = "createdAt"
            case description = "description"
            case discoveryConfiguration = "discoveryConfiguration"
            case encryptionConfiguration = "encryptionConfiguration"
            case name = "name"
            case registryArn = "registryArn"
            case registryId = "registryId"
            case status = "status"
            case statusReason = "statusReason"
            case updatedAt = "updatedAt"
        }
    }

    public struct UpdatedA2aAgentCardDescriptor: AWSEncodableShape {
        /// The value to set for this field. Omit the wrapper to leave the field unchanged.
        public let optionalValue: UpdatedA2aAgentCardDescriptorFields?

        @inlinable
        public init(optionalValue: UpdatedA2aAgentCardDescriptorFields? = nil) {
            self.optionalValue = optionalValue
        }

        public func validate(name: String) throws {
            try self.optionalValue?.validate(name: "\(name).optionalValue")
        }

        private enum CodingKeys: String, CodingKey {
            case optionalValue = "optionalValue"
        }
    }

    public struct UpdatedA2aAgentCardDescriptorFields: AWSEncodableShape {
        /// The patch for the descriptor's data field.
        public let data: UpdatedDescriptorData?
        /// The patch for the descriptor's data schema version field.
        public let dataSchemaVersion: UpdatedDataSchemaVersion?
        /// The patch for the descriptor's source field.
        public let source: UpdatedDescriptorSource?

        @inlinable
        public init(data: UpdatedDescriptorData? = nil, dataSchemaVersion: UpdatedDataSchemaVersion? = nil, source: UpdatedDescriptorSource? = nil) {
            self.data = data
            self.dataSchemaVersion = dataSchemaVersion
            self.source = source
        }

        public func validate(name: String) throws {
            try self.data?.validate(name: "\(name).data")
            try self.dataSchemaVersion?.validate(name: "\(name).dataSchemaVersion")
            try self.source?.validate(name: "\(name).source")
        }

        private enum CodingKeys: String, CodingKey {
            case data = "data"
            case dataSchemaVersion = "dataSchemaVersion"
            case source = "source"
        }
    }

    public struct UpdatedAgUiDescriptor: AWSEncodableShape {
        /// The value to set for this field. Omit the wrapper to leave the field unchanged.
        public let optionalValue: UpdatedAgUiDescriptorFields?

        @inlinable
        public init(optionalValue: UpdatedAgUiDescriptorFields? = nil) {
            self.optionalValue = optionalValue
        }

        public func validate(name: String) throws {
            try self.optionalValue?.validate(name: "\(name).optionalValue")
        }

        private enum CodingKeys: String, CodingKey {
            case optionalValue = "optionalValue"
        }
    }

    public struct UpdatedAgUiDescriptorFields: AWSEncodableShape {
        /// The patch for the descriptor's source field.
        public let source: UpdatedDescriptorSource?

        @inlinable
        public init(source: UpdatedDescriptorSource? = nil) {
            self.source = source
        }

        public func validate(name: String) throws {
            try self.source?.validate(name: "\(name).source")
        }

        private enum CodingKeys: String, CodingKey {
            case source = "source"
        }
    }

    public struct UpdatedAgentSkillsAdditionalData: AWSEncodableShape {
        /// The value to set for this field. Omit the wrapper to leave the field unchanged.
        public let optionalValue: UpdatedAgentSkillsAdditionalDataFields?

        @inlinable
        public init(optionalValue: UpdatedAgentSkillsAdditionalDataFields? = nil) {
            self.optionalValue = optionalValue
        }

        public func validate(name: String) throws {
            try self.optionalValue?.validate(name: "\(name).optionalValue")
        }

        private enum CodingKeys: String, CodingKey {
            case optionalValue = "optionalValue"
        }
    }

    public struct UpdatedAgentSkillsAdditionalDataFields: AWSEncodableShape {
        /// The patch for the agent skills markdown descriptor field.
        public let skillMd: UpdatedAgentSkillsMdDescriptor?

        @inlinable
        public init(skillMd: UpdatedAgentSkillsMdDescriptor? = nil) {
            self.skillMd = skillMd
        }

        public func validate(name: String) throws {
            try self.skillMd?.validate(name: "\(name).skillMd")
        }

        private enum CodingKeys: String, CodingKey {
            case skillMd = "skillMd"
        }
    }

    public struct UpdatedAgentSkillsDefinitionDescriptor: AWSEncodableShape {
        /// The value to set for this field. Omit the wrapper to leave the field unchanged.
        public let optionalValue: UpdatedAgentSkillsDefinitionDescriptorFields?

        @inlinable
        public init(optionalValue: UpdatedAgentSkillsDefinitionDescriptorFields? = nil) {
            self.optionalValue = optionalValue
        }

        public func validate(name: String) throws {
            try self.optionalValue?.validate(name: "\(name).optionalValue")
        }

        private enum CodingKeys: String, CodingKey {
            case optionalValue = "optionalValue"
        }
    }

    public struct UpdatedAgentSkillsDefinitionDescriptorFields: AWSEncodableShape {
        /// The patch for the descriptor's additional data field.
        public let additionalData: UpdatedAgentSkillsAdditionalData?
        /// The patch for the descriptor's data field.
        public let data: UpdatedDescriptorData?
        /// The patch for the descriptor's data schema version field.
        public let dataSchemaVersion: UpdatedDataSchemaVersion?

        @inlinable
        public init(additionalData: UpdatedAgentSkillsAdditionalData? = nil, data: UpdatedDescriptorData? = nil, dataSchemaVersion: UpdatedDataSchemaVersion? = nil) {
            self.additionalData = additionalData
            self.data = data
            self.dataSchemaVersion = dataSchemaVersion
        }

        public func validate(name: String) throws {
            try self.additionalData?.validate(name: "\(name).additionalData")
            try self.data?.validate(name: "\(name).data")
            try self.dataSchemaVersion?.validate(name: "\(name).dataSchemaVersion")
        }

        private enum CodingKeys: String, CodingKey {
            case additionalData = "additionalData"
            case data = "data"
            case dataSchemaVersion = "dataSchemaVersion"
        }
    }

    public struct UpdatedAgentSkillsMdDescriptor: AWSEncodableShape {
        /// The value to set for this field. Omit the wrapper to leave the field unchanged.
        public let optionalValue: UpdatedAgentSkillsMdDescriptorFields?

        @inlinable
        public init(optionalValue: UpdatedAgentSkillsMdDescriptorFields? = nil) {
            self.optionalValue = optionalValue
        }

        public func validate(name: String) throws {
            try self.optionalValue?.validate(name: "\(name).optionalValue")
        }

        private enum CodingKeys: String, CodingKey {
            case optionalValue = "optionalValue"
        }
    }

    public struct UpdatedAgentSkillsMdDescriptorFields: AWSEncodableShape {
        /// The patch for the descriptor's data field.
        public let data: UpdatedDescriptorData?
        /// The patch for the descriptor's data schema version field.
        public let dataSchemaVersion: UpdatedDataSchemaVersion?
        /// The patch for the descriptor's source field.
        public let source: UpdatedDescriptorSource?

        @inlinable
        public init(data: UpdatedDescriptorData? = nil, dataSchemaVersion: UpdatedDataSchemaVersion? = nil, source: UpdatedDescriptorSource? = nil) {
            self.data = data
            self.dataSchemaVersion = dataSchemaVersion
            self.source = source
        }

        public func validate(name: String) throws {
            try self.data?.validate(name: "\(name).data")
            try self.dataSchemaVersion?.validate(name: "\(name).dataSchemaVersion")
            try self.source?.validate(name: "\(name).source")
        }

        private enum CodingKeys: String, CodingKey {
            case data = "data"
            case dataSchemaVersion = "dataSchemaVersion"
            case source = "source"
        }
    }

    public struct UpdatedApprovalConfiguration: AWSEncodableShape {
        /// The value to set for this field. Omit the wrapper to leave the field unchanged.
        public let optionalValue: ApprovalConfiguration?

        @inlinable
        public init(optionalValue: ApprovalConfiguration? = nil) {
            self.optionalValue = optionalValue
        }

        public func validate(name: String) throws {
            try self.optionalValue?.validate(name: "\(name).optionalValue")
        }

        private enum CodingKeys: String, CodingKey {
            case optionalValue = "optionalValue"
        }
    }

    public struct UpdatedAuthorizerConfiguration: AWSEncodableShape {
        /// The new authorizer configuration to set. Omit to leave the existing configuration unchanged.
        public let optionalValue: AuthorizerConfiguration?

        @inlinable
        public init(optionalValue: AuthorizerConfiguration? = nil) {
            self.optionalValue = optionalValue
        }

        public func validate(name: String) throws {
            try self.optionalValue?.validate(name: "\(name).optionalValue")
        }

        private enum CodingKeys: String, CodingKey {
            case optionalValue = "optionalValue"
        }
    }

    public struct UpdatedAutoDetectionConfiguration: AWSEncodableShape {
        /// The value to set for this field. Omit the wrapper to leave the field unchanged.
        public let optionalValue: AutoDetectionConfiguration?

        @inlinable
        public init(optionalValue: AutoDetectionConfiguration? = nil) {
            self.optionalValue = optionalValue
        }

        private enum CodingKeys: String, CodingKey {
            case optionalValue = "optionalValue"
        }
    }

    public struct UpdatedCustomDescriptor: AWSEncodableShape {
        /// The value to set for this field. Omit the wrapper to leave the field unchanged.
        public let optionalValue: UpdatedCustomDescriptorFields?

        @inlinable
        public init(optionalValue: UpdatedCustomDescriptorFields? = nil) {
            self.optionalValue = optionalValue
        }

        public func validate(name: String) throws {
            try self.optionalValue?.validate(name: "\(name).optionalValue")
        }

        private enum CodingKeys: String, CodingKey {
            case optionalValue = "optionalValue"
        }
    }

    public struct UpdatedCustomDescriptorFields: AWSEncodableShape {
        /// The patch for the descriptor's data field.
        public let data: UpdatedDescriptorData?

        @inlinable
        public init(data: UpdatedDescriptorData? = nil) {
            self.data = data
        }

        public func validate(name: String) throws {
            try self.data?.validate(name: "\(name).data")
        }

        private enum CodingKeys: String, CodingKey {
            case data = "data"
        }
    }

    public struct UpdatedDataSchemaVersion: AWSEncodableShape {
        /// The value to set for this field. Omit the wrapper to leave the field unchanged.
        public let optionalValue: String?

        @inlinable
        public init(optionalValue: String? = nil) {
            self.optionalValue = optionalValue
        }

        public func validate(name: String) throws {
            try self.validate(self.optionalValue, name: "optionalValue", parent: name, max: 255)
            try self.validate(self.optionalValue, name: "optionalValue", parent: name, min: 1)
        }

        private enum CodingKeys: String, CodingKey {
            case optionalValue = "optionalValue"
        }
    }

    public struct UpdatedDescription: AWSEncodableShape {
        /// The value to set for this field. Omit the wrapper to leave the field unchanged.
        public let optionalValue: String?

        @inlinable
        public init(optionalValue: String? = nil) {
            self.optionalValue = optionalValue
        }

        public func validate(name: String) throws {
            try self.validate(self.optionalValue, name: "optionalValue", parent: name, max: 4096)
            try self.validate(self.optionalValue, name: "optionalValue", parent: name, min: 1)
        }

        private enum CodingKeys: String, CodingKey {
            case optionalValue = "optionalValue"
        }
    }

    public struct UpdatedDescriptorData: AWSEncodableShape {
        /// The value to set for this field. Omit the wrapper to leave the field unchanged.
        public let optionalValue: String?

        @inlinable
        public init(optionalValue: String? = nil) {
            self.optionalValue = optionalValue
        }

        public func validate(name: String) throws {
            try self.validate(self.optionalValue, name: "optionalValue", parent: name, max: 102400)
            try self.validate(self.optionalValue, name: "optionalValue", parent: name, min: 1)
        }

        private enum CodingKeys: String, CodingKey {
            case optionalValue = "optionalValue"
        }
    }

    public struct UpdatedDescriptorSource: AWSEncodableShape {
        /// The value to set for this field. Omit the wrapper to leave the field unchanged.
        public let optionalValue: DescriptorSource?

        @inlinable
        public init(optionalValue: DescriptorSource? = nil) {
            self.optionalValue = optionalValue
        }

        public func validate(name: String) throws {
            try self.optionalValue?.validate(name: "\(name).optionalValue")
        }

        private enum CodingKeys: String, CodingKey {
            case optionalValue = "optionalValue"
        }
    }

    public struct UpdatedDescriptors: AWSEncodableShape {
        /// The value to set for this field. Omit the wrapper to leave the field unchanged.
        public let optionalValue: UpdatedDescriptorsFields?

        @inlinable
        public init(optionalValue: UpdatedDescriptorsFields? = nil) {
            self.optionalValue = optionalValue
        }

        public func validate(name: String) throws {
            try self.optionalValue?.validate(name: "\(name).optionalValue")
        }

        private enum CodingKeys: String, CodingKey {
            case optionalValue = "optionalValue"
        }
    }

    public struct UpdatedDescriptorsFields: AWSEncodableShape {
        /// The patch for the A2A agent card descriptor.
        public let a2aAgentCard: UpdatedA2aAgentCardDescriptor?
        /// The patch for the agent skills definition descriptor.
        public let agentSkillsDefinition: UpdatedAgentSkillsDefinitionDescriptor?
        /// The patch for the AG-UI descriptor.
        public let agui: UpdatedAgUiDescriptor?
        /// The patch for the custom descriptor.
        public let custom: UpdatedCustomDescriptor?
        /// The patch for the HTTP descriptor.
        public let http: UpdatedHttpDescriptor?
        /// The patch for the MCP server descriptor.
        public let mcpServer: UpdatedMcpServerDescriptor?

        @inlinable
        public init(a2aAgentCard: UpdatedA2aAgentCardDescriptor? = nil, agentSkillsDefinition: UpdatedAgentSkillsDefinitionDescriptor? = nil, agui: UpdatedAgUiDescriptor? = nil, custom: UpdatedCustomDescriptor? = nil, http: UpdatedHttpDescriptor? = nil, mcpServer: UpdatedMcpServerDescriptor? = nil) {
            self.a2aAgentCard = a2aAgentCard
            self.agentSkillsDefinition = agentSkillsDefinition
            self.agui = agui
            self.custom = custom
            self.http = http
            self.mcpServer = mcpServer
        }

        public func validate(name: String) throws {
            try self.a2aAgentCard?.validate(name: "\(name).a2aAgentCard")
            try self.agentSkillsDefinition?.validate(name: "\(name).agentSkillsDefinition")
            try self.agui?.validate(name: "\(name).agui")
            try self.custom?.validate(name: "\(name).custom")
            try self.http?.validate(name: "\(name).http")
            try self.mcpServer?.validate(name: "\(name).mcpServer")
        }

        private enum CodingKeys: String, CodingKey {
            case a2aAgentCard = "a2aAgentCard"
            case agentSkillsDefinition = "agentSkillsDefinition"
            case agui = "agui"
            case custom = "custom"
            case http = "http"
            case mcpServer = "mcpServer"
        }
    }

    public struct UpdatedDiscoveryConfiguration: AWSEncodableShape {
        /// Authorization configuration for the registry, with PATCH semantics
        public let authorizerConfiguration: UpdatedAuthorizerConfiguration?

        @inlinable
        public init(authorizerConfiguration: UpdatedAuthorizerConfiguration? = nil) {
            self.authorizerConfiguration = authorizerConfiguration
        }

        public func validate(name: String) throws {
            try self.authorizerConfiguration?.validate(name: "\(name).authorizerConfiguration")
        }

        private enum CodingKeys: String, CodingKey {
            case authorizerConfiguration = "authorizerConfiguration"
        }
    }

    public struct UpdatedDisplayName: AWSEncodableShape {
        /// The value to set for this field. Omit the wrapper to leave the field unchanged.
        public let optionalValue: String?

        @inlinable
        public init(optionalValue: String? = nil) {
            self.optionalValue = optionalValue
        }

        public func validate(name: String) throws {
            try self.validate(self.optionalValue, name: "optionalValue", parent: name, max: 255)
            try self.validate(self.optionalValue, name: "optionalValue", parent: name, min: 1)
        }

        private enum CodingKeys: String, CodingKey {
            case optionalValue = "optionalValue"
        }
    }

    public struct UpdatedHttpDescriptor: AWSEncodableShape {
        /// The value to set for this field. Omit the wrapper to leave the field unchanged.
        public let optionalValue: UpdatedHttpDescriptorFields?

        @inlinable
        public init(optionalValue: UpdatedHttpDescriptorFields? = nil) {
            self.optionalValue = optionalValue
        }

        public func validate(name: String) throws {
            try self.optionalValue?.validate(name: "\(name).optionalValue")
        }

        private enum CodingKeys: String, CodingKey {
            case optionalValue = "optionalValue"
        }
    }

    public struct UpdatedHttpDescriptorFields: AWSEncodableShape {
        /// The patch for the descriptor's source field.
        public let source: UpdatedDescriptorSource?

        @inlinable
        public init(source: UpdatedDescriptorSource? = nil) {
            self.source = source
        }

        public func validate(name: String) throws {
            try self.source?.validate(name: "\(name).source")
        }

        private enum CodingKeys: String, CodingKey {
            case source = "source"
        }
    }

    public struct UpdatedMcpServerAdditionalData: AWSEncodableShape {
        /// The value to set for this field. Omit the wrapper to leave the field unchanged.
        public let optionalValue: UpdatedMcpServerAdditionalDataFields?

        @inlinable
        public init(optionalValue: UpdatedMcpServerAdditionalDataFields? = nil) {
            self.optionalValue = optionalValue
        }

        public func validate(name: String) throws {
            try self.optionalValue?.validate(name: "\(name).optionalValue")
        }

        private enum CodingKeys: String, CodingKey {
            case optionalValue = "optionalValue"
        }
    }

    public struct UpdatedMcpServerAdditionalDataFields: AWSEncodableShape {
        /// The patch for the MCP tools descriptor field.
        public let tools: UpdatedMcpToolsDescriptor?

        @inlinable
        public init(tools: UpdatedMcpToolsDescriptor? = nil) {
            self.tools = tools
        }

        public func validate(name: String) throws {
            try self.tools?.validate(name: "\(name).tools")
        }

        private enum CodingKeys: String, CodingKey {
            case tools = "tools"
        }
    }

    public struct UpdatedMcpServerDescriptor: AWSEncodableShape {
        /// The value to set for this field. Omit the wrapper to leave the field unchanged.
        public let optionalValue: UpdatedMcpServerDescriptorFields?

        @inlinable
        public init(optionalValue: UpdatedMcpServerDescriptorFields? = nil) {
            self.optionalValue = optionalValue
        }

        public func validate(name: String) throws {
            try self.optionalValue?.validate(name: "\(name).optionalValue")
        }

        private enum CodingKeys: String, CodingKey {
            case optionalValue = "optionalValue"
        }
    }

    public struct UpdatedMcpServerDescriptorFields: AWSEncodableShape {
        /// The patch for the descriptor's additional data field.
        public let additionalData: UpdatedMcpServerAdditionalData?
        /// The patch for the descriptor's data field.
        public let data: UpdatedDescriptorData?
        /// The patch for the descriptor's data schema version field.
        public let dataSchemaVersion: UpdatedDataSchemaVersion?
        /// The patch for the descriptor's source field.
        public let source: UpdatedDescriptorSource?

        @inlinable
        public init(additionalData: UpdatedMcpServerAdditionalData? = nil, data: UpdatedDescriptorData? = nil, dataSchemaVersion: UpdatedDataSchemaVersion? = nil, source: UpdatedDescriptorSource? = nil) {
            self.additionalData = additionalData
            self.data = data
            self.dataSchemaVersion = dataSchemaVersion
            self.source = source
        }

        public func validate(name: String) throws {
            try self.additionalData?.validate(name: "\(name).additionalData")
            try self.data?.validate(name: "\(name).data")
            try self.dataSchemaVersion?.validate(name: "\(name).dataSchemaVersion")
            try self.source?.validate(name: "\(name).source")
        }

        private enum CodingKeys: String, CodingKey {
            case additionalData = "additionalData"
            case data = "data"
            case dataSchemaVersion = "dataSchemaVersion"
            case source = "source"
        }
    }

    public struct UpdatedMcpToolsDescriptor: AWSEncodableShape {
        /// The value to set for this field. Omit the wrapper to leave the field unchanged.
        public let optionalValue: UpdatedMcpToolsDescriptorFields?

        @inlinable
        public init(optionalValue: UpdatedMcpToolsDescriptorFields? = nil) {
            self.optionalValue = optionalValue
        }

        public func validate(name: String) throws {
            try self.optionalValue?.validate(name: "\(name).optionalValue")
        }

        private enum CodingKeys: String, CodingKey {
            case optionalValue = "optionalValue"
        }
    }

    public struct UpdatedMcpToolsDescriptorFields: AWSEncodableShape {
        /// The patch for the descriptor's data field.
        public let data: UpdatedDescriptorData?
        /// The patch for the descriptor's data schema version field.
        public let dataSchemaVersion: UpdatedDataSchemaVersion?

        @inlinable
        public init(data: UpdatedDescriptorData? = nil, dataSchemaVersion: UpdatedDataSchemaVersion? = nil) {
            self.data = data
            self.dataSchemaVersion = dataSchemaVersion
        }

        public func validate(name: String) throws {
            try self.data?.validate(name: "\(name).data")
            try self.dataSchemaVersion?.validate(name: "\(name).dataSchemaVersion")
        }

        private enum CodingKeys: String, CodingKey {
            case data = "data"
            case dataSchemaVersion = "dataSchemaVersion"
        }
    }

    public struct ValidationException: AWSErrorShape {
        /// The list of input fields that failed validation.
        public let fieldList: [ValidationExceptionField]?
        public let message: String
        /// The reason the request failed validation.
        public let reason: ValidationExceptionReason

        @inlinable
        public init(fieldList: [ValidationExceptionField]? = nil, message: String, reason: ValidationExceptionReason) {
            self.fieldList = fieldList
            self.message = message
            self.reason = reason
        }

        private enum CodingKeys: String, CodingKey {
            case fieldList = "fieldList"
            case message = "message"
            case reason = "reason"
        }
    }

    public struct ValidationExceptionField: AWSDecodableShape {
        /// A description of why the field failed validation.
        public let message: String
        /// The name of the field that failed validation.
        public let name: String

        @inlinable
        public init(message: String, name: String) {
            self.message = message
            self.name = name
        }

        private enum CodingKeys: String, CodingKey {
            case message = "message"
            case name = "name"
        }
    }

    public struct WorkloadIdentityDetails: AWSEncodableShape & AWSDecodableShape {
        /// The Amazon Resource Name (ARN) of the workload identity associated with the source resource.
        public let workloadIdentityArn: String

        @inlinable
        public init(workloadIdentityArn: String) {
            self.workloadIdentityArn = workloadIdentityArn
        }

        private enum CodingKeys: String, CodingKey {
            case workloadIdentityArn = "workloadIdentityArn"
        }
    }

    public struct AuthorizerConfiguration: AWSEncodableShape & AWSDecodableShape {
        /// Configuration for a custom JWT authorizer.
        public let customJWTAuthorizer: CustomJWTAuthorizerConfiguration?

        @inlinable
        public init(customJWTAuthorizer: CustomJWTAuthorizerConfiguration? = nil) {
            self.customJWTAuthorizer = customJWTAuthorizer
        }

        public func validate(name: String) throws {
            try self.customJWTAuthorizer?.validate(name: "\(name).customJWTAuthorizer")
        }

        private enum CodingKeys: String, CodingKey {
            case customJWTAuthorizer = "customJWTAuthorizer"
        }
    }

    public struct SelfManagedLatticeResource: AWSEncodableShape & AWSDecodableShape {
        /// The identifier of the VPC Lattice resource configuration, specified as a resource configuration ID or ARN.
        public let resourceConfigurationIdentifier: String?

        @inlinable
        public init(resourceConfigurationIdentifier: String? = nil) {
            self.resourceConfigurationIdentifier = resourceConfigurationIdentifier
        }

        public func validate(name: String) throws {
            try self.validate(self.resourceConfigurationIdentifier, name: "resourceConfigurationIdentifier", parent: name, max: 2048)
            try self.validate(self.resourceConfigurationIdentifier, name: "resourceConfigurationIdentifier", parent: name, min: 20)
            try self.validate(self.resourceConfigurationIdentifier, name: "resourceConfigurationIdentifier", parent: name, pattern: "^((rcfg-[0-9a-z]{17})|(arn:[a-z0-9\\-]+:vpc-lattice:[a-zA-Z0-9\\-]+:\\d{12}:resourceconfiguration/rcfg-[0-9a-z]{17}))$")
        }

        private enum CodingKeys: String, CodingKey {
            case resourceConfigurationIdentifier = "resourceConfigurationIdentifier"
        }
    }
}

// MARK: - Errors

/// Error enum for AgentRegistryControl
public struct AgentRegistryControlErrorType: AWSErrorType {
    enum Code: String {
        case accessDeniedException = "AccessDeniedException"
        case conflictException = "ConflictException"
        case internalServerException = "InternalServerException"
        case resourceNotFoundException = "ResourceNotFoundException"
        case serviceQuotaExceededException = "ServiceQuotaExceededException"
        case throttlingException = "ThrottlingException"
        case validationException = "ValidationException"
    }

    private let error: Code
    public let context: AWSErrorContext?

    /// initialize AgentRegistryControl
    public init?(errorCode: String, context: AWSErrorContext) {
        guard let error = Code(rawValue: errorCode) else { return nil }
        self.error = error
        self.context = context
    }

    internal init(_ error: Code) {
        self.error = error
        self.context = nil
    }

    /// return error code string
    public var errorCode: String { self.error.rawValue }

    /// The caller is not authorized to perform the requested action.
    public static var accessDeniedException: Self { .init(.accessDeniedException) }
    /// The request conflicts with the current state of the resource.
    public static var conflictException: Self { .init(.conflictException) }
    /// The request failed due to an unexpected internal error; the caller may retry.
    public static var internalServerException: Self { .init(.internalServerException) }
    /// The requested resource was not found.
    public static var resourceNotFoundException: Self { .init(.resourceNotFoundException) }
    /// The request would exceed a service quota.
    public static var serviceQuotaExceededException: Self { .init(.serviceQuotaExceededException) }
    /// The request was denied due to request throttling; the caller may retry after a delay.
    public static var throttlingException: Self { .init(.throttlingException) }
    /// The request failed validation of one or more input fields.
    public static var validationException: Self { .init(.validationException) }
}

extension AgentRegistryControlErrorType: AWSServiceErrorType {
    public static let errorCodeMap: [String: AWSErrorShape.Type] = [
        "ValidationException": AgentRegistryControl.ValidationException.self
    ]
}

extension AgentRegistryControlErrorType: Equatable {
    public static func == (lhs: AgentRegistryControlErrorType, rhs: AgentRegistryControlErrorType) -> Bool {
        lhs.error == rhs.error
    }
}

extension AgentRegistryControlErrorType: CustomStringConvertible {
    public var description: String {
        return "\(self.error.rawValue): \(self.message ?? "")"
    }
}
