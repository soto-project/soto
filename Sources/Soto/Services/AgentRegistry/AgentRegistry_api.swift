//===----------------------------------------------------------------------===//
//
// This source file is part of the Soto for AWS open source project
//
// Copyright (c) 2017-2024 the Soto project authors
// Licensed under Apache License v2.0
//
// See LICENSE.txt for license information
// See CONTRIBUTORS.txt for the list of Soto project authors
//
// SPDX-License-Identifier: Apache-2.0
//
//===----------------------------------------------------------------------===//

// THIS FILE IS AUTOMATICALLY GENERATED by https://github.com/soto-project/soto-codegenerator.
// DO NOT EDIT.

#if canImport(FoundationEssentials)
import FoundationEssentials
#else
import Foundation
#endif
@_exported import SotoCore

/// Service object for interacting with AWS AgentRegistry service.
///
/// Agent Registry lets callers discover, list, and retrieve approved records published to a registry.
public struct AgentRegistry: AWSService {
    // MARK: Member variables

    /// Client used for communication with AWS
    public let client: AWSClient
    /// Service configuration
    public let config: AWSServiceConfig

    // MARK: Initialization

    /// Initialize the AgentRegistry client
    /// - parameters:
    ///     - client: AWSClient used to process requests
    ///     - region: Region of server you want to communicate with. This will override the partition parameter.
    ///     - partition: AWS partition where service resides, standard (.aws), china (.awscn), government (.awsusgov).
    ///     - endpoint: Custom endpoint URL to use instead of standard AWS servers
    ///     - middleware: Middleware chain used to edit requests before they are sent and responses before they are decoded 
    ///     - timeout: Timeout value for HTTP requests
    ///     - byteBufferAllocator: Allocator for ByteBuffers
    ///     - options: Service options
    public init(
        client: AWSClient,
        region: SotoCore.Region? = nil,
        partition: AWSPartition = .aws,
        endpoint: String? = nil,
        middleware: AWSMiddlewareProtocol? = nil,
        timeout: TimeAmount? = nil,
        byteBufferAllocator: ByteBufferAllocator = ByteBufferAllocator(),
        options: AWSServiceConfig.Options = []
    ) {
        self.client = client
        self.config = AWSServiceConfig(
            region: region,
            partition: region?.partition ?? partition,
            serviceName: "AgentRegistry",
            serviceIdentifier: "agent-registry",
            serviceProtocol: .restjson,
            apiVersion: "2025-12-01",
            endpoint: endpoint,
            errorType: AgentRegistryErrorType.self,
            middleware: middleware,
            timeout: timeout,
            byteBufferAllocator: byteBufferAllocator,
            options: options
        )
    }





    // MARK: API Calls

    ///  Retrieves multiple discoverable registry records by ID from a single registry. Records that cannot be retrieved are reported individually in the errors list rather than failing the entire request.
    @Sendable
    @inlinable
    public func batchGetDiscoverableRegistryRecord(_ input: BatchGetDiscoverableRegistryRecordRequest, logger: Logger = AWSClient.loggingDisabled) async throws -> BatchGetDiscoverableRegistryRecordResponse {
        try await self.client.execute(
            operation: "BatchGetDiscoverableRegistryRecord", 
            path: "/discoverable-records-batch", 
            httpMethod: .POST, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    ///  Retrieves multiple discoverable registry records by ID from a single registry. Records that cannot be retrieved are reported individually in the errors list rather than failing the entire request.
    ///
    /// Parameters:
    ///   - entries:  The registry-scoped groups of record IDs to retrieve. Currently, you can specify exactly one entry.
    ///   - logger: Logger use during operation
    @inlinable
    public func batchGetDiscoverableRegistryRecord(
        entries: [RegistryRecordsEntry],
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws -> BatchGetDiscoverableRegistryRecordResponse {
        let input = BatchGetDiscoverableRegistryRecordRequest(
            entries: entries
        )
        return try await self.batchGetDiscoverableRegistryRecord(input, logger: logger)
    }

    ///  Lists the discoverable registry records in a registry. You can optionally filter and paginate the results.
    @Sendable
    @inlinable
    public func listDiscoverableRegistryRecords(_ input: ListDiscoverableRegistryRecordsRequest, logger: Logger = AWSClient.loggingDisabled) async throws -> ListDiscoverableRegistryRecordsResponse {
        try await self.client.execute(
            operation: "ListDiscoverableRegistryRecords", 
            path: "/registries/{registryId}/discoverable-records-list", 
            httpMethod: .POST, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    ///  Lists the discoverable registry records in a registry. You can optionally filter and paginate the results.
    ///
    /// Parameters:
    ///   - filters:  The filters to apply to the discoverable registry record list.
    ///   - maxResults:  The maximum number of records to return in a single page. Valid values are 1 through 100.
    ///   - nextToken:  The pagination token returned by a previous request. Use this value to retrieve the next page of results.
    ///   - registryId:  The identifier of the registry whose discoverable records are listed. You can provide either the full Amazon Resource Name (ARN) or the registry ID.
    ///   - logger: Logger use during operation
    @inlinable
    public func listDiscoverableRegistryRecords(
        filters: [RegistryRecordFilter]? = nil,
        maxResults: Int? = nil,
        nextToken: String? = nil,
        registryId: String,
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws -> ListDiscoverableRegistryRecordsResponse {
        let input = ListDiscoverableRegistryRecordsRequest(
            filters: filters, 
            maxResults: maxResults, 
            nextToken: nextToken, 
            registryId: registryId
        )
        return try await self.listDiscoverableRegistryRecords(input, logger: logger)
    }

    ///  Searches the discoverable registry records in a registry using a natural language query. Returns metadata for the matching records ordered by relevance.
    @Sendable
    @inlinable
    public func searchDiscoverableRegistryRecords(_ input: SearchDiscoverableRegistryRecordsRequest, logger: Logger = AWSClient.loggingDisabled) async throws -> SearchDiscoverableRegistryRecordsResponse {
        try await self.client.execute(
            operation: "SearchDiscoverableRegistryRecords", 
            path: "/discoverable-records-search", 
            httpMethod: .POST, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    ///  Searches the discoverable registry records in a registry using a natural language query. Returns metadata for the matching records ordered by relevance.
    ///
    /// Parameters:
    ///   - filters:  An optional structured JSON metadata filter that narrows the search results. Supports the field-level operators $eq, $ne, and $in, and the logical operators $and and $or on filterable fields.
    ///   - maxResults:  The maximum number of results to return. Valid values are 1 through 20. The default value is 10.
    ///   - registryIds:  The registry identifiers to search within. Currently, you must specify exactly one registry identifier. You can provide either the full Amazon Web Services Resource Name (ARN) or the registry ID.
    ///   - searchQuery:  The natural language query to search for matching registry records.
    ///   - logger: Logger use during operation
    @inlinable
    public func searchDiscoverableRegistryRecords(
        filters: AWSDocument? = nil,
        maxResults: Int? = nil,
        registryIds: [String],
        searchQuery: String,
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws -> SearchDiscoverableRegistryRecordsResponse {
        let input = SearchDiscoverableRegistryRecordsRequest(
            filters: filters, 
            maxResults: maxResults, 
            registryIds: registryIds, 
            searchQuery: searchQuery
        )
        return try await self.searchDiscoverableRegistryRecords(input, logger: logger)
    }
}

extension AgentRegistry {
    /// Initializer required by `AWSService.with(middlewares:timeout:byteBufferAllocator:options)`. You are not able to use this initializer directly as there are not public
    /// initializers for `AWSServiceConfig.Patch`. Please use `AWSService.with(middlewares:timeout:byteBufferAllocator:options)` instead.
    public init(from: AgentRegistry, patch: AWSServiceConfig.Patch) {
        self.client = from.client
        self.config = from.config.with(patch: patch)
    }
}

// MARK: Paginators

@available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
extension AgentRegistry {
    /// Return PaginatorSequence for operation ``listDiscoverableRegistryRecords(_:logger:)``.
    ///
    /// - Parameters:
    ///   - input: Input for operation
    ///   - logger: Logger used for logging
    @inlinable
    public func listDiscoverableRegistryRecordsPaginator(
        _ input: ListDiscoverableRegistryRecordsRequest,
        logger: Logger = AWSClient.loggingDisabled
    ) -> AWSClient.PaginatorSequence<ListDiscoverableRegistryRecordsRequest, ListDiscoverableRegistryRecordsResponse> {
        return .init(
            input: input,
            command: self.listDiscoverableRegistryRecords,
            inputKey: \ListDiscoverableRegistryRecordsRequest.nextToken,
            outputKey: \ListDiscoverableRegistryRecordsResponse.nextToken,
            logger: logger
        )
    }
    /// Return PaginatorSequence for operation ``listDiscoverableRegistryRecords(_:logger:)``.
    ///
    /// - Parameters:
    ///   - filters:  The filters to apply to the discoverable registry record list.
    ///   - maxResults:  The maximum number of records to return in a single page. Valid values are 1 through 100.
    ///   - registryId:  The identifier of the registry whose discoverable records are listed. You can provide either the full Amazon Resource Name (ARN) or the registry ID.
    ///   - logger: Logger used for logging
    @inlinable
    public func listDiscoverableRegistryRecordsPaginator(
        filters: [RegistryRecordFilter]? = nil,
        maxResults: Int? = nil,
        registryId: String,
        logger: Logger = AWSClient.loggingDisabled        
    ) -> AWSClient.PaginatorSequence<ListDiscoverableRegistryRecordsRequest, ListDiscoverableRegistryRecordsResponse> {
        let input = ListDiscoverableRegistryRecordsRequest(
            filters: filters, 
            maxResults: maxResults, 
            registryId: registryId
        )
        return self.listDiscoverableRegistryRecordsPaginator(input, logger: logger)
    }
}

extension AgentRegistry.ListDiscoverableRegistryRecordsRequest: AWSPaginateToken {
    @inlinable
    public func usingPaginationToken(_ token: String) -> AgentRegistry.ListDiscoverableRegistryRecordsRequest {
        return .init(
            filters: self.filters,
            maxResults: self.maxResults,
            nextToken: token,
            registryId: self.registryId
        )
    }
}
