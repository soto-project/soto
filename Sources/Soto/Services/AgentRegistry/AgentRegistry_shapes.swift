//===----------------------------------------------------------------------===//
//
// This source file is part of the Soto for AWS open source project
//
// Copyright (c) 2017-2024 the Soto project authors
// Licensed under Apache License v2.0
//
// See LICENSE.txt for license information
// See CONTRIBUTORS.txt for the list of Soto project authors
//
// SPDX-License-Identifier: Apache-2.0
//
//===----------------------------------------------------------------------===//

// THIS FILE IS AUTOMATICALLY GENERATED by https://github.com/soto-project/soto-codegenerator.
// DO NOT EDIT.

#if canImport(FoundationEssentials)
import FoundationEssentials
#else
import Foundation
#endif
@_spi(SotoInternal) import SotoCore

extension AgentRegistry {
    // MARK: Enums

    public enum BatchGetDiscoverableRegistryRecordErrorCode: String, CustomStringConvertible, Codable, Sendable, CodingKeyRepresentable {
        case accessDenied = "ACCESS_DENIED"
        case internalError = "INTERNAL_ERROR"
        case resourceNotFound = "RESOURCE_NOT_FOUND"
        public var description: String { return self.rawValue }
    }

    public enum RecordType: String, CustomStringConvertible, Codable, Sendable, CodingKeyRepresentable {
        case agent = "AGENT"
        case custom = "CUSTOM"
        case gateway = "GATEWAY"
        case mcp = "MCP"
        case skill = "SKILL"
        public var description: String { return self.rawValue }
    }

    public enum RegistryRecordFilterName: String, CustomStringConvertible, Codable, Sendable, CodingKeyRepresentable {
        case descriptorType = "descriptorType"
        ///  Filters records by record type, such as MCP or AGENT.
        case recordType = "recordType"
        public var description: String { return self.rawValue }
    }

    public enum RegistryRecordStatus: String, CustomStringConvertible, Codable, Sendable, CodingKeyRepresentable {
        case approved = "APPROVED"
        case createFailed = "CREATE_FAILED"
        case creating = "CREATING"
        case deprecated = "DEPRECATED"
        case draft = "DRAFT"
        case pendingApproval = "PENDING_APPROVAL"
        case rejected = "REJECTED"
        case updateFailed = "UPDATE_FAILED"
        case updating = "UPDATING"
        public var description: String { return self.rawValue }
    }

    public enum ValidationExceptionReason: String, CustomStringConvertible, Codable, Sendable, CodingKeyRepresentable {
        case cannotParse = "CannotParse"
        case fieldValidationFailed = "FieldValidationFailed"
        case idempotentParameterMismatchException = "IdempotentParameterMismatchException"
        case resourceConflict = "ResourceConflict"
        case rootEventInOtherSession = "EventInOtherSession"
        public var description: String { return self.rawValue }
    }

    // MARK: Shapes

    public struct A2aAgentCardDescriptor: AWSDecodableShape {
        ///  The A2A agent card content, serialized as descriptor payload data.
        public let data: String?
        ///  The schema version of the descriptor payload.
        public let dataSchemaVersion: String?
        ///  The source location from which the A2A (Agent-to-Agent) agent card descriptor content was retrieved.
        public let source: DescriptorSource?

        @inlinable
        public init(data: String? = nil, dataSchemaVersion: String? = nil, source: DescriptorSource? = nil) {
            self.data = data
            self.dataSchemaVersion = dataSchemaVersion
            self.source = source
        }

        private enum CodingKeys: String, CodingKey {
            case data = "data"
            case dataSchemaVersion = "dataSchemaVersion"
            case source = "source"
        }
    }

    public struct AgUiDescriptor: AWSDecodableShape {
        ///  The source location of the AG-UI protocol endpoint.
        public let source: DescriptorSource?

        @inlinable
        public init(source: DescriptorSource? = nil) {
            self.source = source
        }

        private enum CodingKeys: String, CodingKey {
            case source = "source"
        }
    }

    public struct AgentSkillsAdditionalData: AWSDecodableShape {
        ///  The agent skills markdown descriptor associated with the agent skills definition.
        public let skillMd: AgentSkillsMdDescriptor?

        @inlinable
        public init(skillMd: AgentSkillsMdDescriptor? = nil) {
            self.skillMd = skillMd
        }

        private enum CodingKeys: String, CodingKey {
            case skillMd = "skillMd"
        }
    }

    public struct AgentSkillsDefinitionDescriptor: AWSDecodableShape {
        ///  Additional data for the agent skills definition, such as the skills markdown descriptor.
        public let additionalData: AgentSkillsAdditionalData?
        ///  The agent skills definition content, serialized as descriptor payload data.
        public let data: String?
        ///  The schema version of the descriptor payload.
        public let dataSchemaVersion: String?

        @inlinable
        public init(additionalData: AgentSkillsAdditionalData? = nil, data: String? = nil, dataSchemaVersion: String? = nil) {
            self.additionalData = additionalData
            self.data = data
            self.dataSchemaVersion = dataSchemaVersion
        }

        private enum CodingKeys: String, CodingKey {
            case additionalData = "additionalData"
            case data = "data"
            case dataSchemaVersion = "dataSchemaVersion"
        }
    }

    public struct AgentSkillsMdDescriptor: AWSDecodableShape {
        ///  The agent skills markdown content, serialized as descriptor payload data.
        public let data: String?
        ///  The schema version of the descriptor payload.
        public let dataSchemaVersion: String?
        ///  The source location from which the agent skills markdown content was retrieved.
        public let source: DescriptorSource?

        @inlinable
        public init(data: String? = nil, dataSchemaVersion: String? = nil, source: DescriptorSource? = nil) {
            self.data = data
            self.dataSchemaVersion = dataSchemaVersion
            self.source = source
        }

        private enum CodingKeys: String, CodingKey {
            case data = "data"
            case dataSchemaVersion = "dataSchemaVersion"
            case source = "source"
        }
    }

    public struct BatchGetDiscoverableRegistryRecordError: AWSDecodableShape {
        ///  The machine-readable reason that the record could not be retrieved.
        public let errorCode: BatchGetDiscoverableRegistryRecordErrorCode
        ///  An optional human-readable detail about the error. Do not parse this value programmatically.
        public let message: String?
        ///  The identifier of the record that could not be retrieved, echoed from the request in the same format that you supplied (ARN or record ID).
        public let recordId: String
        ///  The identifier of the registry the record was requested from, echoed from the request.
        public let registryId: String

        @inlinable
        public init(errorCode: BatchGetDiscoverableRegistryRecordErrorCode, message: String? = nil, recordId: String, registryId: String) {
            self.errorCode = errorCode
            self.message = message
            self.recordId = recordId
            self.registryId = registryId
        }

        private enum CodingKeys: String, CodingKey {
            case errorCode = "errorCode"
            case message = "message"
            case recordId = "recordId"
            case registryId = "registryId"
        }
    }

    public struct BatchGetDiscoverableRegistryRecordRequest: AWSEncodableShape {
        ///  The registry-scoped groups of record IDs to retrieve. Currently, you can specify exactly one entry.
        public let entries: [RegistryRecordsEntry]

        @inlinable
        public init(entries: [RegistryRecordsEntry]) {
            self.entries = entries
        }

        public func validate(name: String) throws {
            try self.entries.forEach {
                try $0.validate(name: "\(name).entries[]")
            }
        }

        private enum CodingKeys: String, CodingKey {
            case entries = "entries"
        }
    }

    public struct BatchGetDiscoverableRegistryRecordResponse: AWSDecodableShape {
        ///  The per-record errors for records that could not be retrieved. This list is empty when all requested records were returned.
        public let errors: [BatchGetDiscoverableRegistryRecordError]
        ///  The records that were successfully retrieved. Each record correlates to the request by its recordId.
        public let registryRecords: [RegistryRecordSummary]

        @inlinable
        public init(errors: [BatchGetDiscoverableRegistryRecordError], registryRecords: [RegistryRecordSummary]) {
            self.errors = errors
            self.registryRecords = registryRecords
        }

        private enum CodingKeys: String, CodingKey {
            case errors = "errors"
            case registryRecords = "registryRecords"
        }
    }

    public struct CustomDescriptor: AWSDecodableShape {
        /// The custom descriptor content, serialized as descriptor payload data.
        public let data: String?

        @inlinable
        public init(data: String? = nil) {
            self.data = data
        }

        private enum CodingKeys: String, CodingKey {
            case data = "data"
        }
    }

    public struct DescriptorSource: AWSDecodableShape {
        ///  The URL-based descriptor source, populated when descriptor content is synchronized from a URL.
        public let fromUrl: DescriptorSourceFromUrl?

        @inlinable
        public init(fromUrl: DescriptorSourceFromUrl? = nil) {
            self.fromUrl = fromUrl
        }

        private enum CodingKeys: String, CodingKey {
            case fromUrl = "fromUrl"
        }
    }

    public struct DescriptorSourceFromUrl: AWSDecodableShape {
        ///  The URL from which the descriptor content is retrieved.
        public let url: String

        @inlinable
        public init(url: String) {
            self.url = url
        }

        private enum CodingKeys: String, CodingKey {
            case url = "url"
        }
    }

    public struct Descriptors: AWSDecodableShape {
        ///  The A2A agent card descriptor, populated when the record type is AGENT.
        public let a2aAgentCard: A2aAgentCardDescriptor?
        ///  The agent skills definition descriptor, populated when the record type is SKILL.
        public let agentSkillsDefinition: AgentSkillsDefinitionDescriptor?
        ///  The AG-UI descriptor, populated when the record exposes an AG-UI protocol endpoint.
        public let agui: AgUiDescriptor?
        ///  The custom descriptor, populated when the record type is CUSTOM.
        public let custom: CustomDescriptor?
        ///  The HTTP descriptor, populated when the record exposes an HTTP endpoint.
        public let http: HttpDescriptor?
        ///  The MCP server descriptor, populated when the record type is MCP.
        public let mcpServer: McpServerDescriptor?

        @inlinable
        public init(a2aAgentCard: A2aAgentCardDescriptor? = nil, agentSkillsDefinition: AgentSkillsDefinitionDescriptor? = nil, agui: AgUiDescriptor? = nil, custom: CustomDescriptor? = nil, http: HttpDescriptor? = nil, mcpServer: McpServerDescriptor? = nil) {
            self.a2aAgentCard = a2aAgentCard
            self.agentSkillsDefinition = agentSkillsDefinition
            self.agui = agui
            self.custom = custom
            self.http = http
            self.mcpServer = mcpServer
        }

        private enum CodingKeys: String, CodingKey {
            case a2aAgentCard = "a2aAgentCard"
            case agentSkillsDefinition = "agentSkillsDefinition"
            case agui = "agui"
            case custom = "custom"
            case http = "http"
            case mcpServer = "mcpServer"
        }
    }

    public struct DiscoverableRegistryRecordSummary: AWSDecodableShape {
        ///  The timestamp when the registry record was created.
        @CustomCoding<ISO8601DateCoder>
        public var createdAt: Date
        ///  A human-readable description of the registry record. Use this field to explain the record's purpose or content to consumers discovering it in the registry.
        public let description: String?
        ///  The descriptor types that are present on this registry record. Each value corresponds to a descriptor entry key on the approved record.
        public let descriptorTypes: [String]?
        ///  The human-readable display name of the registry record.
        public let displayName: String?
        ///  The name of the registry record. Names are unique within a registry.
        public let name: String
        ///  The Amazon Resource Name (ARN) of the registry record.
        public let recordArn: String
        ///  The unique identifier of the registry record.
        public let recordId: String
        ///  The type of the registry record. MCP is a Model Context Protocol server record, AGENT is an Agent-to-Agent (A2A) agent card record, SKILL is an agent skills definition record, and CUSTOM is a record with a custom descriptor.
        public let recordType: RecordType
        ///  The version identifier of the registry record.
        public let recordVersion: String
        ///  The Amazon Resource Name (ARN) of the parent registry that owns the record.
        public let registryArn: String
        ///  The lifecycle status of the registry record. A record is DRAFT before it is submitted, PENDING_APPROVAL while awaiting curator review, and APPROVED once it is approved and discoverable. REJECTED and DEPRECATED records are not discoverable. The CREATING, UPDATING, CREATE_FAILED, and UPDATE_FAILED values reflect the state of an in-progress or failed asynchronous change.
        public let status: RegistryRecordStatus
        ///  The timestamp when the registry record was last updated.
        @CustomCoding<ISO8601DateCoder>
        public var updatedAt: Date

        @inlinable
        public init(createdAt: Date, description: String? = nil, descriptorTypes: [String]? = nil, displayName: String? = nil, name: String, recordArn: String, recordId: String, recordType: RecordType, recordVersion: String, registryArn: String, status: RegistryRecordStatus, updatedAt: Date) {
            self.createdAt = createdAt
            self.description = description
            self.descriptorTypes = descriptorTypes
            self.displayName = displayName
            self.name = name
            self.recordArn = recordArn
            self.recordId = recordId
            self.recordType = recordType
            self.recordVersion = recordVersion
            self.registryArn = registryArn
            self.status = status
            self.updatedAt = updatedAt
        }

        private enum CodingKeys: String, CodingKey {
            case createdAt = "createdAt"
            case description = "description"
            case descriptorTypes = "descriptorTypes"
            case displayName = "displayName"
            case name = "name"
            case recordArn = "recordArn"
            case recordId = "recordId"
            case recordType = "recordType"
            case recordVersion = "recordVersion"
            case registryArn = "registryArn"
            case status = "status"
            case updatedAt = "updatedAt"
        }
    }

    public struct HttpDescriptor: AWSDecodableShape {
        ///  The source location of the HTTP endpoint.
        public let source: DescriptorSource?

        @inlinable
        public init(source: DescriptorSource? = nil) {
            self.source = source
        }

        private enum CodingKeys: String, CodingKey {
            case source = "source"
        }
    }

    public struct ListDiscoverableRegistryRecordsRequest: AWSEncodableShape {
        ///  The filters to apply to the discoverable registry record list.
        public let filters: [RegistryRecordFilter]?
        ///  The maximum number of records to return in a single page. Valid values are 1 through 100.
        public let maxResults: Int?
        ///  The pagination token returned by a previous request. Use this value to retrieve the next page of results.
        public let nextToken: String?
        ///  The identifier of the registry whose discoverable records are listed. You can provide either the full Amazon Resource Name (ARN) or the registry ID.
        public let registryId: String

        @inlinable
        public init(filters: [RegistryRecordFilter]? = nil, maxResults: Int? = nil, nextToken: String? = nil, registryId: String) {
            self.filters = filters
            self.maxResults = maxResults
            self.nextToken = nextToken
            self.registryId = registryId
        }

        public func encode(to encoder: Encoder) throws {
            let request = encoder.userInfo[.awsRequest]! as! RequestEncodingContainer
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encodeIfPresent(self.filters, forKey: .filters)
            try container.encodeIfPresent(self.maxResults, forKey: .maxResults)
            try container.encodeIfPresent(self.nextToken, forKey: .nextToken)
            request.encodePath(self.registryId, key: "registryId")
        }

        public func validate(name: String) throws {
            try self.filters?.forEach {
                try $0.validate(name: "\(name).filters[]")
            }
            try self.validate(self.filters, name: "filters", parent: name, max: 10)
            try self.validate(self.registryId, name: "registryId", parent: name, max: 2048)
            try self.validate(self.registryId, name: "registryId", parent: name, min: 1)
            try self.validate(self.registryId, name: "registryId", parent: name, pattern: "^(arn:aws(-[^:]+)?:agent-registry:[a-z0-9-]+:[0-9]{12}:registry/)?[a-zA-Z0-9]{12,16}$")
        }

        private enum CodingKeys: String, CodingKey {
            case filters = "filters"
            case maxResults = "maxResults"
            case nextToken = "nextToken"
        }
    }

    public struct ListDiscoverableRegistryRecordsResponse: AWSDecodableShape {
        ///  The pagination token to pass to a subsequent request to retrieve the next page of results. This field is absent when there are no more results.
        public let nextToken: String?
        ///  The page of discoverable registry record summaries.
        public let registryRecords: [DiscoverableRegistryRecordSummary]

        @inlinable
        public init(nextToken: String? = nil, registryRecords: [DiscoverableRegistryRecordSummary]) {
            self.nextToken = nextToken
            self.registryRecords = registryRecords
        }

        private enum CodingKeys: String, CodingKey {
            case nextToken = "nextToken"
            case registryRecords = "registryRecords"
        }
    }

    public struct McpServerAdditionalData: AWSDecodableShape {
        /// The MCP tools descriptor that defines the tools exposed by the MCP server.
        public let tools: McpToolsDescriptor?

        @inlinable
        public init(tools: McpToolsDescriptor? = nil) {
            self.tools = tools
        }

        private enum CodingKeys: String, CodingKey {
            case tools = "tools"
        }
    }

    public struct McpServerDescriptor: AWSDecodableShape {
        ///  Additional data associated with the MCP server descriptor, such as tool definitions.
        public let additionalData: McpServerAdditionalData?
        ///  The MCP server descriptor content, serialized as descriptor payload data.
        public let data: String?
        ///  The schema version of the descriptor payload.
        public let dataSchemaVersion: String?
        ///  The source location from which the MCP (Model Context Protocol) server descriptor content was retrieved.
        public let source: DescriptorSource?

        @inlinable
        public init(additionalData: McpServerAdditionalData? = nil, data: String? = nil, dataSchemaVersion: String? = nil, source: DescriptorSource? = nil) {
            self.additionalData = additionalData
            self.data = data
            self.dataSchemaVersion = dataSchemaVersion
            self.source = source
        }

        private enum CodingKeys: String, CodingKey {
            case additionalData = "additionalData"
            case data = "data"
            case dataSchemaVersion = "dataSchemaVersion"
            case source = "source"
        }
    }

    public struct McpToolsDescriptor: AWSDecodableShape {
        /// The MCP tools descriptor content, serialized as descriptor payload data.
        public let data: String?
        /// The schema version of the descriptor payload.
        public let dataSchemaVersion: String?

        @inlinable
        public init(data: String? = nil, dataSchemaVersion: String? = nil) {
            self.data = data
            self.dataSchemaVersion = dataSchemaVersion
        }

        private enum CodingKeys: String, CodingKey {
            case data = "data"
            case dataSchemaVersion = "dataSchemaVersion"
        }
    }

    public struct RegistryRecordFilter: AWSEncodableShape {
        ///  The attribute to filter on.
        public let name: RegistryRecordFilterName
        ///  The values to match for the attribute.
        public let values: [String]

        @inlinable
        public init(name: RegistryRecordFilterName, values: [String]) {
            self.name = name
            self.values = values
        }

        public func validate(name: String) throws {
            try self.values.forEach {
                try validate($0, name: "values[]", parent: name, max: 255)
                try validate($0, name: "values[]", parent: name, min: 1)
            }
            try self.validate(self.values, name: "values", parent: name, max: 10)
            try self.validate(self.values, name: "values", parent: name, min: 1)
        }

        private enum CodingKeys: String, CodingKey {
            case name = "name"
            case values = "values"
        }
    }

    public struct RegistryRecordSummary: AWSDecodableShape {
        ///  The timestamp when the registry record was created.
        @CustomCoding<ISO8601DateCoder>
        public var createdAt: Date
        ///  A human-readable description of the registry record. Use this field to explain the record's purpose or content to consumers discovering it in the registry.
        public let description: String?
        ///  The protocol-specific descriptors that describe how to connect to and use the record.
        public let descriptors: Descriptors
        ///  The human-readable display name of the registry record.
        public let displayName: String?
        ///  The name of the registry record. Names are unique within a registry.
        public let name: String
        ///  The Amazon Resource Name (ARN) of the registry record.
        public let recordArn: String
        ///  The unique identifier of the registry record.
        public let recordId: String
        ///  The type of the registry record. MCP is a Model Context Protocol server record, AGENT is an Agent-to-Agent (A2A) agent card record, SKILL is an agent skills definition record, and CUSTOM is a record with a custom descriptor.
        public let recordType: RecordType
        ///  The version identifier of the registry record.
        public let recordVersion: String
        ///  The Amazon Resource Name (ARN) of the parent registry that owns the record.
        public let registryArn: String
        ///  The lifecycle status of the registry record. A record is DRAFT before it is submitted, PENDING_APPROVAL while awaiting curator review, and APPROVED once it is approved and discoverable. REJECTED and DEPRECATED records are not discoverable. The CREATING, UPDATING, CREATE_FAILED, and UPDATE_FAILED values reflect the state of an in-progress or failed asynchronous change.
        public let status: RegistryRecordStatus
        ///  The timestamp when the registry record was last updated.
        @CustomCoding<ISO8601DateCoder>
        public var updatedAt: Date

        @inlinable
        public init(createdAt: Date, description: String? = nil, descriptors: Descriptors, displayName: String? = nil, name: String, recordArn: String, recordId: String, recordType: RecordType, recordVersion: String, registryArn: String, status: RegistryRecordStatus, updatedAt: Date) {
            self.createdAt = createdAt
            self.description = description
            self.descriptors = descriptors
            self.displayName = displayName
            self.name = name
            self.recordArn = recordArn
            self.recordId = recordId
            self.recordType = recordType
            self.recordVersion = recordVersion
            self.registryArn = registryArn
            self.status = status
            self.updatedAt = updatedAt
        }

        private enum CodingKeys: String, CodingKey {
            case createdAt = "createdAt"
            case description = "description"
            case descriptors = "descriptors"
            case displayName = "displayName"
            case name = "name"
            case recordArn = "recordArn"
            case recordId = "recordId"
            case recordType = "recordType"
            case recordVersion = "recordVersion"
            case registryArn = "registryArn"
            case status = "status"
            case updatedAt = "updatedAt"
        }
    }

    public struct RegistryRecordsEntry: AWSEncodableShape {
        ///  The record IDs to retrieve from the registry. You can specify 1 through 100 record IDs.
        public let recordIds: [String]
        ///  The identifier of the registry to retrieve the records from. You can provide either the full Amazon Resource Name (ARN) or the registry ID.
        public let registryId: String

        @inlinable
        public init(recordIds: [String], registryId: String) {
            self.recordIds = recordIds
            self.registryId = registryId
        }

        public func validate(name: String) throws {
            try self.recordIds.forEach {
                try validate($0, name: "recordIds[]", parent: name, max: 2048)
                try validate($0, name: "recordIds[]", parent: name, min: 1)
                try validate($0, name: "recordIds[]", parent: name, pattern: "^(arn:aws(-[^:]+)?:agent-registry:[a-z0-9-]+:[0-9]{12}:registry/[a-zA-Z0-9]{12,16}/record/)?[a-zA-Z0-9]{12}$")
            }
            try self.validate(self.registryId, name: "registryId", parent: name, max: 2048)
            try self.validate(self.registryId, name: "registryId", parent: name, min: 1)
            try self.validate(self.registryId, name: "registryId", parent: name, pattern: "^(arn:aws(-[^:]+)?:agent-registry:[a-z0-9-]+:[0-9]{12}:registry/)?[a-zA-Z0-9]{12,16}$")
        }

        private enum CodingKeys: String, CodingKey {
            case recordIds = "recordIds"
            case registryId = "registryId"
        }
    }

    public struct SearchDiscoverableRegistryRecordsRequest: AWSEncodableShape {
        ///  An optional structured JSON metadata filter that narrows the search results. Supports the field-level operators $eq, $ne, and $in, and the logical operators $and and $or on filterable fields.
        public let filters: AWSDocument?
        ///  The maximum number of results to return. Valid values are 1 through 20. The default value is 10.
        public let maxResults: Int?
        ///  The registry identifiers to search within. Currently, you must specify exactly one registry identifier. You can provide either the full Amazon Web Services Resource Name (ARN) or the registry ID.
        public let registryIds: [String]
        ///  The natural language query to search for matching registry records.
        public let searchQuery: String

        @inlinable
        public init(filters: AWSDocument? = nil, maxResults: Int? = nil, registryIds: [String], searchQuery: String) {
            self.filters = filters
            self.maxResults = maxResults
            self.registryIds = registryIds
            self.searchQuery = searchQuery
        }

        public func validate(name: String) throws {
            try self.registryIds.forEach {
                try validate($0, name: "registryIds[]", parent: name, max: 2048)
                try validate($0, name: "registryIds[]", parent: name, min: 1)
                try validate($0, name: "registryIds[]", parent: name, pattern: "^(arn:aws(-[^:]+)?:agent-registry:[a-z0-9-]+:[0-9]{12}:registry/)?[a-zA-Z0-9]{12,16}$")
            }
            try self.validate(self.searchQuery, name: "searchQuery", parent: name, max: 256)
        }

        private enum CodingKeys: String, CodingKey {
            case filters = "filters"
            case maxResults = "maxResults"
            case registryIds = "registryIds"
            case searchQuery = "searchQuery"
        }
    }

    public struct SearchDiscoverableRegistryRecordsResponse: AWSDecodableShape {
        ///  The registry records that match the search query, ordered by relevance.
        public let registryRecords: [RegistryRecordSummary]

        @inlinable
        public init(registryRecords: [RegistryRecordSummary]) {
            self.registryRecords = registryRecords
        }

        private enum CodingKeys: String, CodingKey {
            case registryRecords = "registryRecords"
        }
    }

    public struct ValidationException: AWSErrorShape {
        /// The list of input fields that failed validation.
        public let fieldList: [ValidationExceptionField]?
        public let message: String
        /// The reason the request failed validation.
        public let reason: ValidationExceptionReason

        @inlinable
        public init(fieldList: [ValidationExceptionField]? = nil, message: String, reason: ValidationExceptionReason) {
            self.fieldList = fieldList
            self.message = message
            self.reason = reason
        }

        private enum CodingKeys: String, CodingKey {
            case fieldList = "fieldList"
            case message = "message"
            case reason = "reason"
        }
    }

    public struct ValidationExceptionField: AWSDecodableShape {
        /// A description of why the field failed validation.
        public let message: String
        /// The name of the field that failed validation.
        public let name: String

        @inlinable
        public init(message: String, name: String) {
            self.message = message
            self.name = name
        }

        private enum CodingKeys: String, CodingKey {
            case message = "message"
            case name = "name"
        }
    }
}

// MARK: - Errors

/// Error enum for AgentRegistry
public struct AgentRegistryErrorType: AWSErrorType {
    enum Code: String {
        case accessDeniedException = "AccessDeniedException"
        case internalServerException = "InternalServerException"
        case resourceNotFoundException = "ResourceNotFoundException"
        case throttlingException = "ThrottlingException"
        case unauthorizedException = "UnauthorizedException"
        case validationException = "ValidationException"
    }

    private let error: Code
    public let context: AWSErrorContext?

    /// initialize AgentRegistry
    public init?(errorCode: String, context: AWSErrorContext) {
        guard let error = Code(rawValue: errorCode) else { return nil }
        self.error = error
        self.context = context
    }

    internal init(_ error: Code) {
        self.error = error
        self.context = nil
    }

    /// return error code string
    public var errorCode: String { self.error.rawValue }

    /// The caller is not authorized to perform the requested action.
    public static var accessDeniedException: Self { .init(.accessDeniedException) }
    /// The request failed due to an unexpected internal error; the caller may retry.
    public static var internalServerException: Self { .init(.internalServerException) }
    /// The requested resource was not found.
    public static var resourceNotFoundException: Self { .init(.resourceNotFoundException) }
    /// The request was denied due to request throttling; the caller may retry after a delay.
    public static var throttlingException: Self { .init(.throttlingException) }
    /// The request could not be authenticated.
    public static var unauthorizedException: Self { .init(.unauthorizedException) }
    /// The request failed validation of one or more input fields.
    public static var validationException: Self { .init(.validationException) }
}

extension AgentRegistryErrorType: AWSServiceErrorType {
    public static let errorCodeMap: [String: AWSErrorShape.Type] = [
        "ValidationException": AgentRegistry.ValidationException.self
    ]
}

extension AgentRegistryErrorType: Equatable {
    public static func == (lhs: AgentRegistryErrorType, rhs: AgentRegistryErrorType) -> Bool {
        lhs.error == rhs.error
    }
}

extension AgentRegistryErrorType: CustomStringConvertible {
    public var description: String {
        return "\(self.error.rawValue): \(self.message ?? "")"
    }
}
