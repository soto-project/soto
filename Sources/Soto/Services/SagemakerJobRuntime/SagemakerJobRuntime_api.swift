//===----------------------------------------------------------------------===//
//
// This source file is part of the Soto for AWS open source project
//
// Copyright (c) 2017-2024 the Soto project authors
// Licensed under Apache License v2.0
//
// See LICENSE.txt for license information
// See CONTRIBUTORS.txt for the list of Soto project authors
//
// SPDX-License-Identifier: Apache-2.0
//
//===----------------------------------------------------------------------===//

// THIS FILE IS AUTOMATICALLY GENERATED by https://github.com/soto-project/soto-codegenerator.
// DO NOT EDIT.

#if canImport(FoundationEssentials)
import FoundationEssentials
#else
import Foundation
#endif
@_exported import SotoCore

/// Service object for interacting with AWS SagemakerJobRuntime service.
///
/// Agentic RFT Runtime Service - manages trajectory and transition data
/// for reinforcement fine-tuning jobs.
public struct SagemakerJobRuntime: AWSService {
    // MARK: Member variables

    /// Client used for communication with AWS
    public let client: AWSClient
    /// Service configuration
    public let config: AWSServiceConfig

    // MARK: Initialization

    /// Initialize the SagemakerJobRuntime client
    /// - parameters:
    ///     - client: AWSClient used to process requests
    ///     - region: Region of server you want to communicate with. This will override the partition parameter.
    ///     - partition: AWS partition where service resides, standard (.aws), china (.awscn), government (.awsusgov).
    ///     - endpoint: Custom endpoint URL to use instead of standard AWS servers
    ///     - middleware: Middleware chain used to edit requests before they are sent and responses before they are decoded 
    ///     - timeout: Timeout value for HTTP requests
    ///     - byteBufferAllocator: Allocator for ByteBuffers
    ///     - options: Service options
    public init(
        client: AWSClient,
        region: SotoCore.Region? = nil,
        partition: AWSPartition = .aws,
        endpoint: String? = nil,
        middleware: AWSMiddlewareProtocol? = nil,
        timeout: TimeAmount? = nil,
        byteBufferAllocator: ByteBufferAllocator = ByteBufferAllocator(),
        options: AWSServiceConfig.Options = []
    ) {
        self.client = client
        self.config = AWSServiceConfig(
            region: region,
            partition: region?.partition ?? partition,
            serviceName: "SagemakerJobRuntime",
            serviceIdentifier: "job-runtime.sagemaker",
            signingName: "sagemaker",
            serviceProtocol: .restjson,
            apiVersion: "2026-02-01",
            endpoint: endpoint,
            errorType: SagemakerJobRuntimeErrorType.self,
            middleware: middleware,
            timeout: timeout,
            byteBufferAllocator: byteBufferAllocator,
            options: options
        )
    }





    // MARK: API Calls

    /// Marks a rollout as complete, indicating that no further turns will be appended
    /// to the trajectory. After calling this operation, the trajectory is sealed and
    /// eligible for reward submission via the UpdateReward operation.
    @Sendable
    @inlinable
    public func completeRollout(_ input: CompleteRolloutRequest, logger: Logger = AWSClient.loggingDisabled) async throws -> CompleteRolloutResponse {
        try await self.client.execute(
            operation: "CompleteRollout", 
            path: "/complete-rollout", 
            httpMethod: .POST, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    /// Marks a rollout as complete, indicating that no further turns will be appended
    /// to the trajectory. After calling this operation, the trajectory is sealed and
    /// eligible for reward submission via the UpdateReward operation.
    ///
    /// Parameters:
    ///   - clientToken: A unique, case-sensitive identifier that you provide to ensure the
    ///   - jobArn: The job ARN.
    ///   - status: The target status for the trajectory. Defaults to READY if not specified.
    ///   - trajectoryId: The trajectory ID to mark as complete.
    ///   - logger: Logger use during operation
    @inlinable
    public func completeRollout(
        clientToken: String? = CompleteRolloutRequest.idempotencyToken(),
        jobArn: String,
        status: CompletionStatus? = nil,
        trajectoryId: String,
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws -> CompleteRolloutResponse {
        let input = CompleteRolloutRequest(
            clientToken: clientToken, 
            jobArn: jobArn, 
            status: status, 
            trajectoryId: trajectoryId
        )
        return try await self.completeRollout(input, logger: logger)
    }

    /// Sends an inference request to the model during a job execution. The request
    /// and response bodies are forwarded to and from the model without modification.
    /// Each turn (prompt and response) is captured for later use.
    @Sendable
    @inlinable
    public func sample(_ input: SampleRequest, logger: Logger = AWSClient.loggingDisabled) async throws -> SampleResponse {
        try await self.client.execute(
            operation: "Sample", 
            path: "/sample", 
            httpMethod: .POST, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    /// Sends an inference request to the model during a job execution. The request
    /// and response bodies are forwarded to and from the model without modification.
    /// Each turn (prompt and response) is captured for later use.
    ///
    /// Parameters:
    ///   - body: The raw inference request body in OpenAI-compatible JSON format.
    ///   - jobArn: The job ARN that identifies which model session
    ///   - trajectoryId: The trajectory ID for grouping turns into a single rollout. Each turn
    ///   - logger: Logger use during operation
    @inlinable
    public func sample(
        body: AWSHTTPBody,
        jobArn: String,
        trajectoryId: String,
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws -> SampleResponse {
        let input = SampleRequest(
            body: body, 
            jobArn: jobArn, 
            trajectoryId: trajectoryId
        )
        return try await self.sample(input, logger: logger)
    }

    /// Sends a streaming inference request to the model during a job execution.
    /// Returns the response as a stream of payload chunks. Each turn is captured
    /// for later use.
    @Sendable
    @inlinable
    public func sampleWithResponseStream(_ input: SampleWithResponseStreamRequest, logger: Logger = AWSClient.loggingDisabled) async throws -> SampleWithResponseStreamResponse {
        try await self.client.execute(
            operation: "SampleWithResponseStream", 
            path: "/sample-with-response-stream", 
            httpMethod: .POST, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    /// Sends a streaming inference request to the model during a job execution.
    /// Returns the response as a stream of payload chunks. Each turn is captured
    /// for later use.
    ///
    /// Parameters:
    ///   - body: The raw inference request body in OpenAI-compatible JSON format.
    ///   - jobArn: The job ARN that identifies which model session
    ///   - trajectoryId: The trajectory ID for grouping turns into a single rollout. Each turn
    ///   - logger: Logger use during operation
    @inlinable
    public func sampleWithResponseStream(
        body: AWSHTTPBody,
        jobArn: String,
        trajectoryId: String,
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws -> SampleWithResponseStreamResponse {
        let input = SampleWithResponseStreamRequest(
            body: body, 
            jobArn: jobArn, 
            trajectoryId: trajectoryId
        )
        return try await self.sampleWithResponseStream(input, logger: logger)
    }

    /// Updates the reward values for a trajectory and transitions it to
    /// reward-received status, signaling that it is eligible for processing. Call this
    /// operation after CompleteRollout to provide the computed reward scores.
    @Sendable
    @inlinable
    public func updateReward(_ input: UpdateRewardRequest, logger: Logger = AWSClient.loggingDisabled) async throws -> UpdateRewardResponse {
        try await self.client.execute(
            operation: "UpdateReward", 
            path: "/update-reward", 
            httpMethod: .POST, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    /// Updates the reward values for a trajectory and transitions it to
    /// reward-received status, signaling that it is eligible for processing. Call this
    /// operation after CompleteRollout to provide the computed reward scores.
    ///
    /// Parameters:
    ///   - clientToken: A unique, case-sensitive identifier that you provide to ensure the
    ///   - jobArn: The job ARN.
    ///   - rewards: The list of reward values to assign to this trajectory. Provide one reward
    ///   - trajectoryId: The trajectory ID to update with reward values.
    ///   - logger: Logger use during operation
    @inlinable
    public func updateReward(
        clientToken: String? = UpdateRewardRequest.idempotencyToken(),
        jobArn: String,
        rewards: [Double],
        trajectoryId: String,
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws -> UpdateRewardResponse {
        let input = UpdateRewardRequest(
            clientToken: clientToken, 
            jobArn: jobArn, 
            rewards: rewards, 
            trajectoryId: trajectoryId
        )
        return try await self.updateReward(input, logger: logger)
    }
}

extension SagemakerJobRuntime {
    /// Initializer required by `AWSService.with(middlewares:timeout:byteBufferAllocator:options)`. You are not able to use this initializer directly as there are not public
    /// initializers for `AWSServiceConfig.Patch`. Please use `AWSService.with(middlewares:timeout:byteBufferAllocator:options)` instead.
    public init(from: SagemakerJobRuntime, patch: AWSServiceConfig.Patch) {
        self.client = from.client
        self.config = from.config.with(patch: patch)
    }
}
