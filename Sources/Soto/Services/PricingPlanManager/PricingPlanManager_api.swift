//===----------------------------------------------------------------------===//
//
// This source file is part of the Soto for AWS open source project
//
// Copyright (c) 2017-2024 the Soto project authors
// Licensed under Apache License v2.0
//
// See LICENSE.txt for license information
// See CONTRIBUTORS.txt for the list of Soto project authors
//
// SPDX-License-Identifier: Apache-2.0
//
//===----------------------------------------------------------------------===//

// THIS FILE IS AUTOMATICALLY GENERATED by https://github.com/soto-project/soto-codegenerator.
// DO NOT EDIT.

#if canImport(FoundationEssentials)
import FoundationEssentials
#else
import Foundation
#endif
@_exported import SotoCore

/// Service object for interacting with AWS PricingPlanManager service.
///
/// Manages flat-rate pricing subscriptions for supported services. Use this API to create, approve, update, and cancel subscriptions; associate and disassociate resources; and retrieve subscription details. With a flat-rate pricing subscription, you pay a fixed recurring fee for eligible resources instead of usage-based pricing.
public struct PricingPlanManager: AWSService {
    // MARK: Member variables

    /// Client used for communication with AWS
    public let client: AWSClient
    /// Service configuration
    public let config: AWSServiceConfig

    // MARK: Initialization

    /// Initialize the PricingPlanManager client
    /// - parameters:
    ///     - client: AWSClient used to process requests
    ///     - region: Region of server you want to communicate with. This will override the partition parameter.
    ///     - partition: AWS partition where service resides, standard (.aws), china (.awscn), government (.awsusgov).
    ///     - endpoint: Custom endpoint URL to use instead of standard AWS servers
    ///     - middleware: Middleware chain used to edit requests before they are sent and responses before they are decoded 
    ///     - timeout: Timeout value for HTTP requests
    ///     - byteBufferAllocator: Allocator for ByteBuffers
    ///     - options: Service options
    public init(
        client: AWSClient,
        region: SotoCore.Region? = nil,
        partition: AWSPartition = .aws,
        endpoint: String? = nil,
        middleware: AWSMiddlewareProtocol? = nil,
        timeout: TimeAmount? = nil,
        byteBufferAllocator: ByteBufferAllocator = ByteBufferAllocator(),
        options: AWSServiceConfig.Options = []
    ) {
        self.client = client
        self.config = AWSServiceConfig(
            region: region,
            partition: region?.partition ?? partition,
            serviceName: "PricingPlanManager",
            serviceIdentifier: "pricingplanmanager",
            serviceProtocol: .restjson,
            apiVersion: "2025-08-05",
            endpoint: endpoint,
            errorType: PricingPlanManagerErrorType.self,
            middleware: middleware,
            timeout: timeout,
            byteBufferAllocator: byteBufferAllocator,
            options: options
        )
    }





    // MARK: API Calls

    /// Approves a subscription that is in PENDING_APPROVAL status, activating it and starting billing.  This operation requires the current ETag value for concurrency control. Retrieve it from a previous GetSubscription or ListSubscriptions response.
    @Sendable
    @inlinable
    public func approvePaidSubscription(_ input: ApprovePaidSubscriptionInput, logger: Logger = AWSClient.loggingDisabled) async throws -> ApprovePaidSubscriptionOutput {
        try await self.client.execute(
            operation: "ApprovePaidSubscription", 
            path: "/v1/ApprovePaidSubscription", 
            httpMethod: .POST, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    /// Approves a subscription that is in PENDING_APPROVAL status, activating it and starting billing.  This operation requires the current ETag value for concurrency control. Retrieve it from a previous GetSubscription or ListSubscriptions response.
    ///
    /// Parameters:
    ///   - arn: The ARN of the subscription to approve.
    ///   - clientToken: A unique, case-sensitive identifier that you provide to ensure the request is handled only once.
    ///   - ifMatch: The ETag value from a previous GetSubscription or ListSubscriptions response. This ensures you are approving the expected version of the subscription.
    ///   - logger: Logger use during operation
    @inlinable
    public func approvePaidSubscription(
        arn: String,
        clientToken: String? = ApprovePaidSubscriptionInput.idempotencyToken(),
        ifMatch: String,
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws -> ApprovePaidSubscriptionOutput {
        let input = ApprovePaidSubscriptionInput(
            arn: arn, 
            clientToken: clientToken, 
            ifMatch: ifMatch
        )
        return try await self.approvePaidSubscription(input, logger: logger)
    }

    /// Adds one or more resources to an existing subscription. The subscription must be in an active state that is not pending other changes.  For subscriptions in the CloudFront plan family, the associated resources must include exactly one Amazon CloudFront distribution and one WAF web ACL. You can also include other supported resources, such as Amazon Route 53 hosted zones, and CloudFront KeyValueStores.
    @Sendable
    @inlinable
    public func associateResourcesToSubscription(_ input: AssociateResourcesToSubscriptionInput, logger: Logger = AWSClient.loggingDisabled) async throws -> AssociateResourcesToSubscriptionOutput {
        try await self.client.execute(
            operation: "AssociateResourcesToSubscription", 
            path: "/v1/AssociateResourcesToSubscription", 
            httpMethod: .POST, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    /// Adds one or more resources to an existing subscription. The subscription must be in an active state that is not pending other changes.  For subscriptions in the CloudFront plan family, the associated resources must include exactly one Amazon CloudFront distribution and one WAF web ACL. You can also include other supported resources, such as Amazon Route 53 hosted zones, and CloudFront KeyValueStores.
    ///
    /// Parameters:
    ///   - arn: The ARN of the subscription to add resources to.
    ///   - clientToken: A unique, case-sensitive identifier that you provide to ensure the request is handled only once.
    ///   - ifMatch: The ETag value from a previous GetSubscription or ListSubscriptions response.
    ///   - resourceArns: The ARNs of the resources to add to the subscription.
    ///   - logger: Logger use during operation
    @inlinable
    public func associateResourcesToSubscription(
        arn: String,
        clientToken: String? = AssociateResourcesToSubscriptionInput.idempotencyToken(),
        ifMatch: String,
        resourceArns: [String],
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws -> AssociateResourcesToSubscriptionOutput {
        let input = AssociateResourcesToSubscriptionInput(
            arn: arn, 
            clientToken: clientToken, 
            ifMatch: ifMatch, 
            resourceArns: resourceArns
        )
        return try await self.associateResourcesToSubscription(input, logger: logger)
    }

    /// Cancels a flat-rate pricing subscription.  For active subscriptions, the cancellation is scheduled to take effect at the end of the current billing period. The subscription remains active until that date. To revert a pending cancellation, use CancelSubscriptionChange. For subscriptions in PENDING_APPROVAL status, the subscription is deleted immediately without scheduling.
    @Sendable
    @inlinable
    public func cancelSubscription(_ input: CancelSubscriptionInput, logger: Logger = AWSClient.loggingDisabled) async throws -> CancelSubscriptionOutput {
        try await self.client.execute(
            operation: "CancelSubscription", 
            path: "/v1/CancelSubscription", 
            httpMethod: .POST, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    /// Cancels a flat-rate pricing subscription.  For active subscriptions, the cancellation is scheduled to take effect at the end of the current billing period. The subscription remains active until that date. To revert a pending cancellation, use CancelSubscriptionChange. For subscriptions in PENDING_APPROVAL status, the subscription is deleted immediately without scheduling.
    ///
    /// Parameters:
    ///   - arn: The ARN of the subscription to cancel.
    ///   - clientToken: A unique, case-sensitive identifier that you provide to ensure the request is handled only once.
    ///   - ifMatch: The ETag value from a previous GetSubscription or ListSubscriptions response.
    ///   - logger: Logger use during operation
    @inlinable
    public func cancelSubscription(
        arn: String,
        clientToken: String? = CancelSubscriptionInput.idempotencyToken(),
        ifMatch: String,
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws -> CancelSubscriptionOutput {
        let input = CancelSubscriptionInput(
            arn: arn, 
            clientToken: clientToken, 
            ifMatch: ifMatch
        )
        return try await self.cancelSubscription(input, logger: logger)
    }

    /// Cancels a pending scheduled change on a subscription, such as a pending downgrade or cancellation. The subscription returns to its state before the change was scheduled.  You cannot cancel a scheduled change close to its effective date. If the change is within the processing window, this operation returns an error.
    @Sendable
    @inlinable
    public func cancelSubscriptionChange(_ input: CancelSubscriptionChangeInput, logger: Logger = AWSClient.loggingDisabled) async throws -> CancelSubscriptionChangeOutput {
        try await self.client.execute(
            operation: "CancelSubscriptionChange", 
            path: "/v1/CancelSubscriptionChange", 
            httpMethod: .POST, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    /// Cancels a pending scheduled change on a subscription, such as a pending downgrade or cancellation. The subscription returns to its state before the change was scheduled.  You cannot cancel a scheduled change close to its effective date. If the change is within the processing window, this operation returns an error.
    ///
    /// Parameters:
    ///   - arn: The ARN of the subscription whose pending change you want to cancel.
    ///   - clientToken: A unique, case-sensitive identifier that you provide to ensure the request is handled only once.
    ///   - ifMatch: The ETag value from a previous GetSubscription or ListSubscriptions response.
    ///   - logger: Logger use during operation
    @inlinable
    public func cancelSubscriptionChange(
        arn: String,
        clientToken: String? = CancelSubscriptionChangeInput.idempotencyToken(),
        ifMatch: String,
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws -> CancelSubscriptionChangeOutput {
        let input = CancelSubscriptionChangeInput(
            arn: arn, 
            clientToken: clientToken, 
            ifMatch: ifMatch
        )
        return try await self.cancelSubscriptionChange(input, logger: logger)
    }

    /// Creates a flat-rate pricing subscription for the specified resources.  When approvalMode is set to MANUAL, paid-tier subscriptions are created in PENDING_APPROVAL status and require a separate ApprovePaidSubscription call before billing starts. Free-tier subscriptions are always activated immediately regardless of approval mode. When approvalMode is set to IMMEDIATE or is not specified, the subscription is activated immediately.
    @Sendable
    @inlinable
    public func createSubscription(_ input: CreateSubscriptionInput, logger: Logger = AWSClient.loggingDisabled) async throws -> CreateSubscriptionOutput {
        try await self.client.execute(
            operation: "CreateSubscription", 
            path: "/v1/CreateSubscription", 
            httpMethod: .POST, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    /// Creates a flat-rate pricing subscription for the specified resources.  When approvalMode is set to MANUAL, paid-tier subscriptions are created in PENDING_APPROVAL status and require a separate ApprovePaidSubscription call before billing starts. Free-tier subscriptions are always activated immediately regardless of approval mode. When approvalMode is set to IMMEDIATE or is not specified, the subscription is activated immediately.
    ///
    /// Parameters:
    ///   - approvalMode: Determines whether the subscription requires explicit approval before billing starts. Set to MANUAL to require a separate ApprovePaidSubscription call, or IMMEDIATE to activate the subscription right away. For paid tier plans, this defaults to MANUAL if not specified. For the FREE plan tier, only IMMEDIATE is supported, and it is the default.
    ///   - clientToken: A unique, case-sensitive identifier that you provide to ensure that the request is handled only once. If you send the same request with the same client token, the API returns the original response without creating a duplicate subscription.
    ///   - planFamily: The pricing plan family to subscribe to, such as CloudFront.
    ///   - planTier: The tier level for the subscription, such as FREE, PRO, BUSINESS, or PREMIUM.
    ///   - resourceArns: The ARNs of the resources to include in the subscription. Specify one or more supported resources.  For subscriptions in the CloudFront plan family, the resources must include exactly one Amazon CloudFront distribution and exactly one WAF web ACL. You can also include other supported resources, such as Amazon Route 53 hosted zones and CloudFront KeyValueStores.
    ///   - usageLevel: The usage level within the plan tier. Specify DEFAULT for the base configuration, or a higher level if your plan tier supports it.
    ///   - logger: Logger use during operation
    @inlinable
    public func createSubscription(
        approvalMode: ApprovalMode? = nil,
        clientToken: String? = CreateSubscriptionInput.idempotencyToken(),
        planFamily: String,
        planTier: String,
        resourceArns: [String],
        usageLevel: String? = nil,
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws -> CreateSubscriptionOutput {
        let input = CreateSubscriptionInput(
            approvalMode: approvalMode, 
            clientToken: clientToken, 
            planFamily: planFamily, 
            planTier: planTier, 
            resourceArns: resourceArns, 
            usageLevel: usageLevel
        )
        return try await self.createSubscription(input, logger: logger)
    }

    /// Removes one or more resources from an existing subscription.  For subscriptions in the CloudFront plan family, the associated resources must always include exactly one Amazon CloudFront distribution and exactly one WAF web ACL. You cannot remove these required resources.
    @Sendable
    @inlinable
    public func disassociateResourcesFromSubscription(_ input: DisassociateResourcesFromSubscriptionInput, logger: Logger = AWSClient.loggingDisabled) async throws -> DisassociateResourcesFromSubscriptionOutput {
        try await self.client.execute(
            operation: "DisassociateResourcesFromSubscription", 
            path: "/v1/DisassociateResourcesFromSubscription", 
            httpMethod: .POST, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    /// Removes one or more resources from an existing subscription.  For subscriptions in the CloudFront plan family, the associated resources must always include exactly one Amazon CloudFront distribution and exactly one WAF web ACL. You cannot remove these required resources.
    ///
    /// Parameters:
    ///   - arn: The ARN of the subscription to remove resources from.
    ///   - clientToken: A unique, case-sensitive identifier that you provide to ensure the request is handled only once.
    ///   - ifMatch: The ETag value from a previous GetSubscription or ListSubscriptions response.
    ///   - resourceArns: The ARNs of the resources to remove from the subscription. For subscriptions in the CloudFront plan family, you cannot remove the required CloudFront distribution or WAF web ACL.
    ///   - logger: Logger use during operation
    @inlinable
    public func disassociateResourcesFromSubscription(
        arn: String,
        clientToken: String? = DisassociateResourcesFromSubscriptionInput.idempotencyToken(),
        ifMatch: String,
        resourceArns: [String],
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws -> DisassociateResourcesFromSubscriptionOutput {
        let input = DisassociateResourcesFromSubscriptionInput(
            arn: arn, 
            clientToken: clientToken, 
            ifMatch: ifMatch, 
            resourceArns: resourceArns
        )
        return try await self.disassociateResourcesFromSubscription(input, logger: logger)
    }

    /// Returns the details of a flat-rate pricing subscription, including its current status, associated resources, and any pending scheduled changes.
    @Sendable
    @inlinable
    public func getSubscription(_ input: GetSubscriptionInput, logger: Logger = AWSClient.loggingDisabled) async throws -> GetSubscriptionOutput {
        try await self.client.execute(
            operation: "GetSubscription", 
            path: "/v1/GetSubscription", 
            httpMethod: .POST, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    /// Returns the details of a flat-rate pricing subscription, including its current status, associated resources, and any pending scheduled changes.
    ///
    /// Parameters:
    ///   - arn: The ARN of the subscription to retrieve.
    ///   - logger: Logger use during operation
    @inlinable
    public func getSubscription(
        arn: String,
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws -> GetSubscriptionOutput {
        let input = GetSubscriptionInput(
            arn: arn
        )
        return try await self.getSubscription(input, logger: logger)
    }

    /// Returns a summary of all flat-rate pricing subscriptions in the calling account.
    @Sendable
    @inlinable
    public func listSubscriptions(_ input: ListSubscriptionsInput, logger: Logger = AWSClient.loggingDisabled) async throws -> ListSubscriptionsOutput {
        try await self.client.execute(
            operation: "ListSubscriptions", 
            path: "/v1/ListSubscriptions", 
            httpMethod: .POST, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    /// Returns a summary of all flat-rate pricing subscriptions in the calling account.
    ///
    /// Parameters:
    ///   - nextToken: A token from a previous ListSubscriptions response. If the response included a nextToken, there are more results available. Pass this value to retrieve the next page of results.
    ///   - logger: Logger use during operation
    @inlinable
    public func listSubscriptions(
        nextToken: String? = nil,
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws -> ListSubscriptionsOutput {
        let input = ListSubscriptionsInput(
            nextToken: nextToken
        )
        return try await self.listSubscriptions(input, logger: logger)
    }

    /// Changes the plan tier of an existing subscription.  Upgrades take effect immediately. Downgrades are scheduled and the current tier remains unchanged until the end of the billing cycle (calendar month). You cannot update a subscription while a scheduled change is pending. To make a new change, first cancel the pending change using CancelSubscriptionChange. This operation replaces the plan tier value. If you omit the optional usageLevel field, it is reset to the default.
    @Sendable
    @inlinable
    public func updateSubscription(_ input: UpdateSubscriptionInput, logger: Logger = AWSClient.loggingDisabled) async throws -> UpdateSubscriptionOutput {
        try await self.client.execute(
            operation: "UpdateSubscription", 
            path: "/v1/UpdateSubscription", 
            httpMethod: .POST, 
            serviceConfig: self.config, 
            input: input, 
            logger: logger
        )
    }
    /// Changes the plan tier of an existing subscription.  Upgrades take effect immediately. Downgrades are scheduled and the current tier remains unchanged until the end of the billing cycle (calendar month). You cannot update a subscription while a scheduled change is pending. To make a new change, first cancel the pending change using CancelSubscriptionChange. This operation replaces the plan tier value. If you omit the optional usageLevel field, it is reset to the default.
    ///
    /// Parameters:
    ///   - arn: The ARN of the subscription to update.
    ///   - clientToken: A unique, case-sensitive identifier that you provide to ensure the request is handled only once.
    ///   - ifMatch: The ETag value from a previous GetSubscription or ListSubscriptions response. This ensures you are updating the expected version of the subscription.
    ///   - planTier: The new tier level for the subscription.
    ///   - usageLevel: The usage level within the plan tier. Specify DEFAULT for the base configuration. If omitted, the usage level is reset to the default.
    ///   - logger: Logger use during operation
    @inlinable
    public func updateSubscription(
        arn: String,
        clientToken: String? = UpdateSubscriptionInput.idempotencyToken(),
        ifMatch: String,
        planTier: String,
        usageLevel: String? = nil,
        logger: Logger = AWSClient.loggingDisabled        
    ) async throws -> UpdateSubscriptionOutput {
        let input = UpdateSubscriptionInput(
            arn: arn, 
            clientToken: clientToken, 
            ifMatch: ifMatch, 
            planTier: planTier, 
            usageLevel: usageLevel
        )
        return try await self.updateSubscription(input, logger: logger)
    }
}

extension PricingPlanManager {
    /// Initializer required by `AWSService.with(middlewares:timeout:byteBufferAllocator:options)`. You are not able to use this initializer directly as there are not public
    /// initializers for `AWSServiceConfig.Patch`. Please use `AWSService.with(middlewares:timeout:byteBufferAllocator:options)` instead.
    public init(from: PricingPlanManager, patch: AWSServiceConfig.Patch) {
        self.client = from.client
        self.config = from.config.with(patch: patch)
    }
}

// MARK: Paginators

@available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
extension PricingPlanManager {
    /// Return PaginatorSequence for operation ``listSubscriptions(_:logger:)``.
    ///
    /// - Parameters:
    ///   - input: Input for operation
    ///   - logger: Logger used for logging
    @inlinable
    public func listSubscriptionsPaginator(
        _ input: ListSubscriptionsInput,
        logger: Logger = AWSClient.loggingDisabled
    ) -> AWSClient.PaginatorSequence<ListSubscriptionsInput, ListSubscriptionsOutput> {
        return .init(
            input: input,
            command: self.listSubscriptions,
            inputKey: \ListSubscriptionsInput.nextToken,
            outputKey: \ListSubscriptionsOutput.nextToken,
            logger: logger
        )
    }
    /// Return PaginatorSequence for operation ``listSubscriptions(_:logger:)``.
    ///
    /// - Parameters:
    ///   - logger: Logger used for logging
    @inlinable
    public func listSubscriptionsPaginator(
        logger: Logger = AWSClient.loggingDisabled        
    ) -> AWSClient.PaginatorSequence<ListSubscriptionsInput, ListSubscriptionsOutput> {
        let input = ListSubscriptionsInput(
        )
        return self.listSubscriptionsPaginator(input, logger: logger)
    }
}

extension PricingPlanManager.ListSubscriptionsInput: AWSPaginateToken {
    @inlinable
    public func usingPaginationToken(_ token: String) -> PricingPlanManager.ListSubscriptionsInput {
        return .init(
            nextToken: token
        )
    }
}
