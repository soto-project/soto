//===----------------------------------------------------------------------===//
//
// This source file is part of the Soto for AWS open source project
//
// Copyright (c) 2017-2024 the Soto project authors
// Licensed under Apache License v2.0
//
// See LICENSE.txt for license information
// See CONTRIBUTORS.txt for the list of Soto project authors
//
// SPDX-License-Identifier: Apache-2.0
//
//===----------------------------------------------------------------------===//

// THIS FILE IS AUTOMATICALLY GENERATED by https://github.com/soto-project/soto-codegenerator.
// DO NOT EDIT.

#if canImport(FoundationEssentials)
import FoundationEssentials
#else
import Foundation
#endif
@_spi(SotoInternal) import SotoCore

extension Uxc {
    // MARK: Enums

    public enum AccountColor: String, CustomStringConvertible, Codable, Sendable, CodingKeyRepresentable {
        case darkblue = "darkBlue"
        case green = "green"
        case lightblue = "lightBlue"
        case none = "none"
        case orange = "orange"
        case pink = "pink"
        case purple = "purple"
        case red = "red"
        case teal = "teal"
        case yellow = "yellow"
        public var description: String { return self.rawValue }
    }

    // MARK: Shapes

    public struct GetAccountCustomizationsInput: AWSEncodableShape {
        public init() {}
    }

    public struct GetAccountCustomizationsOutput: AWSDecodableShape {
        /// The account color preference. A value of none indicates that you have not set a color.
        public let accountColor: AccountColor?
        /// The list of Amazon Web Services Region codes that are visible to the account in the Amazon Web Services Management Console. A value of null indicates that you have not configured this feature and all Regions are visible. For a list of valid Region codes, see Amazon Web Services Regions.
        public let visibleRegions: [String]?
        /// The list of Amazon Web Services service identifiers that are visible to the account in the Amazon Web Services Management Console. A value of null indicates that you have not configured this feature and all services are visible. For valid service identifiers, call ListServices.
        public let visibleServices: [String]?

        @inlinable
        public init(accountColor: AccountColor? = nil, visibleRegions: [String]? = nil, visibleServices: [String]? = nil) {
            self.accountColor = accountColor
            self.visibleRegions = visibleRegions
            self.visibleServices = visibleServices
        }

        private enum CodingKeys: String, CodingKey {
            case accountColor = "accountColor"
            case visibleRegions = "visibleRegions"
            case visibleServices = "visibleServices"
        }
    }

    public struct ListServicesInput: AWSEncodableShape {
        /// The maximum number of results to return per page.
        public let maxResults: Int?
        /// The token for retrieving the next page of results. Use the nextToken value from a previous response.
        public let nextToken: String?

        @inlinable
        public init(maxResults: Int? = nil, nextToken: String? = nil) {
            self.maxResults = maxResults
            self.nextToken = nextToken
        }

        public func encode(to encoder: Encoder) throws {
            let request = encoder.userInfo[.awsRequest]! as! RequestEncodingContainer
            _ = encoder.container(keyedBy: CodingKeys.self)
            request.encodeQuery(self.maxResults, key: "maxResults")
            request.encodeQuery(self.nextToken, key: "nextToken")
        }

        public func validate(name: String) throws {
            try self.validate(self.maxResults, name: "maxResults", parent: name, max: 1000)
            try self.validate(self.maxResults, name: "maxResults", parent: name, min: 1)
            try self.validate(self.nextToken, name: "nextToken", parent: name, max: 512)
            try self.validate(self.nextToken, name: "nextToken", parent: name, min: 32)
            try self.validate(self.nextToken, name: "nextToken", parent: name, pattern: "^[-A-Za-z0-9+/_]+=*$")
        }

        private enum CodingKeys: CodingKey {}
    }

    public struct ListServicesOutput: AWSDecodableShape {
        /// The token for retrieving the next page of results. This value is null when no more results are available.
        public let nextToken: String?
        /// The list of available Amazon Web Services service identifiers.
        public let services: [String]?

        @inlinable
        public init(nextToken: String? = nil, services: [String]? = nil) {
            self.nextToken = nextToken
            self.services = services
        }

        private enum CodingKeys: String, CodingKey {
            case nextToken = "nextToken"
            case services = "services"
        }
    }

    public struct UpdateAccountCustomizationsInput: AWSEncodableShape {
        /// The account color preference to set. Set to none to reset to the default (no color).
        public let accountColor: AccountColor?
        /// The list of Amazon Web Services Region codes to make visible in the Amazon Web Services Management Console. Set to null to reset to the default, which makes all Regions visible. For a list of valid Region codes, see Amazon Web Services Regions.
        public let visibleRegions: [String]?
        /// The list of Amazon Web Services service identifiers to make visible in the Amazon Web Services Management Console. Set to null to reset to the default, which makes all services visible. For valid service identifiers, call ListServices.
        public let visibleServices: [String]?

        @inlinable
        public init(accountColor: AccountColor? = nil, visibleRegions: [String]? = nil, visibleServices: [String]? = nil) {
            self.accountColor = accountColor
            self.visibleRegions = visibleRegions
            self.visibleServices = visibleServices
        }

        public func validate(name: String) throws {
            try self.visibleRegions?.forEach {
                try validate($0, name: "visibleRegions[]", parent: name, pattern: "^[a-z]{2}(-[a-z]{1,10}){1,2}-[1-9]$")
            }
            try self.validate(self.visibleRegions, name: "visibleRegions", parent: name, max: 100)
            try self.visibleServices?.forEach {
                try validate($0, name: "visibleServices[]", parent: name, max: 64)
                try validate($0, name: "visibleServices[]", parent: name, min: 1)
                try validate($0, name: "visibleServices[]", parent: name, pattern: "^[a-z0-9]+(-[a-z0-9]+)*$")
            }
            try self.validate(self.visibleServices, name: "visibleServices", parent: name, max: 500)
        }

        private enum CodingKeys: String, CodingKey {
            case accountColor = "accountColor"
            case visibleRegions = "visibleRegions"
            case visibleServices = "visibleServices"
        }
    }

    public struct UpdateAccountCustomizationsOutput: AWSDecodableShape {
        /// The current account color preference after the update.
        public let accountColor: AccountColor?
        /// The current list of visible Region codes after the update.
        public let visibleRegions: [String]?
        /// The current list of visible service identifiers after the update.
        public let visibleServices: [String]?

        @inlinable
        public init(accountColor: AccountColor? = nil, visibleRegions: [String]? = nil, visibleServices: [String]? = nil) {
            self.accountColor = accountColor
            self.visibleRegions = visibleRegions
            self.visibleServices = visibleServices
        }

        private enum CodingKeys: String, CodingKey {
            case accountColor = "accountColor"
            case visibleRegions = "visibleRegions"
            case visibleServices = "visibleServices"
        }
    }

    public struct ValidationException: AWSErrorShape {
        /// The list of fields that are invalid.
        public let fieldList: [ValidationExceptionField]?
        public let message: String

        @inlinable
        public init(fieldList: [ValidationExceptionField]? = nil, message: String) {
            self.fieldList = fieldList
            self.message = message
        }

        private enum CodingKeys: String, CodingKey {
            case fieldList = "fieldList"
            case message = "message"
        }
    }

    public struct ValidationExceptionField: AWSDecodableShape {
        /// A message describing the validation exception.
        public let message: String
        /// The field name with the validation exception.
        public let path: String

        @inlinable
        public init(message: String, path: String) {
            self.message = message
            self.path = path
        }

        private enum CodingKeys: String, CodingKey {
            case message = "message"
            case path = "path"
        }
    }
}

// MARK: - Errors

/// Error enum for Uxc
public struct UxcErrorType: AWSErrorType {
    enum Code: String {
        case accessDeniedException = "AccessDeniedException"
        case internalServerException = "InternalServerException"
        case throttlingException = "ThrottlingException"
        case validationException = "ValidationException"
    }

    private let error: Code
    public let context: AWSErrorContext?

    /// initialize Uxc
    public init?(errorCode: String, context: AWSErrorContext) {
        guard let error = Code(rawValue: errorCode) else { return nil }
        self.error = error
        self.context = context
    }

    internal init(_ error: Code) {
        self.error = error
        self.context = nil
    }

    /// return error code string
    public var errorCode: String { self.error.rawValue }

    /// You don't have sufficient access to perform this operation. Verify that your IAM policy includes the required uxc: permissions for the operation that you are calling. For more information on IAM permissions, see Amazon Web Services managed policies for Amazon Web Services Management Console.
    public static var accessDeniedException: Self { .init(.accessDeniedException) }
    /// The service encountered an internal error. Try your request again later.
    public static var internalServerException: Self { .init(.internalServerException) }
    /// The request was denied because of request throttling. Reduce the frequency of your requests.
    public static var throttlingException: Self { .init(.throttlingException) }
    /// The input fails to satisfy the constraints specified by an Amazon Web Services service.
    public static var validationException: Self { .init(.validationException) }
}

extension UxcErrorType: AWSServiceErrorType {
    public static let errorCodeMap: [String: AWSErrorShape.Type] = [
        "ValidationException": Uxc.ValidationException.self
    ]
}

extension UxcErrorType: Equatable {
    public static func == (lhs: UxcErrorType, rhs: UxcErrorType) -> Bool {
        lhs.error == rhs.error
    }
}

extension UxcErrorType: CustomStringConvertible {
    public var description: String {
        return "\(self.error.rawValue): \(self.message ?? "")"
    }
}
